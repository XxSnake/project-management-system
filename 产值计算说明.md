# 核心需求
    目前产值计算模块不能达到我想要的效果，因为计算规则比较复杂，请整理我下面的需求，重新设计项目的产值计算模块

## 合同部分

* **已签订合同** 

    * **定价方案 A** 按照各个检测项目单价和实际完成的检测数量计算

    * **定价方案 B** 如果合同是按照建筑面积签订的，那么根据每次完成的面积占比计算

        * **记录要求** 每次计算后需登记当次完成的比例

        * **上限预警** 累计完成产值总量不得超过合同总额的 **100%**

        * **超限处理** 如果完成总量已经超过了合同的 **100%** 那么在输出产值报表的时候，只输出工作量，但是超出的部分不计算产值

        * **自定义输入** 增加一个按钮，在无法确定当次检测活动占比时可以手动输入


    * **定价方案 C** 允许用户手动输入单次检测的产值信息，包括工作量和产值金额，并添加备注

* **未签订合同**

    * **定价方案** 因为没有检测项目单价，所以每个月的产值记录只输出工作量

        * **计算时限** 只输出工作量不计算产值的时限为：上传该项目关联合同之前，如果上传关联了该项目的合同，那么在上传关联之后的下个月开始，计算产值

        * **计算规则** 计算产值的当月，合并计算原来未计算的产值，下月开始，按照实际产生产值计算

        * **上限预警** 累计完成产值总量不得超过合同总额的 **100%**

        * **超限处理** 如果完成总量已经超过了合同的 **100%** 那么在输出产值报表的时候，只输出工作量，但是超出的部分不计算产值
        
        * **自定义输入** 增加一个按钮，在无法确定当次检测活动占比时可以手动输入
