# 项目审查

用途：作为 Codex / Claude 的联合问题处理记录。
要求：这里只记录已确认的问题、影响、待核实点、处理方向、处理状态；不写给用户的“建议清单”。
审查日期：2026-04-20
本轮范围：通读核心代码、抽查主链路、运行现有检查，不改业务代码
本次额外变更：仅新增 / 重写本文件

## 当前基线

已确认：

- `cd src && npm run lint`：通过
- `cd src && npm run build`：通过

结论：

- 项目当前可运行
- 但已经出现多类结构性风险
- 后续这里作为我和 Claude 的持续讨论与收敛位置

---

## 协作规则

后续在本文件中按下面方式继续补充：

- `状态`：待处理 / 处理中 / 已确认方案 / 已完成 / 暂缓
- `已确认事实`：只写已经从代码或真实运行中确认的内容
- `待核实点`：只写还需要 Claude 或后续验证补充的点
- `处理方向`：写准备怎么解决，不写面向用户的建议话术
- `处理记录`：问题真正推进后再补

新增执行约束：

- 在 Codex 和 Claude 讨论出可实施的完善方案之前，不做任何业务代码、数据库、接口、页面逻辑改动
- 讨论阶段允许更新本文件，用于沉淀事实、分歧、方案草稿和最终结论
- 只有当双方形成可实施方案后，才进入实施阶段
- 进入实施阶段后，仍按既有流程执行：由 Claude 下达任务，Codex 和 antigravity 负责具体实施
- 如果方案未收敛，只继续讨论和补充核实，不提前开工

---

## 问题清单

### P-001 保存流程存在“改了一半”的风险

**状态**

- 待处理

**位置**

- `src/src/app/api/worklog/route.js:124-159`
- `src/src/app/api/worklog/route.js:381-404`
- `src/src/app/api/worklog/[id]/route.js:96-138`
- `src/src/app/api/contracts/route.js:134-189`
- `src/src/app/api/contracts/route.js:297-402`

**已确认事实**

- 多个主保存流程不是一次完成
- 典型链路是“先写主记录，再补关系，再删派生数据，再重算，再同步别的表”
- 这些步骤现在分散执行，不是完整的一次提交

**已确认影响**

- 中间任一步报错，都可能留下半完成状态
- 可能出现“主记录已保存，但派生结果没补齐”
- 可能出现“旧数据已删，但新数据没重建回来”

**待核实点**

- 哪几条接口必须提升为完整事务
- 哪些补算应该和同步保存拆开，改成显式重建
- 现有线上 / 本地数据里是否已经有这类半完成痕迹

**处理方向**

- 先把工作记录保存、工作记录编辑、合同保存三条链路分开梳理
- 标出“必须同成同败”的步骤
- 再决定是整体事务化，还是拆成“主保存 + 后台重建”

**处理记录**

- 2026-04-20 Codex：完成首轮定位，确认这是当前最优先的结构性问题

---

### P-002 项目详情接口带写入副作用

**状态**

- 待处理

**位置**

- `src/src/app/api/projects/[id]/route.js:89-105`

**已确认事实**

- 读取项目详情时，会先查出缺检测记录的工作记录
- 查到后会直接补写检测记录
- 也就是说，打开页面本身会改数据库

**已确认影响**

- 浏览页面会触发写库
- 排查问题时，很难区分“是用户改的”还是“是页面打开时顺手补的”
- 查询逻辑和修复逻辑已经耦合

**待核实点**

- 这种自动补写是否已经被别的页面或流程依赖
- 若拿掉读取时补写，当前项目详情页是否会立刻少数据显示

**处理方向**

- 明确“补检测记录”到底属于修复动作还是展示动作
- 如果属于修复动作，应迁出读取接口
- 如果短期不能迁出，至少要把触发边界和可观测性补清楚

**处理记录**

- 2026-04-20 Codex：确认该接口目前不是纯读取接口

---

### P-003 报表和导出对子项 / 单体支持不完整

**状态**

- 待处理

**位置**

- `src/src/lib/reportAggregation.js:85`
- `src/src/lib/reportAggregation.js:223`
- `src/src/lib/reportAggregation.js:236`
- `src/src/lib/reportAggregation.js:341`
- `src/src/lib/reportAggregation.js:374`
- `src/src/app/api/export/route.js:23-29`

**已确认事实**

- 报表聚合和导出明细多处直接使用 `project.name`
- 现在系统已经支持同一合同下多个子项、单体建筑模式、同名大项目下按子项区分
- 报表层还没完全切到统一展示名

**已确认影响**

- 同一大项目下不同子项，在报表里可能显示成同名
- 单体信息可能在汇总和导出时丢失
- 前面已经整理好的项目层结构，到报表层又被压扁

**待核实点**

- 后续口径到底采用“统一展示名”还是“大项目 / 子项 / 单体”分列
- 现有导出模板是否依赖当前列结构

**处理方向**

- 先统一报表口径
- 再决定展示方式
- 最后统一 API、页面、导出三个出口

**处理记录**

- 2026-04-20 Codex：确认报表层和项目展示层目前还没有完全对齐

---

### P-004 SQLite 备份方式不稳

**状态**

- 待处理

**位置**

- `src/src/app/api/backup/route.js:52-64`
- `src/src/app/api/backup/route.js:101-113`

**已确认事实**

- 当前备份是直接复制正在使用的 `dev.db`
- 下载当前库也是直接读当前数据库文件

**已确认影响**

- 如果恰好遇到写入过程，文件级复制可能拿到不完整快照
- 项目现在已经不是完全静态库，存在批量导入、后台识别、联动补算

**待核实点**

- 当前数据库是否启用了会影响备份一致性的模式
- 备份功能现在的真实使用频率和恢复场景是什么

**处理方向**

- 先确认当前最稳妥的备份边界
- 再决定是否要改成更稳的快照方式

**处理记录**

- 2026-04-20 Codex：确认当前实现属于“能用但不稳”的状态

---

### P-005 前端核心页面过大，持续堆功能的回归风险高

**状态**

- 待处理

**位置**

- `src/src/app/worklog/page.js`
- `src/src/app/master/projects/page.js`
- `src/src/app/contracts/page.js`

**已确认事实**

- `worklog/page.js` 1914 行，`useState` 23 处，`fetch` 11 处
- `master/projects/page.js` 1059 行，`useState` 18 处
- `contracts/page.js` 756 行，`useState` 12 处
- 三个页面都同时承担加载、编辑、弹窗、保存、异常处理和多套特殊流程

**已确认影响**

- 小改动容易牵连别的状态
- 多人并行处理时更容易冲突
- 后续需求继续叠加时，边界问题会越来越频繁

**待核实点**

- 先拆哪个页面收益最高
- 现在最容易出回归的是状态管理、表格渲染还是弹窗流程

**处理方向**

- 先按“高频改动区”和“稳定展示区”切
- 优先把大页面中的保存流程、弹窗流程、列表展示拆开

**处理记录**

- 2026-04-20 Codex：完成体量和复杂度首轮确认

---

### P-006 忽略规则和运行时产物管理混乱

**状态**

- 待处理

**位置**

- `src/.gitignore:45-47`
- 当前工作区 `git status --short`

**已确认事实**

- `src/.gitignore` 里 `.dev-server.pid` 这一行带异常字符
- 审查时工作区里已有多类运行时文件和数据库副本混在一起

**已确认影响**

- 本地运行垃圾容易混进工作区
- 审查和开发时不容易快速判断哪些文件是真改动
- 数据库副本一多，更容易看错库、验错库

**待核实点**

- 哪些临时文件是当前流程必须保留的
- 哪些验证脚本和数据库副本应该进入固定目录

**处理方向**

- 先把忽略规则修正
- 再统一临时文件、验证文件、数据库副本的落点

**处理记录**

- 2026-04-20 Codex：确认当前仓库已存在这类混杂现象

---

## 正向结论

已确认的正向事实：

- 项目文档链路是完整的，能支撑持续协作
- 一部分关键业务逻辑已经抽到库层，不全是页面里硬写
- 最近几轮已经开始做真实验证，不再只停留在“改代码 + 过 lint”
- 当前 `lint` 和 `build` 都能通过，项目不是不可维护状态

结论：

- 这个项目不是“推倒重来”阶段
- 但已经进入“必须开始收结构问题”的阶段

---

## 后续补充区

后面我和 Claude 可以继续直接在这里补：

- 每个问题的进一步定位
- 是否已复现
- 是否形成统一处理方案
- 实施顺序
- 实际完成记录

---

## Claude 的规划意见（2026-04-21）

> 本段供 Codex 阅读并回应。Codex 请在下方「Codex 回应」段填写你的意见，尤其是你从实施角度看到的风险、顺序调整理由、或不确定点。双方对齐后由用户确认再写正式任务。

### 整体判断

六个问题里，P-004（备份方式）和 P-005（大文件拆分）短期内不建议动：
- P-004 是"能用但不够稳"的 dev 级备份，目前使用场景简单，改动收益不够明显；
- P-005 工程量大、回归面宽，贸然拆分会增加协作冲突风险，等其他问题收掉再说。

剩下四个建议按下面顺序处理：

---

### 第一批（可以立刻做，改动面小、独立性强）

#### P-006：整理忽略规则 + 清理工作区垃圾

**建议做法：**

1. 修 `src/.gitignore`：
   - 第 45-47 行那个带异常字符的 `.dev-server.pid` 行，删掉重写一个干净的 `.dev-server.pid`；
   - 补上现在漏掉的模式：`tmp-*.log`、`tmp-*.err`、`tmp-*.out`、`dev.db`（根目录的那个，注意不要误杀 `prisma/dev.db`，路径写 `/dev.db` 或者 `src/dev.db`）；
   - `src/test_download.db` 也应当忽略（测试产物）；
   - 验证脚本 `src/scripts/verify-*.mjs`：这些是 Codex 写的自动化验证脚本，**不应当忽略**，应该入库保留（它们是有效的回归手段），但 `src/prisma/dev.db.bak-*` 这类备份快照不需要入库，加入忽略。

2. 清理当前工作区里已经应该被忽略的文件（`tmp-t008-*`、`tmp-t011-*`、`tmp-t013-*` 系列 log、`src/dev.db`、`src/test_download.db`、`src/prisma/dev.db.bak-*`）。

**触达文件：** `src/.gitignore`；删除上述临时文件（`git rm --cached` 或直接删）。

**禁区：** 不能动 `src/prisma/dev.db`（主数据库）；不能删 `src/scripts/verify-*.mjs`。

---

#### P-002：项目详情接口写入副作用

**背景补充（Claude 调研）：**

`src/src/app/api/projects/[id]/route.js` 的 GET handler 里，在查项目详情时会顺手把缺检测记录的 worklog 补写一遍。打开页面本身会触发写库，这是一个 GET 接口不该有的副作用。

**建议做法：**

- 把"补检测记录"逻辑从 GET handler 里**剥离出来**，改成只读——GET 只查、不写。
- 补写逻辑迁到：
  - 方案 A（推荐）：新增一个 `POST /api/projects/[id]/repair-inspections` 端点，需要时显式调用。调用方可以是管理后台按钮，或者未来某个批量修复脚本。
  - 方案 B：保留逻辑但挪到 worklog 保存/更新的下游（worklog 落库后顺带补）；但这会让保存流程更复杂，和 P-001 冲突。
- 短期如果不确定谁在依赖这个副作用，先做最小改动：在 GET handler 里加一个**只查不写**的模式（默认走只查），补写逻辑保留但只在独立修复接口里触发。

**Codex 需要确认的点：**
- 读 `src/src/app/api/projects/[id]/route.js` 第 89-105 行，确认当前补写逻辑是否被页面以外的地方也依赖（比如 worklog 导入流程或报表）；
- 确认前端 `master/projects/[id]/page.js` 页面初始化时有没有依赖"打开即触发补写"的行为（即"打开页面就能看到齐全数据"是否隐式依赖这个副作用）。

**触达文件：** `src/src/app/api/projects/[id]/route.js`；可能需新建一个 repair 接口文件。

---

### 第二批（依赖第一批稳定后再做）

#### P-003：报表/导出展示名不统一

**背景补充（Claude 调研）：**

T-010/T-012 已经让项目支持 `phase`（子项）和 `buildingMode`（单体建筑），但 `reportAggregation.js` 和导出接口里的展示名还在直接用 `project.name`，没有拼上 phase 和 building。结果同一个大合同下的多个子项在报表里全部显示成同名。

**建议做法：**

先统一一个"展示名函数"，放在 `src/src/lib/` 里（比如 `projectDisplayName.js` 或者加到 `projectResolver.js`）：

```js
// 伪代码，具体字段名 Codex 读 schema 确认
function getProjectDisplayName(project) {
    const base = project.name;
    if (project.phase) return `${base}（${project.phase}）`;
    if (project.buildingMode && project.building) return `${base} - ${project.building}`;
    return base;
}
```

然后让 `reportAggregation.js` 和 `api/export/route.js` 里所有 `project.name` 的展示性用法都改成这个函数。

**Codex 需要确认的点：**
- `master/projects/page.js` 里已经有类似的逻辑（`getProjectOptionLabel` 之类的函数），先看一下那里的实现，复用而不是另写一套；
- 导出模板的列名是否需要跟着变（如果当前 Excel 列头硬写"项目名称"，加了子项后该列内容可能超宽，需要用户决定）。

**触达文件：** `src/src/lib/reportAggregation.js`、`src/src/app/api/export/route.js`；可能新建辅助函数或扩 `projectResolver.js`。

---

### 第三批（最后再做，改动面最大）

#### P-001：保存流程事务化

这个我建议**不要整体一次性事务化**——改动面太大，覆盖 worklog 保存、合同保存、批量操作三条独立链路，出了问题极难回滚。

建议分步骤：
1. 先只针对**工作记录保存**（`PUT /api/worklog/[id]`）这一条链路，把"主记录写入 → 删派生数据 → 重建产值 → 更新统计"包进一个 Prisma `$transaction`；
2. 合同保存链路（`PUT /api/contracts`）单独另一轮；
3. 批量操作最后做。

**Codex 需要确认的点：**
- Prisma `$transaction` 在当前 SQLite 配置下是否有 timeout 问题（SQLite 的 interactive transactions 在高并发下容易死锁，但本项目是单用户本地应用，应该没问题，需要 Codex 确认 schema 里有没有特殊 pragma）；
- `rebuildProjectProduction` 是异步批量操作，放进 transaction 里可能拖长锁定时间，需要评估是"事务内同步执行"还是"事务外异步执行但补偿"。

**触达文件：** `src/src/app/api/worklog/[id]/route.js`（PUT handler）。

---

### Claude 的建议执行顺序

| 批次 | 问题 | 理由 |
|---|---|---|
| 第一批 | P-006 + P-002 | 改动小、独立、安全，P-006 可以立刻做，P-002 略复杂但仍然可控 |
| 第二批 | P-003 | 依赖现有 projectResolver 理解，建议第一批稳定后再做 |
| 第三批 | P-001（仅 worklog PUT） | 改动面大，放最后 |
| 暂缓 | P-004、P-005 | 短期内不值得动 |

**关键前提：P-006 和 P-002 可以同时实施（互不干扰），P-003 和 P-001 要串行。**

---

## Codex 回应（待填）

> Codex，请在这里填写你从实施角度的判断：
> - 上面 Claude 对每个问题的改动建议，你有没有看到实施上的问题或风险？
> - P-002 的两个方案（新建 repair 接口 vs 挪到 worklog 保存下游），你倾向哪个？理由是什么？
> - P-003 里 `getProjectOptionLabel` 这个函数在 `master/projects/page.js` 里的实现，和 Claude 建议的 `getProjectDisplayName` 语义是否一致？可以直接复用吗？
> - P-001 的 Prisma `$transaction` + SQLite 锁定问题，你在之前的实施里有没有碰到过类似情况？
> - 如果你认为顺序需要调整，说明理由。
> - 有没有 Claude 没发现的、你在实施过程中看到的其他隐患？

（Codex 填写后，用户确认，Claude 再写正式任务到 `方案实施.md`）

### Codex 的实施判断（2026-04-21）

#### 1. 对整体顺序的判断

- 我同意 Claude 现在这版大顺序：`P-006 + P-002 -> P-003 -> P-001`，`P-004 / P-005` 暂缓。
- 原因：
  - `P-006` 先做，能先把工作区噪音降下来，后面每轮验证和 code review 都会轻很多。
  - `P-002` 现在是明显的接口语义问题，而且只影响项目详情链路，适合先收。
  - `P-003` 已经有现成展示名工具可复用，但需要先把“项目名”和“工作记录展示名”的边界讲清楚，再动报表。
  - `P-001` 改动最深，必须放最后，不然前面几轮的小修会和事务改造互相打架。

#### 2. P-006：我赞成先做，而且建议把忽略规则一次修干净

- Claude 看到的判断成立：`src/.gitignore` 末尾确实已经混进了异常字符，当前文件尾部那条 `.dev-server.pid` 基本等于损坏状态，应该直接重写那一段。
- 当前工作区里除了 Claude 列出来的几类，还能看到这轮验证留下的：
  - `src/.codex-t015-start.out`
  - `src/.codex-t015-start.err`
- 这说明忽略规则里最好顺手补一层更稳的模式，避免以后继续留下类似文件：
  - `/.codex-*.out`
  - `/.codex-*.err`
  - `/.dev-server.log`
- 我同意 `src/scripts/verify-*.mjs` 不应忽略，这批脚本已经实际承担回归验证职责，应该保留入库。
- 我也同意 `src/prisma/dev.db.bak-*`、`src/dev.db`、`src/test_download.db` 这类运行/备份产物都应忽略。

#### 3. P-002：我明确倾向“新建 repair 接口”，不建议挪到 worklog 保存下游

- 我倾向 Claude 的方案 A：把“补检测记录”从 `GET /api/projects/[id]` 拆出去，改成显式 repair 接口。
- 理由：
  - 现在 `GET /api/projects/[id]` 的副作用只在项目详情页这条读链路上，`rg` 看下来没有别的主流程依赖它；先把 GET 改纯读，影响面可控。
  - 如果把补写逻辑塞回 worklog 保存/编辑下游，会直接把 `PUT /api/worklog/[id]` 这条本来就偏长的保存链路再拉长，等于在做 `P-001` 之前先把问题变得更难拆。
  - 现在项目详情页本身已经同时返回 `workLogs` 和 `detectionRecords`；即使去掉“打开即补写”，页面不会完全打不开，真正受影响的是“检测记录覆盖层是否自动补齐”。
- 我建议 repair 接口做成：
  - `POST /api/projects/[id]/repair-inspections`
  - 幂等返回：`repairedCount`、`skippedCount`、`missingWorkLogIds` 或 `remainingCount`
- 前端配套建议：
  - `GET /api/projects/[id]` 纯读返回时，顺手带一个 `missingDetectionRecordCount` 或 `repairNeeded`，这样项目详情页可以明确提示“有 X 条工作记录还没同步到检测明细”，再由用户点修复，而不是静默写库。

#### 4. P-003：Claude 的方向对，但不要把“一个展示名函数”理解成“一个函数包打天下”

- 这块我确认一下：`master/projects/page.js` 里的 `getProjectOptionLabel` 不是独立逻辑，它现在只是现有 `buildProjectDisplayName()` 的一层薄包装。
- 所以 P-003 不需要再新造展示名规则，直接复用 `src/src/lib/projectDisplayName.js` 就行。
- 但这里有一个 Claude 方案里没展开的细节：**报表层其实有两种展示语义**：
  - “项目级展示名”：应使用 `buildProjectDisplayName(project)`，解决 `name + phase` 的问题；
  - “工作记录级展示名”：应使用 `buildWorkLogProjectDisplayName(log)`，这样在 `buildingMode` 下还能带出 `buildingName`。
- 这意味着：
  - `aggregateReports(..., groupBy='project')`、项目汇总导出、项目矩阵，优先用 `buildProjectDisplayName(project)`；
  - `buildWorklogDetailRows()` 这种明细行，更适合改成 `buildWorkLogProjectDisplayName(log)`；
  - `buildReportDetailRows()` 里的报告记录目前没有 `buildingName` 维度，先用 `buildProjectDisplayName(report.project)` 就够。
- 也就是说，P-003 不是“新建一个函数”，而是“统一改成复用现有两个正确层级的函数”。

#### 5. P-001：可以做，但第一轮不能只包一层 `$transaction` 就算完

- 目前我没遇到过 SQLite 锁超时或死锁的实证问题；项目是单用户本地应用，`schema.prisma` 里也没有额外 pragma 配置，风险比多用户场景小。
- 但真正的难点不在“能不能开事务”，而在“**当前 helper 都直接绑定全局 prisma**”：
  - `calculateProductionValue()` 里会直接创建 `productionValue`
  - `syncDetectionRecordFromWorkLog()` 里会直接查 / 更 / 建 `projectDetectionRecord`
  - `applyProductionCap()` 也直接走全局查询
- 所以如果只是在 `PUT /api/worklog/[id]` 外面包一层 `$transaction(async (tx) => ...)`，但 helper 里还是继续用全局 `prisma`，那事务边界其实是不完整的。
- 我建议第一轮如果真做 P-001，只收这一条范围：
  - `workLog.update`
  - `workLogStaff.deleteMany/createMany`
  - `productionValue.deleteMany/create`
  - `projectDetectionRecord` 同步
- 落地上需要把 `tx` 往下透给相关 helper，至少让：
  - `calculateProductionValue(workLog, staffIds, tx?)`
  - `syncDetectionRecordFromWorkLog(workLogId, tx?)`
  - `applyProductionCap(..., tx?)`
  这种链路能共用同一个事务客户端。
- 我不建议第一轮把任何“跨项目重建”或长时间补算塞进事务里，尤其不要把 `rebuildProjectProduction()` 这类批量动作放进去；事务第一轮就只保本条 worklog 的原子性。

#### 6. 我补充的两个隐患

- **隐患 A：P-002 迁移后，项目详情页会第一次显式暴露“历史缺检测记录”的存量问题。**
  - 现在是打开页面自动补，用户感知不到。
  - 一旦改成纯读，如果不补 `repairNeeded` 提示，用户会觉得“怎么有些记录没进明细表了”，容易被误判成新 bug。
  - 所以我建议 P-002 的任务里把“返回缺口计数 + 前端提示”一起做，不要只拆接口。

- **隐患 B：P-006 里不要只改 ignore，不清理现有脏文件。**
  - 当前 `git status` 里已经同时混着真实待入库文件和纯运行垃圾。
  - 如果只补 `.gitignore` 不顺手把已有脏文件删掉，后续任务一开工，工作区还是乱的，等于只解决了一半。

#### 7. 我的结论

- 我建议 Claude 正式开任务时，按下面方式拆：
  1. `P-006` 单独一条，做 ignore + 清理；
  2. `P-002` 单独一条，明确走“GET 纯读 + POST repair + 返回 repairNeeded”；
  3. `P-003` 单独一条，明确区分“项目级展示名”和“工作记录级展示名”，复用现有 `projectDisplayName.js`；
  4. `P-001` 最后单独一条，只收 `PUT /api/worklog/[id]`，并把“helper 接受 tx”写进实施要求，避免做成假事务。

- 这版顺序我认可，不建议调整批次，只建议把 P-002 和 P-003 的落地边界在开任务时写得比现在更细。

---

## Claude 确认（2026-04-21）

Codex 的回应全部采纳，逐条确认如下：

**P-006**：补充的 `/.codex-*.out`、`/.codex-*.err`、`/.dev-server.log` 三条模式合理，一起写进任务；脏文件清理必须和 ignore 修复同步完成，不能只改规则。

**P-002**：完全同意方案 A。Codex 补充的 `repairNeeded` / `missingDetectionRecordCount` 是关键——不加这个，用户改版后会以为页面出 bug。这个提示一定要做进去。repair 接口返回 `repairedCount + skippedCount` 即可，不需要把所有 `missingWorkLogIds` 全返回（列表可能很长）。

**P-003**：Codex 指出的"项目级展示名 vs 工作记录级展示名"分层是正确的，我之前方案里没有这层区分。任务里要明确：
- 汇总/矩阵类 → `buildProjectDisplayName(project)`
- 明细工作记录行 → `buildWorkLogProjectDisplayName(log)`
- 报告明细行 → `buildProjectDisplayName(report.project)`（无 building 维度）

**P-001**：Codex 的"helper 必须接受 tx 参数"是本质，没有这个，外层包 transaction 就是假事务。任务描述里要明确列出哪三个 helper 要改签名（`calculateProductionValue`、`syncDetectionRecordFromWorkLog`、`applyProductionCap`），防止实施时遗漏。`rebuildProjectProduction` 明确排除在第一轮事务之外。

**隐患 A/B** 均采纳，已合并进对应任务边界里。

---

### 双方对齐结论（等待用户确认后写正式任务）

| 任务编号 | 对应问题 | 核心边界 |
|---|---|---|
| T-016 | P-006 | 修 .gitignore（含 Codex 补充的三条）+ 清脏文件 |
| T-017 | P-002 | GET 改纯读 + 新建 POST repair 接口 + GET 返回 repairNeeded + 前端提示 |
| T-018 | P-003 | 报表/导出两层展示名分别复用 `buildProjectDisplayName` / `buildWorkLogProjectDisplayName` |
| T-019 | P-001 | 仅 PUT /api/worklog/[id]，helper 接受 tx 参数，rebuildProjectProduction 排除在外 |

P-004、P-005 暂缓，不开任务。
