# 改动说明

本文件是本仓库的**变更日志 + 协作规范文档**，服务于以下场景：
- 在**两台电脑**（办公室 / 家里）之间接力开发
- 使用**多个 AI 编码助手**（Claude Code、Codex、Antigravity 等）交替开发

> **核心原则：每次改动完成后就更新本文件，最晚不得晚于 commit/push 前。目标是让另一台电脑上的另一个模型不看聊天记录，也能直接接手。**

---

## 协作规范（所有 AI 助手必读）

### 1. 变更记录规则

- 每次改动完成后**必须**更新本文件，不能等到下次顺手再补
- 每次 commit/push 前再检查一次本文件，确保这次改动已经写进去
- 记录格式见下方模板，关键字段：日期、提交哈希、改动摘要、触达文件、数据库迁移、验证结果、遗留事项
- 记录按**时间倒序**排列（最新的在最上面）
- 记录要写得让别的模型和别的电脑都能直接看懂，不能依赖某段聊天上下文
- 只要还有没做完的后续事项，就必须写进本文件的「当前待办」或当前记录的「遗留/注意」

### 2. 多机接力开发

- 用户在两台电脑之间通过 `git push` / `git pull` 同步代码
- 每次在新机器上开工前，先读本文件最上面的最新记录和「当前待办」
- 每次在新机器上 `git pull` 后，检查本文件最新记录的「数据库迁移」和「遗留/注意」
- 如果标注需要迁移，执行 `cd src && npx prisma db push`（本项目使用 SQLite + db push，不用 migrate）
- 如果 Prisma Client 报错（如 `Unknown argument`），先停 dev server 再执行 `npx prisma generate`

### 3. 多模型协作要求

- Claude Code、Codex、Antigravity 接手前都先看本文件，不要假设上一个模型的上下文还在
- 记录里优先写清楚**结果、影响范围、是否需要手动操作、是否影响数据库、还剩什么没做**
- 如果同一件事已经在后续记录中修过，旧记录里的遗留说明要补一句“已在后续解决”，避免误导
- 涉及规则类改动时，要把规则写成稳定说法，比如“布点只记工作量，不计入产值”，不要只写成临时聊天表述
- 如果改动会影响其他模型继续开发的方式，也要同步更新本文件里的协作规范

### 3.1 多模型分工协作（2026-04-14 起生效，2026-04-16 更新）

**角色分工：**
- **Claude**：只负责规划、验证、整理规范，不直接改源码
- **Codex / Antigravity**：负责实施，按 `方案实施.md` 里标记为 🟡 的任务动手
- **用户**：在各模型之间传递成果，充当调度和裁判

**多实施者并行规则（重要）：**
- Codex 和 Antigravity 可能**同时接到不同任务**，各自独立实施
- 每个实施者开工前**必须先读本文件最新记录 + `方案实施.md` 全部任务状态**，确认自己要做的任务仍然是 🟡，没有被另一方抢先完成或正在做
- 如果发现目标任务已经是 🔵（别人在做）或 🟢/✅（已完成），**不要重复做**，改做下一个 🟡 任务或挂起等用户指示
- 每个实施者完成后，必须在"实施记录"段和 `改动说明.md` 的变更记录里标注**由谁完成**，格式：`（该部分由 Codex 完成）` 或 `（该部分由 Antigravity 完成）`

**完成后交付流程（Codex / Antigravity 都遵守）：**
1. 在 `方案实施.md` 里把对应任务状态从 🔵 改成 🟢 并填"实施记录"段，末尾注明 `（该部分由 XXX 完成）`
2. 在 `改动说明.md` 顶部（当前待办下面）追加一条变更记录，标题带上任务 ID（如 `[T-009]`），正文末尾也注明 `（该部分由 XXX 完成）`
3. **不要碰另一个实施者正在做或已完成的任务的代码和文件**

**Claude 复验流程：**
- Claude 每次接手，必读顺序：`改动说明.md` 最新记录 → `方案实施.md` 相关任务 → 确认是谁完成的 → 再开始验证
- 验证通过 → `方案实施.md` 里标 ✅；不通过 → 标 ❌ 并在任务下追加"反馈"段，任务状态回到 🟡

### 4. 技术栈与项目结构

| 层 | 技术 |
|---|---|
| 框架 | Next.js 14+ App Router（`src/src/app/`） |
| ORM | Prisma + SQLite（`src/prisma/schema.prisma`，`src/prisma/dev.db`） |
| 样式 | 全局 CSS（`src/src/app/globals.css`），无 Tailwind |
| 状态 | 纯 React useState，无状态库 |
| 部署 | 本地开发，`start-dev-hidden.vbs` 启动 |

关键目录：
```
src/
├── prisma/schema.prisma          # 数据模型定义
├── src/app/api/                  # API 路由（REST）
├── src/app/master/               # 主数据管理页面（项目、人员等）
├── src/app/contracts/            # 合同管理页面
├── src/lib/                      # 业务逻辑（产值计算、OCR 解析等）
├── src/components/               # 共享组件（Sidebar 等）
└── .env                          # 环境变量（API Key 等，不入库）
```

### 5. 编码规范

- **文件编码**：UTF-8，LF 换行（Windows 上 git 会自动处理 CRLF）
- **命名**：API 路由用 RESTful 风格（GET/POST/PUT/DELETE），函数用 camelCase
- **数据库变更**：修改 `schema.prisma` 后用 `npx prisma db push` 同步，不使用 migration 文件
- **构建验证**：每次改动后执行 `npx next build` 验证，确保无编译错误
- **文档同步**：只要有功能、规则、接口、数据库、流程或待办发生变化，必须同步更新本文件；必要时一并更新 `docs/api_reference.md`
- **Prisma Client**：修改 schema 后如果 dev server 正在运行，需要先停 server 再 `npx prisma generate`
- **前端组件**：所有页面组件都是 `'use client'`，使用 `useSearchParams()` 的页面需要 `<Suspense>` 包裹
- **产值计算**：四种计价方式（unit/area/mixed/lumpsum），逻辑分布在 `productionCalculator.js`、`productionCap.js`、`worklogBilling.js`

### 6. Git 工作流

- 单分支 `master`，直接 push
- commit message 用中文，简明扼要说明改了什么
- `.gitignore` 排除：`node_modules`、`.next`、`contracts/`（上传的合同文件）、`package-lock.json`、`.env`
- **已纳入版本控制**：`*.db`（数据库文件，多机同步）、`*.vbs` / `*.cmd`（启动停止脚本）
- commit/push 前检查本文件是否已经写入这次改动；没写就不能当成已交接完成

### 7. 当前功能模块状态

| 模块 | 状态 | 说明 |
|------|------|------|
| 工作记录 | 稳定 | 手动填写 + Excel 导入，自动识别/创建项目 |
| 合同管理 | 活跃开发 | OCR 上传解析 + 手动填写，支持编辑已有合同 |
| 项目管理 | 稳定 | CRUD + 合同关联 + 项目合并 |
| 项目详情 | 稳定 | 统一表格（合同+检测+报告），合同弹窗查看 |
| 产值计算 | 稳定 | 四种计价方式，100% 上限检查 |
| 产值报表 | 稳定 | 按人员/项目分组统计 |
| 内部指导价 | 暂停 | 侧边栏已隐藏，代码保留，暂不参与产值计算 |
| OCR 解析 | 需优化 | GLM-5 思考模型 reasoning_content 解析已加 fallback |

---

## 变更记录模板

```markdown
## YYYY-MM-DD — 简要标题

**提交：** `xxxxxxx`

**改动：**
1. ...

**触达文件：**
- `path/to/file`

**数据库迁移：** 是/否 — 说明

**验证：**
- ...

**遗留/注意：**
- ...
```

---

## 当前待办（2026-04-22）

> **注：** 从 2026-04-14 起，具体任务转到 `方案实施.md` 统一排队。本节只留总览和状态，详情看 `方案实施.md`。

- **T-001 [P0] ✅** 修复产值匹配的单位不一致 bug（清华园项目 818 1 月产值虚高 74 万） —— **Claude 已于 2026-04-15 复验通过**
- **T-007 [P0] ✅** 邓川片区项目合并 + 单体建筑模式 —— **Claude 已于 2026-04-16 复验通过**
- **T-008 [P1] ✅** 同合同多项目”子项”引导 + phase=null 告警 —— **Claude 已于 2026-04-16 复验通过**
- **T-009 [P1] ✅** 合同项目组概览 + 批量整理子项名称 + 工作记录按子项展示 —— **Claude 已于 2026-04-16 复验通过**（Codex 完成）
- **T-010 [P0] ✅** 放开子项 + 单体建筑共存 & 合并古生种质二期保育室 —— **Claude 已于 2026-04-16 复验通过**（Antigravity 完成）
- **T-012 [P0] ✅** 合同项目组整理扩展：支持"合并 / 子项 / 单体"三种完整层级操作 —— **Claude 已复验通过**（Antigravity 完成）
- **T-011 [P0] ✅** 工作记录导入"近似同名项目识别 + 人工确认"流程 —— **Claude 已于 2026-04-17 复验通过**（Codex 完成）
- **T-013 [P1] ✅** 拆分工作记录改为"复制一份保留原记录 + 自由编辑副本" —— **Claude 已于 2026-04-20 复验通过**（Codex 完成）
- **T-017 [P1] ✅** 项目详情 GET 改纯读 + 单独 repair 入口 + 页面提示 —— **Claude 已于 2026-04-21 复验通过**（Codex 完成）
- **T-016 [P1] ✅** 修 `.gitignore` + 清理工作区脏文件 —— **Claude 已于 2026-04-21 复验通过**（Codex 完成）
- **T-015 [P0] ✅** 工作记录界面 area / mixed / lumpsum 三种合同都支持手动占比 + 手动产值 —— **Claude 已于 2026-04-21 复验通过**（Codex 完成）
- **T-014 [P1] ✅** 合同管理界面点工程名打开合同详情弹窗（支持编辑 `priceItems`） —— **Claude 已于 2026-04-21 复验通过**（Codex 完成）
- **T-018 [P2] ✅** 报表/导出展示名统一 —— **Claude 已于 2026-04-22 复验通过**（Codex 完成）
- **T-019 [P2] ✅** worklog PUT 事务化 —— **Claude 已于 2026-04-22 复验通过**（Codex 完成）
- **T-002 [P1] ⏸️** Mixed 合同区分"单价条 vs 打包条"（涉及 schema，等 T-001 决定后再议；注：T-015 部分解决了 mixed 下的占比诉求，T-002 的"按条级别区分"仍是单独问题，等 T-020 收件箱落地后再评估）
- **T-003 [废弃]** → 2026-04-22 归并进 T-020「数量异常」tab
- **T-004 [废弃]** → 2026-04-22 归并进 T-021「缺占比」tab（支持批量分摊）
- **T-005 [废弃]** → 2026-04-22 归并进 T-022「项目未绑合同」tab
- **T-006 [废弃]** → 2026-04-22 归并进 T-020「缺单价」tab
- **T-020 [P0] ✅** 异常态收件箱骨架 + 基础规则落地 —— **已于 2026-04-22 复验通过**（Codex 完成）
- **T-021 [P1] ✅** E5 缺占比批量处理（依赖 T-020） —— **Claude 已于 2026-04-23 复验通过**（Codex 完成）
- **T-022 [P1] ✅** E7 项目级 `noContractExpected` 标记（依赖 T-020） —— **Claude 已于 2026-04-23 复验通过**（Codex 完成）
- **T-023 [P2] ✅** E8 LLM 项目名判重（依赖 T-020） —— **Claude 已于 2026-04-27 第二轮复验通过**（Codex 完成）。首轮反馈的乱码文案、验证脚本断言、后台旧扫描覆盖新状态都修好；`lint` / `build` / `node src/scripts/verify-project-fuzzy-match.mjs` 三连过，DOM 文本断言和 confirm/merge 错误消息断言能直接挡住未来同类问题
- **T-024 [P1] ✅** E9 产值超限进收件箱 + 修复动作（依赖 T-020） —— **Claude 已于 2026-04-23 复验通过**（Codex 完成）

~~用今天修好的导入规则回补 q=0~~ → 已由 2026-04-14《批量回补 quantity=0》完成

---

## 2026-04-27 — [T-023] 修复 E8 复验反馈：乱码文案、验证断言和后台旧扫描覆盖
**提交：** `(待提交)`

**改动：**
1. 修正了 E8 收件箱里用户能看到的乱码文案，包括“已并入”“候选”“已不存在”“疑似重名提示”等。
2. 修正了 E8 确认 / 合并接口缺参数时的错误提示，现在统一返回正常中文 `缺少有效的项目 ID`。
3. 补强 `verify-project-fuzzy-match.mjs`：复验时不再只检查页面能打开，还会检查页面包里有正确中文文案，并检查两条接口的错误提示。
4. 复验脚本重跑时发现后台判重还有一个隐藏并发问题：旧扫描可能在用户确认或改名后才写回，重新把项目推回 E8。已给判重写入加快照保护，过期结果不会覆盖新状态。

**触达文件：**
- `src/src/app/master/inbox/page.js`
- `src/src/app/api/inbox/fuzzy-match/confirm/route.js`
- `src/src/app/api/inbox/fuzzy-match/merge/route.js`
- `src/src/lib/projectFuzzyMatchScheduler.js`
- `src/scripts/verify-project-fuzzy-match.mjs`
- `改动说明.md`
- `方案实施.md`

**数据库迁移：** 否

**验证：**
- `cd src && npm run lint`：通过
- `cd src && npm run build`：通过
- `cd src && node scripts/verify-project-fuzzy-match.mjs`：通过
  - 相似项目进入 E8
  - 确认“不是同一个项目”后从 E8 消失
  - 改名后再次扫描不会回到 E8
  - 并入候选项目后 E8 清空
  - 页面中文文案和两条接口错误提示都被脚本实际检查

**遗留/注意：**
- 本轮没有改数据库结构，也没有改正式业务数据。
- 第一次在沙箱内跑复验脚本时 Prisma 本地引擎被 Windows 拦截，放到沙箱外重跑后已通过。 （该部分由 Codex 完成）

---

## 2026-04-24 — [T-023] E8 项目疑似重名进收件箱，支持确认独立 / 合并 / 改名
**提交：** `(待提交)`

**改动：**
1. 项目现在会在新建、编辑、批量整理、项目合并后异步跑一轮后台判重。它会先按项目名相似度筛候选，再用文本模型判断是不是值得人工复核；命中后把项目挂进 E8“项目疑似重名”。
2. 收件箱里的 E8 不再只是展示。每条疑似重名项目都可以直接做三件事：确认“不是同一个项目”、跳去项目页改名、或者把当前项目并入候选项目。
3. 我把 E8 从 worklog 异常里拆开了：它有自己单独的收件箱数据结构，也有自己单独的确认/合并接口，不会再误走“已知忽略”那套 worklog 逻辑。
4. 另外补了一个手动触发扫描的入口，最终地址是 `POST /api/internal/run-fuzzy-match`。原先放在 `_internal` 目录下的写法在当前框架里实际上不会暴露成路由，实测会 404，这次一起修正了。
5. 为了方便反复复验，我补了 `src/scripts/verify-project-fuzzy-match.mjs`：脚本会起临时库、起本地假模型服务、跑完整条 E8 流程，不依赖外部模型配置。

**触达文件：**
- `src/prisma/schema.prisma`
- `src/prisma/migrations/20260423143000_add_project_fuzzy_match_fields/migration.sql`
- `src/src/lib/projectFuzzyMatchLLM.js`
- `src/src/lib/projectFuzzyMatchScheduler.js`
- `src/src/lib/projectMerge.js`
- `src/src/lib/workLogExceptions.js`
- `src/src/app/api/projects/route.js`
- `src/src/app/api/projects/batch-rename/route.js`
- `src/src/app/api/projects/merge/route.js`
- `src/src/app/api/inbox/exceptions/route.js`
- `src/src/app/api/inbox/acknowledge/route.js`
- `src/src/app/api/inbox/reset-acknowledgement/route.js`
- `src/src/app/api/inbox/fuzzy-match/confirm/route.js`
- `src/src/app/api/inbox/fuzzy-match/merge/route.js`
- `src/src/app/api/internal/run-fuzzy-match/route.js`
- `src/src/app/master/inbox/page.js`
- `src/src/app/master/projects/page.js`
- `src/scripts/verify-project-fuzzy-match.mjs`
- `docs/api_reference.md`
- `改动说明.md`
- `方案实施.md`

**数据库迁移：** 是 —— 已新增 `Project.fuzzyMatchStatus`、`Project.fuzzyMatchCandidateIds`、`Project.fuzzyMatchedAt`，并执行过 `cd src && npx prisma db push && npx prisma generate`

**验证：**
- `cd src && npx prisma generate`：通过
- `cd src && npx prisma db push`：通过
- `cd src && npm run lint`：通过
- `cd src && npm run build`：通过
- `node src/scripts/verify-project-fuzzy-match.mjs`：通过
  - 新建一条与“E8 判重验证项目一期（总包）”近似的新项目后，会进入 E8，候选里带上原项目
  - 调 `POST /api/inbox/fuzzy-match/confirm` 后，这条 E8 记录立即消失
  - 把该项目改名成明显不同的“二期（独立项目）”后，再次扫描返回 `distinct`，不会重新进入 E8
  - 再造一条相似项目并调 `POST /api/inbox/fuzzy-match/merge` 后，来源项目被删掉，E8 清空
  - 同一轮脚本里还额外确认 `/master/inbox` 页面请求可正常返回 `200`

**遗留/注意：**
- 这轮把 E8 的手动扫描入口最终定成了 `/api/internal/run-fuzzy-match`；后续再写脚本或文档时不要继续用旧的 `_internal` 路径。
- 浏览器自动点页面这件事，在这台机器上仍然受 Chromium `spawn EPERM` 限制；这次主要靠临时库 + 真接口脚本把整条流程跑通。 （该部分由 Codex 完成）

---

## 2026-04-23 — [T-024] 收件箱 E9 支持“接受 cap / 改合同额 / 去总账处理”
**提交：** `(待提交)`

**改动：**
1. 收件箱里的“产值超限”现在不只是显示。每条超限记录都能直接做三件事：接受当前截断结果、跳去合同页改合同总额、跳去工作记录总账继续拆分或改绑项目。
2. “接受 cap”这条路单独做了说明，避免和普通“忽略异常”混淆。页面会明确告诉用户：这不是忽略错误，而是确认“当前截断后的产值就是我要保留的结果”。
3. “去总账处理”不再只是打开总账首页。现在会把来源项目和“产值超限”状态一起带过去，总账页会自动筛到对应项目的超限记录，并选中触发来源那条记录，减少二次查找。
4. API 文档同步补了 E9 的说明：`exceptionType=exceeded` 表示接受当前 cap 结果，不是普通忽略。

**触达文件：**
- `src/src/app/master/inbox/page.js`
- `src/src/app/worklog/page.js`
- `docs/api_reference.md`
- `改动说明.md`
- `方案实施.md`

**数据库迁移：** 否

**验证：**
- `cd src && npm run lint`：通过
- `cd src && npm run build`：通过
- 用临时库复制 `src/prisma/dev.db` 后，启动 `next start` 做接口实测：
  - 造出一组“合同额 10000、累计产值 12000”的临时数据后，相关记录会进入 E9
  - 调 `POST /api/inbox/acknowledge` 传 `exceptionType=exceeded` 后，记录立即从 E9 消失
  - 调 `POST /api/inbox/reset-acknowledgement` 后，记录会重新出现在 E9
  - 把合同金额改到 `15000` 后，记录自动重算并从 E9 消失
  - 同时核对到：超限时第二条记录被截到 `4000`，合同额调大后恢复成 `6000`
- 额外尝试用 Playwright 实点 `/master/inbox -> 去总账处理 -> /worklog`，但这台环境启动 Chromium 仍是 `spawn EPERM`，所以浏览器级点击链路没法在本机跑完；对应页面代码已接好，构建通过。

**遗留/注意：**
- 本轮没有改 cap 算法，只补了收件箱动作和跳转流程，符合任务边界。
- 浏览器自动化验证依旧受这台机器限制；后续 Claude / 用户若要复验页面交互，需要在能启动浏览器的环境里点一遍。 （该部分由 Codex 完成）

---

## 2026-04-22 — [T-022] 无合同项目可直接标记为“无需合同”，支持批量处理
**提交：** `(待提交)`

**改动：**
1. 项目详情页新增“合同要求”卡片。现在无合同项目可以直接勾选“此项目无需合同”，保存后这个项目下的无合同工作记录会自动从 E7 收件箱消失；取消勾选后会重新出现。
2. 项目列表页新增“合同策略”列，明确区分“按合同处理”“待关联合同”“无需合同”；并补了单条切换和批量切换按钮，方便把一批内部项目、试点项目一次性处理掉。
3. 新增 `PUT /api/projects/[id]`，专门接收 `noContractExpected` 更新；同时补了后端保护：已关联合同的项目不能被标成“无需合同”，如果后来重新关联合同，这个标记也会自动清掉，避免数据互相打架。

**触达文件：**
- `src/src/app/api/projects/[id]/route.js`
- `src/src/app/api/projects/route.js`
- `src/src/app/master/projects/[id]/page.js`
- `src/src/app/master/projects/page.js`
- `docs/api_reference.md`
- `改动说明.md`
- `方案实施.md`

**数据库迁移：** 否

**验证：**
- `cd src && cmd /c npm run lint`：通过。
- `cd src && cmd /c npm run build`：通过。
- 复制 `src/prisma/dev.db` 到临时库后，真实调用构建产物里的 `GET /api/inbox/exceptions?type=workload-only` + `PUT /api/projects/[id]`：
  - 当前 E7 对应的无合同项目仍是 13 个，项目 ID 为 `816, 817, 820, 821, 823, 826, 827, 831, 839, 843, 844, 849, 850`。
  - 把项目 `816` 标记为“无需合同”后，它立刻从 E7 消失；再取消标记后，会重新出现在 E7。
  - 对这 13 个项目逐个走一遍“标记为无需合同”后，E7 收件箱清零。
  - 另拿一个已关联合同的项目 `811` 去试，接口返回 `400`，正确拦截了非法设置。

**遗留/注意：**
- 这轮没有在收件箱页额外补 E7 的批量按钮，批量处理入口放在项目列表页里。
- 当前终端环境里本地页面启动脚本依旧不稳定，所以这轮验证仍以 `build` + 构建产物直调接口 + 临时库真实写入校验为主。
（该部分由 Codex 完成）

## 2026-04-22 — [T-021] 缺占比支持批量分摊，先预览再统一保存
**提交：** `(待提交)`

**改动：**
1. 收件箱 E5 tab 现在支持勾选、多选、按合同整组选择，以及“全选 / 反选 / 清空”这类批量操作；只有同一合同下的一组记录才能进入下一步，避免误把两份合同混在一起处理。
2. 新增批量预览弹窗，支持“按数量加权”“平均分”“统一百分比”三种策略；用户先看到每条的新占比和预计产值，确认后才统一保存。
3. 新增 `POST /api/inbox/batch-allocate` 接口，真正落库时复用现有 worklog 更新链路，确保占比写入后会同步重算产值，不会出现“批量页一套、单条编辑一套”的结果。
4. 顺手把一条本来不该出现在 E5 的布点类记录 `#5426` 剔除了。这类记录按现有规则本来就“不计产值”，继续混在“缺占比”里只会造成假异常和假待办。

**触达文件：**
- `src/src/lib/inboxBatchAllocation.js`
- `src/src/lib/workLogMutations.js`
- `src/src/lib/workLogExceptions.js`
- `src/src/app/api/inbox/batch-allocate/route.js`
- `src/src/app/api/worklog/[id]/route.js`
- `src/src/app/master/inbox/page.js`
- `docs/api_reference.md`
- `改动说明.md`
- `方案实施.md`

**数据库迁移：** 否

**验证：**
- `cd src && cmd /c npm run lint`：通过。
- `cd src && cmd /c npm run build`：通过。
- 复制 `src/prisma/dev.db` 到两份临时库后，真实调用构建产物里的 `GET /api/inbox/exceptions` + `POST /api/inbox/batch-allocate`：
  - 当前可批量处理的 E5 实际是 **19 条**，其中项目 `819` 为 **14 条**、项目 `824` 为 **5 条**；布点类 `#5426` 已不再出现在 E5。
  - 项目 `819` 的 14 条按数量加权后，占比总和精确为 `100%`，14 条都成功写入占比并生成产值。
  - 项目 `824` 的 5 条统一填 `20%` 后，5 条都成功写入 `20%`，并生成产值。
  - 故意混选 `819 + 824` 各 1 条时，接口返回 `400`，正确拦截跨合同批量。
  - 另外单独用项目 `819` 的 `5436 / 5435 / 5434` 三条跑了一轮“平均分”，保存前后占比总和都精确为 `100%`。

**遗留/注意：**
- 这轮只处理 E5“缺占比”的批量入口，不包含 E7 项目级“无需合同”（T-022）、E8 判重（T-023）和 E9 修复动作（T-024）。
- 当前终端环境里本地页面启动脚本依旧不稳定，所以这轮验证仍以 `build` + 构建产物直调接口 + 临时库真实写入校验为主。
（该部分由 Codex 完成）

## 2026-04-22 — [T-020] 异常收件箱骨架落地，侧栏可直接看待处理总数
**提交：** `(待提交)`

**改动：**
1. 新增统一的异常判断库，并把 E1/E3/E4/E5/E6/E7/E9 的识别口径收在一处；同时落库 `workLog.acknowledgedExceptions` 和 `project.noContractExpected`，后面接口、页面和侧栏都复用同一套结果。
2. 新增收件箱接口和页面：现在有 `/master/inbox` 页面、`/api/inbox/exceptions` 列表接口、`/api/inbox/acknowledge` / `reset-acknowledgement` 忽略接口；侧栏新增“异常处理”入口，并显示当前未处理总数徽标。
3. 页面支持按分类切 tab、直接编辑工作记录、转手动产值、单条填占比、跳转合同页/项目页，以及按异常类型做“标已知忽略”；E9 只显示不修复，保持和任务边界一致。
4. 这轮没有改产值算法，也没有提前实现 T-021/T-022/T-023/T-024 的后续功能；只是把异常识别、展示和基础修复入口先搭完整。

**触达文件：**
- `src/prisma/schema.prisma`
- `src/src/lib/workLogExceptions.js`
- `src/src/app/api/inbox/exceptions/route.js`
- `src/src/app/api/inbox/acknowledge/route.js`
- `src/src/app/api/inbox/reset-acknowledgement/route.js`
- `src/src/app/master/inbox/page.js`
- `src/src/components/Sidebar.js`
- `src/src/app/globals.css`
- `docs/api_reference.md`
- `改动说明.md`
- `方案实施.md`

**数据库迁移：** 是 —— 已执行 `cd src && npx prisma db push && npx prisma generate`

**验证：**
- `cd src && cmd /c npm run lint`：通过。
- `cd src && cmd /c npm run build`：通过。
- 用构建产物直接调用新接口，确认真实库里原 T-004 的 20 条缺占比记录全部出现在 E5 tab，ID 与预期完全一致。
- 用构建产物直接调用新接口，确认当前真实库里 E7 tab 对应 13 个无合同项目，项目 ID 为 `816, 817, 820, 821, 823, 826, 827, 831, 839, 843, 844, 849, 850`。
- 用构建产物直接调用新接口，确认项目 `812` 在 E4 tab 下有 4 条缺单价记录，ID 为 `5279, 5281, 5293, 5358`。
- 复制主库到临时库后，真实跑了一轮 E1/E3/E4/E5/E6/E7/E9 场景：异常能出现，修完后会消失；`acknowledge` 连续调两次不报错，`reset-acknowledgement` 后会重新出现。

**遗留/注意：**
- 本轮没做批量填占比（T-021）、项目级“无需合同”操作入口（T-022）、项目重名 LLM 判重（T-023）和超限修复动作（T-024）。
- 本地无头启动脚本在当前终端环境里没能稳定拉起可访问服务，所以页面联动主要靠 `build` + 构建产物直调接口 + 临时库动作验证兜底；代码路径和数据结果都已实际跑通。
（该部分由 Codex 完成）

## 2026-04-22 — [T-019] worklog 保存改成同事务提交，失败会整条回滚
**提交：** `(待提交)`

**改动：**
1. `PUT /api/worklog/[id]` 里的主记录更新、人员重建、旧产值删除、新产值重算、检测记录同步，现已统一包进同一个事务里；中间任一步报错，整条保存都会回滚，不再留下“主记录改了、后续没跟上”的半完成状态。
2. `calculateProductionValue`、`applyProductionCap`、`syncDetectionRecordFromWorkLog` 都补了可选事务参数；不传时继续走原来的全局库，传入时参与当前事务，避免把别的调用方一起改坏。
3. 保存入口顺手把“按项目名找/建项目”和“按姓名找/建人员”也切到同一事务客户端里，避免保存失败时残留半拉子关联数据。

**触达文件：**
- `src/src/app/api/worklog/[id]/route.js`
- `src/src/lib/productionCalculator.js`
- `src/src/lib/productionCap.js`
- `src/src/lib/detectionRecordSync.js`
- `改动说明.md`
- `方案实施.md`

**数据库迁移：** 否

**验证：**
- `cd src && cmd /c npm run lint`：通过。
- `cd src && cmd /c npm run build`：通过。
- 临时复制 `src/prisma/dev.db` 到独立测试库后，启动 `next start` 指向临时库，真实跑了两轮 `PUT /api/worklog/[id]`：
  - 正常保存：成功把数量改成 `3`、备注改成 `after-success`、手工产值改成 `30`，并确认人员关系、产值记录、检测记录都同步更新。
  - 故意失败：把 `staffNames` 传成重复姓名，触发 `workLogStaff.createMany` 的唯一约束报错；接口返回 `500` 后，数据库里这条记录仍保持上一轮成功状态，数量/备注/手工产值/人员/产值/检测记录都没有被半途改坏。

**遗留/注意：**
- 这轮只收 `PUT /api/worklog/[id]`，没有去动合同保存链路，也没有改 `rebuildProjectProduction` 的事务边界。
- 验证全程使用临时库，结束后已删除临时库和日志，没有碰主库。 （该部分由 Codex 完成）

## 2026-04-22 — [T-018] 报表和导出统一显示完整项目名
**提交：** `(待提交)`

**改动：**
1. 报表汇总里的项目名改为统一走现成展示名函数：有子项的显示成“项目名（子项）”，不再只显示根项目名。
2. 导出明细里的工作记录项目名也改成统一展示名；遇到单体建筑模式时，会带出对应单体名，避免明细里多条看起来同名。
3. 项目-人员矩阵导出同样补上展示名，但没有改列头、模板结构和任何产值计算逻辑。

**触达文件：**
- `src/src/lib/reportAggregation.js`
- `src/src/app/api/export/route.js`
- `改动说明.md`
- `方案实施.md`

**数据库迁移：** 否

**验证：**
- `cd src && cmd /c npm run lint`：通过。
- `cd src && cmd /c npm run build`：通过。
- 启动本地 `next start` 后直接打真实接口和导出：
  - `/api/reports?groupBy=project` 已能看到 `云南省洱海流域山水林田湖草沙一体化保护和修复工程（洱源县入洱海河流（弥茨河等4条）水生态环境保护和修复项目）`
  - 明细导出里已能看到 `洱源产业园区邓川片区污水收集及处理项目（污水处理用房）`
  - 项目-人员矩阵导出也已出现带子项的完整项目名
  - 同时核对了汇总总额和矩阵总额，确认这轮只改显示名，没有把产值数字改坏

**遗留/注意：**
- 本轮没有去改 `buildProjectDisplayName` / `buildWorkLogProjectDisplayName` 的实现，只是把报表和导出都接到现有统一口径上。
- 验证只用真实接口和导出文件，没有额外改数据库内容。 （该部分由 Codex 完成）

## 2026-04-21 — [T-017] 项目详情页改成只读打开，并把修复入口单独拆出来
**提交：** `(待提交)`

**改动：**
1. `GET /api/projects/[id]` 不再顺手补写缺失的检测明细，只返回 `repairNeeded` 和 `missingDetectionRecordCount`，把“需要修复”这件事明确暴露给前端。
2. 新增 `POST /api/projects/[id]/repair-inspections`，专门负责补齐当前项目下缺失的检测明细；已有明细的工作记录会跳过，可重复调用。
3. 项目详情页顶部新增黄色提示条和“点击修复”按钮，修复中会锁按钮，修完后给出成功/失败提示并刷新页面数据。
4. `docs/api_reference.md` 已补上新返回字段和新接口说明，便于 Claude 后续复验时直接对照。

**触达文件：**
- `src/src/app/api/projects/[id]/route.js`
- `src/src/app/api/projects/[id]/repair-inspections/route.js`
- `src/src/app/master/projects/[id]/page.js`
- `docs/api_reference.md`
- `改动说明.md`
- `方案实施.md`

**数据库迁移：** 否

**验证：**
- `cd src && cmd /c npm run lint`：通过。
- `cd src && cmd /c npm run build`：通过。
- 临时复制 `src/prisma/dev.db` 到临时库后，启动 `next start` 指向临时库，真实造 1 条“有 worklog、但没有 detectionRecord”的项目数据：
  - 第一次 `GET /api/projects/[id]` 返回 `repairNeeded=true`、`missingDetectionRecordCount=1`，且库里仍然没有 detectionRecord，证明 GET 已经纯读。
  - 第一次 `POST /api/projects/[id]/repair-inspections` 返回 `repairedCount=1, skippedCount=0`。
  - 第二次 `POST /api/projects/[id]/repair-inspections` 返回 `repairedCount=0, skippedCount=1`，幂等通过。
- 额外尝试用 Playwright 打开 `/master/projects/[id]` 自动点“点击修复”，但这台环境启动 Chromium 仍报 `spawn EPERM`；因此本轮页面自动化只做到“尝试过但被环境拦住”，接口级真验证已完成。

**遗留/注意：**
- 验证使用的临时 SQLite 库、临时项目和临时 worklog 都只存在于临时库里，验证结束后已删除临时库，没有碰主库。
- Claude 若要复验页面按钮本身，仍需要在有浏览器权限的环境里跑一次；本地这台机器的无头浏览器限制没有变化。
（该部分由 Codex 完成）

## 2026-04-21 — [T-016] 修 .gitignore 并清掉工作区临时脏文件
**提交：** `(待提交)`

**改动：**
1. 修掉了 `src/.gitignore` 末尾损坏的 `.dev-server.pid` 行，并补齐临时日志、测试库、备份库的忽略规则。
2. 仓库根目录的 `.gitignore` 也同步补上了对应规则，避免根目录的临时日志继续出现在状态列表里。
3. 删除了本轮点名的临时日志、测试库和备份副本，但没有碰 `src/prisma/dev.db` 主库，也没有删 `src/scripts/verify-*.mjs`。

**触达文件：**
- `.gitignore`
- `src/.gitignore`
- `改动说明.md`
- `方案实施.md`

**数据库迁移：** 否

**验证：**
- `git status --short`：确认这批脏文件已经不再出现在状态列表里。
- `git check-ignore -v tmp-t008-server.err.log tmp-t011-server.err src/dev.db src/test_download.db src/prisma/dev.db.bak-20260414-082247 src/scripts/verify-project-name-matcher.mjs`：确认新规则已命中临时文件，验证脚本未被误伤。
- `Get-Item src/prisma/dev.db`：确认主库仍在。

**遗留/注意：**
- 当前工作区里剩下的修改属于其它功能任务，不是这轮清理遗留。
（该部分由 Codex 完成）

## 2026-04-20 — [T-014] 合同页项目名打开合同详情并支持直接改价目表
**提交：** `(待提交)`

**改动：**
1. 新增 `ContractDetailModal` 共用弹窗，把合同基本信息和价目表收口到一个组件里；`/master/projects` 里原来的合同详情弹窗已经改成复用它，保持只读查看行为。
2. `/contracts` 列表中的“工程名称”和“关联项目”都改成了可点击入口，点任一处都会打开同一个合同详情弹窗；在合同页弹窗里可以直接新增、删除、修改 `priceItems`，保存时复用现有 `PUT /api/contracts` 做整表覆盖。
3. 合同页弹窗补了“编辑合同”入口，点击后会关闭弹窗并把当前合同带回顶部编辑表单继续修改；本轮同时确认现有 `PUT /api/contracts` 已支持 `priceItems` 覆盖，所以没有改后端和 `docs/api_reference.md`。

**触达文件：**
- `src/src/components/ContractDetailModal.jsx`
- `src/src/app/contracts/page.js`
- `src/src/app/master/projects/page.js`
- `方案实施.md`
- `改动说明.md`

**数据库迁移：** 否
**验证：**
- `cd src && cmd /c npm run lint`：通过。
- `cd src && cmd /c npm run build`：通过。
- 启动本地 dev server 后，用临时合同 / 临时项目做真实页面验证：
  - `/contracts` 里“工程名称”和“关联项目”两个入口都能打开同一个详情弹窗。
  - 在弹窗里把第一行单价从 `100` 改到 `250`，保存后弹出“合同价目表已更新，并重算了 1 个关联项目”，再查接口确认价目表已实际更新。
  - 再从“关联项目”入口打开弹窗，点击“编辑合同”后，顶部编辑表单成功带出当前合同。
  - `/master/projects` 里同一合同 badge 仍能打开详情弹窗，且保持只读。
- 回归核对：项目 `818` 的 `2026-01` 产值合计仍为 `0`；项目 `819` 仍为 `buildingMode=true, phase=null, workLogs=15`；项目 `829` 仍为 `buildingMode=true, phase="古生种质资源二期", workLogs=18`。

**遗留/注意：**
- 本轮没有修改 `PUT /api/contracts`，因为现有实现已经支持 `priceItems` 整表覆盖；因此 `docs/api_reference.md` 未改。
- 页面验证使用的临时合同 `T014-E2E-*` 和临时项目都已自动删除，没有留下测试脏数据。
（该部分由 Codex 完成）
---

## 2026-04-20 — [T-015 复验 · 不通过] Claude 对 T-015 的独立验证结论
**提交：** 无代码改动（仅追加验证反馈到 `方案实施.md` 和本文件）

**结论：** T-015 **复验不通过**，回退到 `方案实施.md` 🟡 待修复。Codex 的功能骨架（UI 放开 / mixed 分支 / mixed-allocation-share capMode / split pendingAllocations / `verify-mixed-allocation.mjs`）已接近到位，Codex 自测脚本也能跑过，但独立复验发现 2 处阻塞级缺陷，Codex fixture 参数正好规避没触发。

**发现的两处阻塞缺陷：**

1. **`src/src/lib/productionCalculator.js:255` priceSource 中文双编码乱码**
   - 字面量 `娣峰悎璁¤垂鎵撳寘閮ㄥ垎鍗犳瘮` 是 UTF-8 字节被当 GBK 再存 UTF-8 的经典产物，应读作 `混合计费打包部分占比`
   - 实证：临时造 mixed+`allocationShare=0.3` 记录，`productionValue.priceSource` 实际落库值就是 `娣峰悎璁¤垂鎵撳寘閮ㄥ垎鍗犳瘮 30%`（hex `e5a8a3 e5b3b0 e6828e e79281 c2a4 …`）。前端显示和导出都会看到乱码
   - Codex 自测只断言 `calculationMode / workloadShare`，没断言 `priceSource`，所以漏掉
   - 目前线上 DB 无 mojibake 残留（现有 mixed 合同都没配 `lumpSumAmount`，本分支从未被触发），修完后无需回刷历史

2. **`src/src/lib/productionCap.js:getContractTotalAmount` mixed 分支未计入 `lumpSumAmount`**
   - 现状：`if (contract.pricingMode === 'mixed') { const total = unitTotal + areaAmount; ... }`（第 39-43 行）
   - 问题：T-015 第一次让 mixed 合同用上 `lumpSumAmount` 作打包部分总额，但合同总额口径还是旧的 `unitTotal + areaAmount`。结果 mixed 合同的「非 allocation-share 路径」（手工产值、单价回退、cap 计算）全都按 `unitTotal + areaAmount` 封顶，忽略打包部分
   - 实证：造 mixed 合同 `lumpSumAmount=100000, priceItems 合计=5000`。工作记录 `manualTotalValue=12345` → 期望存 12345；实测 `productionValue.sum=5000, exceeded=true`，被 `applyProductionCap` 静默裁到 5000
   - Codex 自测 fixture 的 `priceItems` 合计 `14550` 恰好大于 manualTotalValue=12345，所以 cap 从未咬到，自测脚本永远过
   - 副作用：等用户真按 T-015 意图配 mixed+`lumpSumAmount` 并存几条 `allocation-share` 记录后，同项目下任何「非 allocation-share」的工作记录都会在 `applyProductionCap` 里被「累计已超 5000 合同额」误判成超限归零，肉眼只能看到"数字被改了或被清零"，几乎无法自诊断

**推荐修复：**
1. 第 255 行字面量改回正确 UTF-8 的 `混合合同打包占比`（或与 line 193/232 同结构的等价写法），顺手在 `verify-mixed-allocation.mjs` 给 Case B 加一条 `priceSource` 断言防回归
2. `productionCap.js:getContractTotalAmount` mixed 分支改为 `total = unitTotal + areaAmount + (Number.parseFloat(contract.lumpSumAmount) || 0)`；并在 `verify-mixed-allocation.mjs` 加一个 case：`lumpSumAmount=100000, priceItems=5000, manualTotalValue=12345` → 期望产值=12345

**本轮验证用的临时数据：** 已全部清理（`workLog / project / contract / staff` 里 `CLAUDE-T015-*` 和 `T015-*` 前缀残留数均为 0）。

**复验命令痕迹：**
- `cd src && VERIFY_BASE_URL=http://127.0.0.1:3215 node scripts/verify-mixed-allocation.mjs` → 6/6 PASS（Codex 脚本）
- Claude 独立脚本（临时，已删）23 项断言 → 20 PASS / 3 FAIL（3 FAIL 全落在上述两处缺陷上）
- `productionValue` 表 mojibake 抽样：rows = 0；unit / mixed / lumpsum 各 3 条样本无回归
- mixed 合同 lumpSumAmount 分布：共 4 条 mixed 合同，`lumpSumAmount>0` 为 0 条，`mixed+allocationShare=null` 共 47 条，全部处于"尚未启用 T-015 新路径"状态

（该部分由 Claude 完成）

---

## 2026-04-20 — [T-015 修复] 补上 Claude 复验指出的两处阻塞问题
**提交：** `(待提交)`

**改动：**
1. 修正了 mixed 占比分摊产值写入时的说明文字，`priceSource` 现在会正常写成 `混合计费打包部分占比 xx%`，不会再把乱码存进库里。
2. 修正了 mixed 合同总额的口径。现在总额会把打包部分也一起算进去，所以 mixed 场景下的手工产值和单价回退不再被错误压到“只有单价部分总额”的范围内。
3. 补强了 `src/scripts/verify-mixed-allocation.mjs`：Case B 新增中文说明断言，Case F 新增“单价部分只有 5000、但手工产值 12345 仍保持 12345”的回归场景；脚本自启服务时也会先重建，避免拿旧 build 做假验证。

**触达文件：**
- `src/src/lib/productionCalculator.js`
- `src/src/lib/productionCap.js`
- `src/scripts/verify-mixed-allocation.mjs`
- `方案实施.md`
- `改动说明.md`

**数据库迁移：** 无

**验证：**
- `cd src && node scripts/verify-mixed-allocation.mjs`：通过。Case A-F 与 split 全部 PASS。
- `cd src && npm run lint`：通过。
- `cd src && npm run build`：通过。
- 关键数据回归实查：
  - 项目 `818` 产值合计仍为 `0`
  - 项目 `819` 仍为 `buildingMode=true`、`phase=null`、`15` 条工作记录
  - 项目 `829` 仍为 `buildingMode=true`、`phase="古生种质资源二期"`、`18` 条工作记录
  - 当前 mixed 合同里 `allocationShare=null` 仍是 `47` 条，但 `lumpSumAmount>0` 的仍为 `0` 条，所以 `T-004` 总量不变
  - 库里 `priceSource` 含 `娣峰悎` 的乱码记录数为 `0`

**遗留/注意：**
- 这轮没有改正式业务数据，只修掉了 Claude 复验指出的两处逻辑问题，并把专项验证补严了。
- 当前正式库里的 mixed 合同还没有配置 `lumpSumAmount`，所以这轮修复不会触发历史数据变化。

（该部分由 codex完成）

---

## 2026-04-20 — [T-015] mixed / area / lumpsum 工作记录支持手动占比与手工产值
**提交：** `(待提交)`

**改动：**
1. 工作记录页放开了 mixed 合同的占比输入框，`area / mixed / lumpsum` 三种合同现在都能在总账里直接填占比；占比弹窗也改成按合同类型切换说明文字和合同金额字段，不再把 mixed 和包干价都误写成面积合同。
2. 产值计算补上了 mixed 的新优先级：先看 `manualTotalValue`，再看 `allocationShare + lumpSumAmount`，最后才回退到原来的单价逻辑。对应的 mixed 打包部分上限也单独按 `lumpSumAmount` 卡，不再和别的计费口径混在一起。
3. 工作记录导入和拆分返回的 `pendingAllocations` 都补进了 mixed。现在 mixed 合同如果配置了 `lumpSumAmount`，导入或复制副本后也会把“待确认占比”的记录直接带回前端继续处理。
4. 新增 `src/scripts/verify-mixed-allocation.mjs` 自测脚本，会自动起本地服务、造临时 mixed 合同和工作记录，完整跑完 Case A-E 以及 split 场景，再清掉临时数据。

**触达文件：**
- `src/src/app/worklog/page.js`
- `src/src/lib/productionCalculator.js`
- `src/src/lib/productionCap.js`
- `src/src/app/api/worklog/[id]/split/route.js`
- `src/src/app/api/worklog/route.js`
- `src/scripts/verify-mixed-allocation.mjs`
- `docs/api_reference.md`
- `方案实施.md`

**数据库迁移：** 无

**验证：**
- `cd src && node scripts/verify-mixed-allocation.mjs`：通过。Case A-E 和 mixed split `pendingAllocations` 都实跑通过，脚本结束后临时合同/项目/工作记录会自动清理。
- `cd src && npm run lint`：通过。
- `cd src && npm run build`：通过。
- 真实页面实测：我额外造了一条临时 mixed 工作记录，实际打开 `/worklog` 页面，先在总账里填 `30%` 占比，随后直接查库确认 `allocationShare=0.3`、产值=`24000`、`calculationMode='allocation-share'`；再实际点“更多”切到手工产值填 `4321`，查库确认 `manualTotalValue=4321`、产值=`4321`、`calculationMode='manual'`。
- 回归核对：项目 `818` 产值合计仍为 `0`；项目 `819` 仍为 `buildingMode=true, phase=null, workLogs=15`；项目 `829` 仍为 `buildingMode=true, phase="古生种质资源二期", workLogs=18`。
- 队列核对：当前库里 mixed 合同下 `allocationShare=null` 的记录共有 `47` 条，但这些 mixed 合同的 `lumpSumAmount` 目前全部为空，真正新增到“可补打包占比”范围的记录是 `0` 条，所以 `T-004` 总量仍保持 `20` 条。

**遗留/注意：**
- 这轮只补了 mixed 的能力，不会自动给历史 mixed 工作记录回填占比，也不会替现有 mixed 合同自动补 `lumpSumAmount`。
- 当前库里的 mixed 合同都还没填 `lumpSumAmount`，所以这轮主要是把“以后配置了打包总额时，工作记录页和导入/拆分链路都能正确处理”的能力补齐。

（该部分由 codex完成）

---

## 2026-04-20 — [T-013] 拆分工作记录改成“保留原记录，复制副本”
**提交：** `(待提交)`

**改动：**
1. 把工作记录拆分从“原记录减掉一部分数量，再新建一条拆出记录”改成了“原记录完全不动，直接复制出一条新的副本”。现在点拆分后，原记录的数量、占比、手工产值和原有产值都保持原样。
2. 拆分接口现在主字段改为 `quantity`，不再要求新记录数量必须小于原数量；`0` 和远大于原数量的值都允许。旧字段 `splitQuantity` 还兼容，但只是别名，不再走旧限制。
3. 新副本会继续清空占比和手工产值，等用户后续确认；`rawText` 里的标记也从旧的 `[manual-split from #...]` 改成了更准确的 `[manual-copy from #...]`。
4. 工作记录页拆分弹窗也同步改了：默认数量直接显示原数量；“拆出数量”改成“新记录数量”；去掉“拆分后原记录剩余”那块，明确提示“原记录原样不动，只复制一份新的给你改”。
5. 顺手去掉了前端原本“数量 <= 0 不让点拆分”的限制，和后端新语义保持一致。

**触达文件：**
- `src/src/app/api/worklog/[id]/split/route.js`
- `src/src/app/worklog/page.js`
- `docs/api_reference.md`
- `方案实施.md`
- `改动说明.md`

**数据库迁移：** 否

**验证：**
- `cd src && npm run lint`
- `cd src && npm run build`
- 启动 `next start --hostname 127.0.0.1 --port 3100` 后，真实接口验证：
  - 默认 `{}` 复制：原记录不变，副本数量 = 原数量
  - `{ quantity: 0 }`：成功，副本数量为 `0`
  - `{ quantity: 999 }`：成功，副本数量为 `999`
  - `{ quantity: -1 }`：返回 `400`
  - 在邓川包干项目上临时造一条已填 `allocationShare=25` 的记录再复制，`pendingAllocations` 只返回副本，不返回原记录
- 启动同一服务后，用 Playwright 真实打开 `/worklog` 页面点拆分：
  - 弹窗默认数量显示 `10`
  - 第一次不改直接确认后，数量变成 `[10, 10]`
  - 第二次改成 `20` 后，数量变成 `[10, 10, 20]`
  - 第三次改成 `0` 后，数量变成 `[0, 10, 10, 20]`
- 回归核对：
  - 项目 `818` 的 `2026-01` 产值仍为 `0`
  - 项目 `819` 仍为 `buildingMode=true`、`phase=null`、`15` 条工作记录
  - 项目 `829` 仍为 `buildingMode=true`、`phase="古生种质资源二期"`、`18` 条工作记录
- 验证结束后，临时项目、临时工作记录、临时页面测试数据和临时服务日志都已清理，没有残留

**遗留/注意：**
- 这次没有新建单独的验证脚本文件，接口和页面实测都是用两段临时 `node --input-type=module -` 命令跑完并清理的
- 当前 `T013*` 临时项目和临时工作记录残留都是 `0`
- （该部分由 codex完成）

---

## 2026-04-16 — [T-011] 工作记录导入改成“先预览再确认”
**提交：** `(待提交)`

**改动：**
1. 新增 `projectNameMatcher.js`，把项目名近似匹配从原来的“没找到就直接新建”拆成独立的本地规则模块。现在会统一处理中划线、空格、全角括号，并给出近似分数；像 `#815 / #837` 这种只差一个中划线的项目名，会在导入预览里一起列出来让用户确认。
2. 扩展了 `POST /api/worklog`：新增 `mode=preview` 只解析不落库，逐行返回 `精确匹配 / 近似候选 / 全新项目`；新增 `mode=commit`，支持把某行明确导入到已有项目、已有单体建筑项目，或者作为新项目创建。`mode` 不传时保留原来的旧行为，不影响老调用方。
3. 工作记录页的 Excel 导入和 WPS 粘贴导入都改成了“预览导入”。用户现在会先看到逐行状态和决策下拉，再点“确认导入”；近似项目默认给候选，全新项目默认给新建输入框，不再一键悄悄造台账。
4. 额外补了一层保护：如果导入名和某个旧项目完全一样，但库里还存在其它非常接近的旧项目，预览也会故意显示成“近似候选”，逼用户手动确认，避免继续把数据悄悄挂错地方。

**触达文件：**
- `src/src/lib/projectNameMatcher.js`
- `src/scripts/verify-project-name-matcher.mjs`
- `src/src/app/api/worklog/route.js`
- `src/src/app/worklog/page.js`
- `docs/api_reference.md`
- `方案实施.md`
- `改动说明.md`

**数据库迁移：** 否

**验证：**
- `cd src && node scripts/verify-project-name-matcher.mjs`
- `cd src && npm run lint`
- `cd src && npm run build`
- 启动 `next start --hostname 127.0.0.1 --port 3100` 后，真实接口验证：
  - `mode=preview` 预览 `#837` 这条项目名时，返回 `fuzzy`，候选中包含 `#837` 和 `#815`
  - 手动指定导入到 `#815` 后，工作记录实际落到 `projectId=815`，没有新增同名项目
  - 全新项目预览阶段不会提前建项目，确认后才会创建
  - 不带 `mode` 的旧接口仍按原行为自动建新项目
- 启动同一服务后，用 Playwright 真实打开 `/worklog` 页面并点击验证：
  - 粘贴导入近似同名时，页面显示“近似候选”，切到 `#815` 后可正常确认导入
  - 粘贴导入全新项目时，页面显示“全新项目”，预览阶段不会写库，确认后才会创建
- 回归核对：
  - 项目 `818` 的 `2026-01` 产值仍为 `0`
  - 项目 `819` 仍为 `buildingMode=true`、`phase=null`、`15` 条工作记录
  - 项目 `829` 仍为 `buildingMode=true`、`phase="古生种质资源二期"`、`18` 条工作记录
- 验证结束后，临时工作记录、临时项目、临时页面测试数据和临时服务日志都已清理，没有残留

**遗留/注意：**
- Claude 已于 2026-04-17 复验通过，`T-011` 已标 ✅
- 这次不会自动帮用户合并库里已经存在的近似同名旧项目（例如 `#815 / #837`）；只是防止后续再继续导入出新的重复项目
- 2026-04-16 [T-010] 里提到的“2 级嵌套自动匹配还不完善”问题，现已由本任务补上导入预览与人工确认流程，后续不会再靠静默自动匹配硬吃这类场景
- （该部分由 codex完成）

---

## 2026-04-16 — [T-012] 合同项目组整理扩展
**提交：** `(待提交)`

**改动：**
1. 重写了 `PUT /api/projects/batch-rename` 接口，支持 `role` 为 `subproject`、`merge-into` 和 `building-under` 的拓扑操作。合并和单体挂载均在 `prisma.$transaction` 内级联处理工作记录和检测记录。
2. 重构了项目详情页 `[id]/page.js` 中合同组管理面板：
   - 使用 `groupDecisions` 替代简单的 `groupPhases` 状态。
   - 实现每个项目的角色选择（独立、并入、单体）。
   - 添加防重名、嵌套循环拓扑锁（badge 提示目标项）。
3. 编写 `src/scripts/verify-batch-rename-roles.mjs` 测试脚本验证所有三种角色的事务级联与并入。
4. 更新 API Reference 文档。

---

## 2026-04-16 — [T-010] 放开子项 + 单体建筑共存 & 合并古生种质二期保育室
**提交：** `(待提交)`

**改动：**
1. 删除了原本为了规避概念混乱而设置的 `phase`（子项名）与 `buildingMode`（单体建筑模式）无法共存的后端接口约束。
2. 解除了项目管理 `/master/projects/page.js` 中针对 `buildingMode=true` 时，对 `phase` 字段的 `disabled` 状态限制以及清除逻辑，使得子项下亦可开启多单体模式。
3. 新增 `src/scripts/merge-gusheng-buildings.mjs` 数据修复脚本，将原本被错拆为两个子项的“古生种质资源二期”（#829 与 #842 项目）成功整合。脚本设定了 `1#保育室` 和 `2#保育室` 的 `buildingName`、合并了 #842 记录至 #829、删除了 #842 并且为 #829 补上了统一 phase 以及 `buildingMode=true`。

**触达文件：**
- `src/src/app/api/projects/route.js`
- `src/src/app/api/projects/batch-rename/route.js`
- `src/src/app/master/projects/page.js`
- `src/scripts/merge-gusheng-buildings.mjs`
- `方案实施.md`
- `改动说明.md`

**数据库迁移：** 否（无 schema 修改，但利用脚本做过线上数据调整与备份，产生了 `dev.db.bak-20260416-gusheng`）

**验证：**
- `node scripts/merge-gusheng-buildings.mjs --apply` 执行成功：
  - #842 项目已被顺利删除。
  - #829 工作记录整合至 18 条。
  - `1#保育室` 与 `2#保育室` 根据来源被分别分配给这 18 条工作记录中的 9 条。
  - API 与前端运行无阻。

**遗留/注意：**
- Claude 已于 2026-04-16 复验通过，`T-010` 已标 ✅
- 本次任务明确列为"禁区"的 `projectResolver.js` 2 级嵌套自动匹配逻辑未改；相关导入误判风险已在后续 `T-011` 里通过“预览 + 人工确认”流程兜住，不再依赖静默自动匹配
- （该部分由 Antigravity 完成）

---

## 2026-04-16 — [T-009] 合同项目组整理与分组汇总
**提交：** `(待提交)`

**改动：**
1. 项目详情页现在会把同一合同下的全部项目一起带出来。像合同 #14 这组项目，在 `/master/projects/829` 里已经能直接看到 6 个兄弟项目，不用再来回切页面核对。
2. 项目详情页顶部新增“合同项目组”区块，可以直接填写统一后的大项目名称，并逐条填写各子项名称；点“保存全部修改”会一次性保存整组，不再需要一个个项目进去改。
3. 同一页面下新增“全部工作记录（合同项目组汇总）”，会把同合同项目的工作记录按子项分组展开，方便按子项一眼看全，不会只剩当前项目自己的记录。
4. 新增 `/api/projects/batch-rename`，专门处理同合同项目组的一次性整理；接口会拦住“漏提某个兄弟项目”“统一后重名”“单体建筑模式下误填子项”这几类容易出错的情况。

**触达文件：**
- `src/src/app/api/projects/[id]/route.js`
- `src/src/app/api/projects/batch-rename/route.js`
- `src/src/app/master/projects/[id]/page.js`
- `src/src/app/globals.css`
- `docs/api_reference.md`
- `方案实施.md`
- `改动说明.md`

**数据库迁移：** 否

**验证：**
- `cd src && npm run lint`
- `cd src && npm run build`
- 启动 `next start --hostname 127.0.0.1 --port 3100` 后，真实接口核对：
  - `/api/projects/829` 返回 `6` 个同合同项目，合计 `24` 条工作记录
  - `/api/projects/819` 仍为 `buildingMode=true`，同合同项目数为 `1`
- 用临时合同 `T009-VALIDATION` 造了 `3` 个项目、`5` 条工作记录，打开真实页面 `/master/projects/863` 走完整保存流程，保存后 3 个项目统一成 `T009统一大项目`，子项分别写成 `子项-A / 子项-B / 子项-C`
- 回归数据核对：
  - 项目 `818` 当前累计产值仍为 `0`
  - 合同 `#9` 下 `#811 / #836` 仍保持 `phase=null`
- 验证结束后，临时合同、临时项目、临时工作记录、Playwright 临时脚本和临时日志都已删除，没有残留测试数据

**遗留/注意：**
- 合同 #14 的 6 个项目现在只是“可整理”，我没有替用户自动改名；最终大项目名和各子项名仍由用户自己确认后保存
- 等 Claude 复验通过后，再把 `T-009` 状态改成 ✅
- （该部分由 codex完成）

---

## 2026-04-16 — [T-008] 同合同多项目“子项”引导补齐
**提交：** `(待提交)`

**改动：**
1. 项目管理页的“同合同”弹窗补了“工程阶段 / 子项”输入框。现在选“同一项目下的不同子项”时，可以当场填子项；如果留空，会先弹二次确认，不会再无声地留成空白。
2. 项目管理页顶部新增黄色提醒，专门点出“同一合同下已有多个项目，但还有项目没填子项”的合同组，当前库里会直接提示合同 #14 和 #9。
3. 合同页保存前补了同样的“合并 / 子项”选择。编辑合同时如果这次保存会把合同继续挂到新的项目上，而这份合同下面已经有别的项目，会先弹选择框；选“子项”时会把 phase 一起保存。
4. `/api/contracts` 现在会返回关联网项目的 `phase`，并接收 `projectPhase`、`sharedContractMode`、`allowBlankSharedPhase`。如果前端没先做“合并 / 子项”选择，接口会返回 `409`，避免再悄悄生成 phase=null 的同合同项目。

**触达文件：**
- `src/src/app/master/projects/page.js`
- `src/src/app/contracts/page.js`
- `src/src/app/api/contracts/route.js`
- `docs/api_reference.md`
- `方案实施.md`

**数据库迁移：** 否

**验证：**
- `cd src && npm run lint`
- `cd src && npm run build`
- 启动 `next start --hostname 127.0.0.1 --port 3100` 后，真实调接口验证：
  - 项目页模拟保存可把子项写成 `SUB-PROJ`
  - 合同页未带选择直接保存会返回 `409`
  - 合同页带 `sharedContractMode='subitem'` 后可正常保存，并把子项写成 `SUB-CONTRACT`
  - 当前库里黄色提醒命中的合同组包含 #14 和 #9
- 验证结束后，临时合同和临时项目已全部删除，库里没有残留 `T008*` 测试数据

**遗留/注意：**
- 历史数据没有自动补 phase，仍需用户手动处理：
  - 合同 #14 下的 6 个项目：#814、#825、#828、#829、#834、#842
  - 合同 #9 下的 2 个项目：#811、#836（这组更像应合并，不一定是子项）
- 合同 #12 的 819 已由 T-007 处理完成，本轮未改动

---

## 2026-04-15 — [T-007] 邓川项目合并为单体建筑模式

**提交：** `(待提交)`

**改动：**
1. 给项目和工作记录补了两层能力：项目可以开启“单体建筑模式”，工作记录可以单独记“单体建筑名”。这样 `项目名（xxx）` 不再只能被当成子项项目，也可以落到同一个主项目下面。
2. 项目名解析规则改了：如果某个主项目已经开启单体建筑模式，再导入、编辑、拆分 `项目名（单体名）` 时，会直接挂回主项目，并把括号里的内容写成这条工作记录的单体建筑；普通项目仍保持原来的“子项项目”逻辑。
3. 新增一次性脚本 `src/scripts/merge-dengchuan-buildings.mjs`，默认只预演；加 `--apply` 后会先备份数据库，再把原 851 的 4 条记录写成“污水处理用房”，然后合并进 819，并开启 819 的单体建筑模式。
4. 已按真实数据执行脚本：原项目 `851` 已删除，项目 `819` 现在是单体建筑模式，共 `15` 条工作记录，其中 `4` 条带单体建筑、`11` 条没有单体名，仍统一挂在合同 `12` 下。
5. 页面也一起补齐了：项目管理里能开关单体建筑模式并看到标记；项目详情页会展示“单体建筑列表”和工作记录里的“单体建筑”列；工作记录页项目名会显示成 `项目名（单体）`，但按项目筛选时仍按主项目维度工作。

**触达文件：**
- `src/prisma/schema.prisma`
- `src/src/lib/projectDisplayName.js`
- `src/src/lib/projectResolver.js`
- `src/src/app/api/worklog/route.js`
- `src/src/app/api/worklog/[id]/route.js`
- `src/src/app/api/worklog/[id]/split/route.js`
- `src/src/app/api/projects/route.js`
- `src/src/app/api/projects/[id]/route.js`
- `src/src/app/master/projects/page.js`
- `src/src/app/master/projects/[id]/page.js`
- `src/src/app/worklog/page.js`
- `src/scripts/merge-dengchuan-buildings.mjs`
- `src/prisma/dev.db`
- `src/prisma/dev.db.bak-20260415-dengchuan`
- `docs/api_reference.md`
- `改动说明.md`
- `方案实施.md`

**数据库迁移：** 是 —— 已执行 `cd src && npx prisma db push`、`npx prisma generate`；`src/prisma/dev.db` 已同步新字段，并已通过脚本把邓川 `851 -> 819` 合并落库；同时新增备份 `src/prisma/dev.db.bak-20260415-dengchuan`

**验证：**
- `cd src && npm run build`
- `cd src && node scripts/merge-dengchuan-buildings.mjs`
- `cd src && node scripts/merge-dengchuan-buildings.mjs --apply`
- 数据库实测：`project 851` 已不存在；`project 819.buildingMode=true`；`project 819` 当前 `15` 条 worklog，其中 `4` 条 `buildingName` 非空、`11` 条为空
- `GET http://127.0.0.1:3000/api/projects/819`
  - 返回 `buildingMode=true`
  - `buildingSummaries` 当前为 `1` 条，`污水处理用房 = 4 条 / 0 元`
  - `workLogs` 当前为 `15` 条
- 实际调用 `POST /api/worklog` 做临时导入验证后已清理：
  - `洱源产业园区邓川片区污水收集及处理项目（新测试单体）` 会落到 `819`，写成 `buildingName=新测试单体`，且**不会新建项目**
  - `洱源产业园区邓川片区污水收集及处理项目（污水处理用房）` 会落到 `819`，写成 `buildingName=污水处理用房`，且**不会新建项目**
  - `大理建投·清华园项目（T007临时子项）` 仍按旧逻辑新建 `phase` 项目，`buildingName` 为空
- 页面入口连通性检查：
  - `/master/projects` → `200`
  - `/master/projects/819` → `200`
  - `/worklog` → `200`

**遗留/注意：**
- 项目 `819` 合并后，原来待补占比的队列已从 `11` 条变成 `15` 条，`T-004` 已同步改成 `20` 条总量
- 这次只解决“同一项目下多栋单体共用检测项目”的模型；“同合同多项目但 phase 仍为空”的引导问题仍留给 `T-008`

---

## 2026-04-15 — [T-001] 清华园单位错配止血

**提交：** `(待提交)`

**改动：**
1. 在 `worklogMatching` 增加单位归一化和兼容组，工作记录与合同价目只要单位不兼容，就直接从匹配池里剔除；全部不兼容时返回 `no-price-match`，不再硬算产值。
2. 保留“同组可匹配”的最小兼容规则：面积、质量、项次、点、根、构件、长度、组/栋各自成组；两边都空仍兼容，一边空一边非空则改为不兼容。
3. 为了让真实补算能把**旧错值一起清掉**，最小补强了 `retroactiveCalculation`：仅对 `unit/mixed` 合同改为整项目重跑；`area/lumpsum` 仍保留原来的人工占比补算逻辑。
4. 新增 `src/scripts/verify-unit-match.mjs` 自测脚本，能直接证明“点 对 m² 返回 null”，同时验证“个构件 对 个”仍可保留候选。
5. 已对项目 818 执行真实补算，清掉了 2026-01 那批由单位错配导致的异常产值；项目 813、815、833 的已知正常记录未受影响。

**触达文件：**
- `src/src/lib/worklogMatching.js`
- `src/src/lib/productionCalculator.js`
- `src/scripts/verify-unit-match.mjs`
- `src/prisma/dev.db`
- `改动说明.md`
- `方案实施.md`

**数据库迁移：** 否（不改 schema；但已通过 `POST /api/projects/retroactive` 重算项目 818，`src/prisma/dev.db` 中该项目产值数据已更新）

**验证：**
- `cd src && node scripts/verify-unit-match.mjs`
  - `PASS 点 对 m² 必须拦截: 返回 null`
  - `PASS 个构件 对 个 应保留: 保留 1 个候选`
  - `PASS 单位归一化 平方米 -> m²: 归一化结果: m²`
- `cd src && npm run build`
- 实测 `POST /api/projects/retroactive`，项目 `818` 返回 `completed`，`18` 条工作记录全部为 `no-price-match`
- 数据库实测：#5275、#5280、#5283、#5284、#5285、#5286、#5287、#5288、#5413、#5414 当前产值都为 `0`
- 数据库实测：项目 `818` 在 `2026-01` 的 `10` 条工作记录产值合计已为 `0`
- 对照值复核未变：#5268=`5800`、#5271=`1974.9`、#5321=`1800`、#5420=`2000`、#5421=`300`、#5422=`300`、#5423=`2000`

**遗留/注意：**
- 这次只做单位兼容止血；Mixed 合同里“单价条 vs 打包条”的长期建模仍留在 `T-002`
- 由于本次已对项目 818 执行真实补算，`src/prisma/dev.db` 需要跟代码一起同步

---

## 2026-04-14 — 工作记录总账改为完整显示

**提交：** `(待提交)`

**改动：**
1. 把工作记录总账里容易被截断的列改成了可自动长高的多行输入框，项目名、检测内容、备注、人员都能直接完整显示。
2. 备注和其他带换行的内容现在会按原样展开，不再挤成一行，方便逐条核对。
3. 表格单元格改成顶部对齐，并允许内容自动换行，长文本不会再被压缩得看不全。

**触达文件：**
- `src/src/app/worklog/page.js`
- `src/src/app/globals.css`
- `改动说明.md`

**数据库迁移：** 否

**验证：**
- `cd src && npm run build`

**遗留/注意：**
- 这次只改显示方式和输入框形态，没有改工作记录的保存逻辑。

---

## 2026-04-14 — 工作记录页快捷编辑 + 拆分记录 + 合同匹配补强

**提交：** `(待提交)`

**改动：**
1. **工作记录页改成表格内直接修改**：日期、项目、检测内容、备注、占比、数量、单位、人员都能直接点进去改，失焦自动保存；保留“更多”入口继续改手工产值等少用项。
2. **新增拆分工作记录能力**：每条记录都可以打开拆分弹窗，填写拆出数量和新记录信息后，系统会保留原记录剩余数量并新增一条拆出记录，同时同步检测记录。
3. **补一条拆分接口** `POST /api/worklog/[id]/split`：拆分时会清空原记录/新记录的已确认占比与手工产值，避免沿用旧值；有需要的面积/包干记录会重新进入待确认队列。
4. **补强合同价目匹配规则**：给“桩身完整性检测”“实体检测大包项”“后置埋件锚固承载力”“竖向变形观测”“基坑监测”这些合同写法补了别名桥接，解决工作记录叫法和合同叫法不一致的问题。
5. **顺手清掉一部分待办**：已对项目 818、830、812 重新补算。结果是项目 818 原来 16 条未匹配记录全部接回；项目 830 原来 5 条里 4 条接回，剩 1 条旧数据里的“布”改按“不计产值”处理；项目 812 还剩 4 条确实需要继续人工核对的记录。

**触达文件：**
- `src/src/app/worklog/page.js`
- `src/src/app/api/worklog/[id]/split/route.js`（新建）
- `src/src/lib/worklogMatching.js`
- `src/src/lib/worklogClassification.js`
- `src/src/app/globals.css`
- `docs/api_reference.md`
- `src/prisma/dev.db`
- `改动说明.md`

**数据库迁移：** 否（不改 schema；但已对项目 812/818/830 的历史记录重新补算，`src/prisma/dev.db` 有数据变更）

**验证：**
- `cd src && npm run build`
- 用真实接口验证 `PUT /api/worklog/5364`，确认单字段快速修改能写回，再把测试改动恢复
- 用真实接口验证 `POST /api/worklog/5364/split`，确认能生成拆分记录和检测记录，再把测试记录、测试检测记录全部删除并恢复原数据
- 用 `POST /api/projects/retroactive` 重新补算项目 818、830、812，确认 818 的 16 条未匹配记录全部接回，830 不再有 `no-price-match`

**遗留/注意：**
- 工作记录页里目前为了稳妥保留了一份隐藏旧表作兜底，用户只会看到新的可直接编辑表；后续如有空可以把旧表彻底删掉，减一点页面负担。
- 项目 812 还剩 4 条没接回的记录，主要是焊缝探伤/“测”这类合同里本来就没直接对应项或上下文仍不够明确的情况，暂时不建议继续硬匹配。

---

## 2026-04-14 — 批量回补 quantity=0 的历史工作记录

**提交：** `(待提交)`

**改动：**
1. **新增一次性修复脚本** `src/scripts/repair-zero-quantity-worklogs.mjs`，默认 dry-run，加 `--apply` 才真写库
   - 从 `WorkLog.rawText` 还原原始单元格，套当前的 observation/method-bundle/displacement 三套拆分规则（非 LLM 路径）
   - 第一条原子记录就地 UPDATE（保留 id/project/staff 关系）
   - 多出来的原子记录按原 WorkLog 复制一份，`rawText` 打上 `[auto-split from #N]` 标记
2. **批量修复结果**：44 条候选 → 41 条救回 + 24 条拆分新增 + 3 条规则救不了（编制方案、取消检测、出报告）
   - 整体 WorkLog 数 101 → 125，quantity=0 的 44 → 3
3. **调 22 个受影响项目的 retroactive 补算接口**重算产值。典型匹配结果：
   - 已有合同 + 能匹配单价 → 10 条记录拿到产值
   - lumpsum/area 合同 → 16 条停在 pending-area-share，等人工补占比
   - 无合同项目 → 14 个项目仍然 workload-only
   - 合同存在但检测项目名匹配不上 → no-price-match 若干条
4. **备份**：写库前已拷贝 `src/prisma/dev.db` → `src/prisma/dev.db.bak-20260414-082247`

**触达文件：**
- `src/scripts/repair-zero-quantity-worklogs.mjs`（新建）
- `src/prisma/dev.db`
- `改动说明.md`

**数据库迁移：** 否（只改数据，不改 schema）

**验证：**
- dry-run 预览已看过一轮，41 条就地更新 + 24 条新增 + 3 条跳过
- apply 后数据库实测：总 125 条、quantity=0 只剩 3 条、auto-split 新增 24 条
- 通过 `POST /api/projects/retroactive` 对 22 个项目逐个补算产值

**遗留/注意：**
- 少数拆分结果不完美（已在当次对话里标出，比如 #5315 #5323 #5336 的子项 testContent 没识别到”沉降观测/防雷检测”，只是数量能救回来），后续如需可再手工调整或让规则更精细
- 16 条 pending-area-share / 14 个无合同项目 / 若干 no-price-match 见”当前待办”
- 如果这次修复出了意外，可用备份恢复：停 dev server 后 `cp src/prisma/dev.db.bak-20260414-082247 src/prisma/dev.db`
- 脚本保留在仓库里（scripts/），是一次性工具；后续如发现新一批 quantity=0 还可以再跑

---

## 2026-04-13 — 工作记录导入修复 + 布点不计产值 + 项目页合同弹窗优化 + 合同页刷新修复 + 接力规范补充

**提交：** `(待提交)`

**改动：**
1. **修复工作记录导入的常见拆分问题**：现在能更稳地识别 `4组（32点）`、`布37点，测150点`、`板厚层高各3个构件`、`植筋：6圆20根` 这类写法，减少数量被导成 0、检测项目被拆碎的问题
2. **明确“布点不计产值”规则**：像“布37点”这类沉降观测布点，只保留工作记录，不算入产值；同一条里的“测150点”仍正常计算产值
3. **优化单个项目页里的合同详情弹窗**：放宽弹窗和“检测项目”列，长内容不再被挤成很窄的竖排，查看合同明细更清楚
4. **修复合同上传页的刷新报错**：合同页面里的“刷新”操作不再因为旧方法名报错
5. **补强跨电脑/跨模型接力规范**：把“每次改动后立即更新本文件”“接手前先看本文件”“记录要写到不同模型都能直接看懂”这些要求明确写入本文件

**触达文件：**
- `src/src/lib/wpsParser.js`
- `src/src/lib/productionCalculator.js`
- `src/src/lib/worklogBilling.js`
- `src/src/lib/worklogClassification.js`
- `src/src/app/master/projects/[id]/page.js`
- `src/src/app/contracts/page.js`
- `改动说明.md`

**数据库迁移：** 否

**验证：**
- `cd src && npm run lint`
- `cd src && npm run build`
- 用真实导入链路验证：`布37点，测150点` 会拆成两条，其中“布点”只记工作量不计产值，“观测”正常计产值

**遗留/注意：**
- 这次修的是导入规则和计价规则，历史上已经导坏的旧工作记录还没有批量回补
- 如果项目本身仍然没关联合同、名字重复未合并，或者面积/包干记录还没补占比，报表结果还是会继续受影响

---

## 2026-04-13 — 合同编辑支持改项目名/改绑项目，并同步重算受影响数据

**提交：** `(待提交)`

**改动：**
1. **合同编辑可顺手改项目名**：编辑合同时恢复工程名称输入，选中当前绑定项目后可直接改名
2. **合同编辑可改绑定项目**：新增「关联项目」选择，可保留当前项目、改绑到已有项目，或按工程名称新建并改绑
3. **合同改动后自动同步数据**：合同内容、项目名或绑定关系变更后，会把受影响项目的历史工作记录/报告产值重新计算，避免旧数据残留
4. **旧项目会自动解绑**：合同改绑到新项目后，原项目的合同关系会解除，不再继续挂着旧合同

**触达文件：**
- `src/src/app/contracts/page.js`
- `src/src/app/api/contracts/route.js`
- `src/src/lib/productionCalculator.js`
- `docs/api_reference.md`
- `改动说明.md`

**数据库迁移：** 否

**验证：**
- 合同编辑后，项目名和绑定关系会一起同步更新
- 受影响项目会自动重算历史数据，旧项目会自动解绑

**遗留/注意：**
- 当前合同编辑默认按“一份合同对应一个主项目”来调整绑定关系；如果后续要长期支持一份合同绑定多个项目，需要再补单独的多选交互
- 合同编辑触发的是历史数据重算，数量多时会比单纯改文字稍慢一些

---

## 2026-04-13 — 人员管理简化：去掉手机号和角色，支持点击姓名直接修改

**提交：** `c129307`

**改动：**
1. **人员页改成轻量维护**：表单只保留姓名，不再在页面上录入手机号和角色
2. **支持点击姓名直接修改**：列表中点击姓名即可原地编辑，回车保存、失焦保存、Esc 取消
3. **新增人员更新接口**：`PUT /api/staff` 支持按 ID 更新姓名

**触达文件：**
- `src/src/app/master/staff/page.js`
- `src/src/app/api/staff/route.js`
- `src/prisma/dev.db`

**数据库迁移：** 否

**验证：**
- 人员页新增、改名、原地编辑流程已打通

**遗留/注意：**
- 当前只是前端简化，`Staff` 表里的 `phone`、`role` 字段仍保留，历史数据不会丢
- `POST /api/staff` 仍兼容接收 `phone`、`role`，但当前页面已不再使用

---

## 2026-04-13 — 合同编辑 + 混合计费/包干价 + 内部价隐藏 + 合同弹窗 + 数据库和脚本纳入版控

**提交：** `0e4fb1d`

**改动：**
1. **合同支持编辑**：合同列表新增「编辑」按钮，点击后加载合同数据到表单，PUT 请求更新合同信息和价目表（整体替换）
   - API 新增 `PUT /api/contracts`：更新合同基本信息 + 删旧价目表 + 创建新价目表
   - 前端区分新建/编辑模式，编辑模式隐藏工程名称和文件上传
2. **新增混合计费合同**（`pricingMode: 'mixed'`）：面积部分 + 单价部分组合计费，产值按单价匹配计算，上限为两部分之和
3. **新增包干价合同**（`pricingMode: 'lumpsum'`）：固定总价，按占比分配产值，类似面积合同逻辑
   - Schema 新增 `lumpSumAmount` 字段
   - `productionCalculator.js` 新增方案 D（包干价）
   - `productionCap.js`、`worklogBilling.js`、`reportAggregation.js` 均适配
4. **合同详情弹窗**：项目管理页和项目详情页，点击合同标签弹出检测项目清单（序号/项目/数量/单位/单价/小计/合计）
   - `api/projects/route.js` GET 改为 include `contract.priceItems`
5. **内部指导价模块隐藏**：`Sidebar.js` 注释掉「单价管理」入口，代码和数据保留
6. **合同表单输入优化**：
   - 检测类别列宽加至 260px / minWidth 230px（容纳15个汉字）
   - 数量、单价改为 `type="text" inputMode="decimal"`，去掉上下箭头按钮
7. **OCR 标准化恢复**：`upload/route.js` 恢复 `normalizeOcrPriceItems` 调用
8. **GLM-5 思考模型解析修复**：`contractParser.js` 增大 `TEXT_MAX_TOKENS` 到 16384，`parseGLMResponse` 新增从 `reasoning_content` 逐行提取检测项目的 fallback 策略
9. **数据库和启动脚本纳入版控**：`.gitignore` 调整后，`src/prisma/dev.db` 与 `start-dev*.vbs/.cmd`、`stop-dev.cmd` 进入版本控制，便于多机接力开发

**触达文件：**
- `src/prisma/schema.prisma`（新增 `lumpSumAmount`）
- `src/prisma/dev.db`
- `src/src/app/api/contracts/route.js`（新增 PUT）
- `src/src/app/contracts/page.js`（编辑功能 + 新计价选项 + 输入框优化）
- `src/src/app/api/projects/route.js`（include priceItems）
- `src/src/app/master/projects/page.js`（合同弹窗 + 计价方式显示）
- `src/src/app/master/projects/[id]/page.js`（合同弹窗）
- `src/src/app/api/upload/route.js`（恢复 normalizeOcrPriceItems + 调试日志）
- `src/src/lib/contractParser.js`（TEXT_MAX_TOKENS + reasoning_content 提取）
- `src/src/lib/productionCalculator.js`（包干价方案 D + mixed 走单价）
- `src/src/lib/productionCap.js`（新计价方式上限计算）
- `src/src/lib/worklogBilling.js`（normalizePricingMode 扩展 + 包干价状态）
- `src/src/lib/reportAggregation.js`（计价方式显示）
- `src/src/components/Sidebar.js`（隐藏内部指导价）
- `start-dev-hidden.vbs`
- `start-dev.cmd`
- `start-dev.vbs`
- `stop-dev.cmd`
- `.gitignore`

**数据库迁移：** **是** — 另一台机器 `git pull` 后需执行 `cd src && npx prisma db push`（新增 `lumpSumAmount` 字段）

**验证：**
- 合同编辑、混合计费、包干价、合同弹窗、隐藏内部指导价都已跑通过基础检查

**遗留/注意：**
- `prisma generate` 需停 dev server 后执行（文件锁冲突），然后重启 dev server
- OCR 解析 reasoning_content 的逐行提取仍有项目名称包含冗余文本和重复项的问题，后续可优化去重
- 当时这版里的“合同编辑不更新工程名和关联项目关系”已在同日后续记录中解决，接手时以更上面的新记录为准

---

## 2026-04-10 — 合同 OCR 解析修复 + 项目 API 修正

**提交：** `d85a364`

**改动：**
1. 合同 OCR 解析：增大 TEXT_MAX_TOKENS、修复 reasoning_content 解析
2. 项目 API：include contract.priceItems
3. 合同弹窗初版
4. 输入框宽度优化 + Suspense 包裹

**触达文件：**
- 见 commit `d85a364` diff

**数据库迁移：** 无

---

## 2026-04-10 — 项目合并功能

**提交：** `8cd6caa`

**改动：**
1. **项目合并**：勾选2个以上项目 → 选择保留目标 → 工作记录/检测记录/报告全部迁移 → 删除来源项目
   - 新增 `POST /api/projects/merge`：事务内完成迁移和删除
   - 项目列表页新增「合并项目」按钮和合并对话框

**触达文件：**
- `src/src/app/api/projects/merge/route.js`（新建）
- `src/src/app/master/projects/page.js`

**数据库迁移：** 无

---

## 2026-04-09 — 重构项目详情页为统一表格

**提交：** `55e016f`

**改动：**
1. **项目详情页大改**：去掉三个 Tab（合同信息/检测记录/出具报告），改为统一表格
   - 表格列：序号 | 检测项目 | 合同数量 | 单价 | 已检数量 | 检测时间 | 检测人员 | 报告编号 | 备注
   - 合同编号做成 badge 放在项目名称旁边，可点击
   - `findPriceItem()` 模糊匹配检测记录到合同价目表
2. **项目名称可点击**：项目列表页点击项目名称直接进入详情页
3. **合同上传简化**：去掉 OCR 检测参数匹配确认（后已恢复），加宽数量/单位/单价输入框
4. 新增 `POST /api/projects/[id]/detection-records`：手动添加检测记录
5. 侧边栏移除「检测报告」独立入口

**触达文件：**
- `src/src/app/master/projects/[id]/page.js`（重写）
- `src/src/app/master/projects/page.js`
- `src/src/app/contracts/page.js`
- `src/src/app/api/upload/route.js`
- `src/src/app/api/projects/[id]/detection-records/route.js`（新建）
- `src/src/components/Sidebar.js`

**数据库迁移：** 无

---

## 2026-04-08 — 清理测试数据 + 新增改动说明文件

**提交：** `b4afcef`

**改动：**
1. 清理上一轮验证遗留：删除测试合同/项目/PDF/验证脚本
2. 新建本文件 `改动说明.md`

**数据库迁移：** 无

---

## 2026-04-08 — 合同管理链路（CRUD + OCR 上传 + 自动关联项目）

**提交：** `6d83902`

**改动：**
1. 新增 `POST/GET/DELETE /api/contracts`：合同 CRUD + 自动关联/创建项目 + 产值补算
2. 新增 `/contracts` 前端页面：文件上传 OCR + 手动填写 + 单价/面积计价
3. `.gitignore` 修正
4. 项目列表按钮拆分：「手动新建(含合同)」和「快速新建(仅名称)」
5. `/contracts?manual=1` 模式

**数据库迁移：** 无（复用已有表）

---

## 2026-04-07 — 项目详情页 + 检测记录覆盖层 + 工程名去重

**提交：** `6d83902`（与上条合并）

**改动：**
1. Schema 新增 `ProjectDetectionRecord`
2. 新增 `detectionRecordSync.js`：覆盖层同步 + 漂移检测
3. Worklog POST/PUT 后自动同步覆盖层
4. 新增 `GET /api/projects/[id]` + `PATCH/DELETE /api/projects/[id]/detection-records/[rid]`
5. 新增详情页 `/master/projects/[id]/page.js`
6. `POST /api/projects` 工程名去重 409

**数据库迁移：** **是** — `npx prisma db push`

---

## 2026-04-06 — 产值上限 Bug 修复

**提交：** `d4c0ff5`

**改动：** 修复产值上限计算 3 个 Bug

---

## 2026-04-05 — 产值计算模块重构

**提交：** `a5c00b2`

**改动：** 重构产值计算：支持多定价方案（单价/面积/手动）、100% 上限检查、补算

---

## 2026-04-04 — 初始提交

**提交：** `1451b2e`

**改动：** 检测项目管理系统初始代码
## 2026-04-15 - 同合同项目关联新增“合并 / 子项”选择

**改动**
1. 项目列表里，如果把项目关联到一份已经被别的项目使用的合同，不再直接绑定。
2. 现在会先弹出选择：
   - 合并为同一个项目：把当前项目并入已有关联项目，原项目删除，记录一起转移。
   - 同一项目下的不同子项：当前项目保留，只是共用这份合同。
3. “工程阶段”文案同步改成“工程阶段 / 子项”，方便把楼栋、单体、分项写清楚。
4. 合同编辑接口改为默认保留同合同下的其他项目，不会因为保存合同把其他已关联子项自动解绑。

**触达文件**
- `src/src/app/master/projects/page.js`
- `src/src/app/master/projects/[id]/page.js`
- `src/src/app/api/contracts/route.js`

**验证**
- `cmd /c npm run build` 通过
- 用真实接口做了两组临时验证：
  - 临时项目关联到合同 `12` 后，确认能和原项目同时保留为同合同下的子项
  - 保存合同 `12` 后，确认不会把其他同合同项目解绑
  - 临时项目执行合并后，确认来源项目被删除，临时工作记录成功转移到目标项目
- 验证结束后，临时项目和临时工作记录已全部清理

## 2026-04-15 - 合并后项目名变成问号修复

**改动**
1. 把已经变成一串 `?` 的项目 `819` 恢复回真实项目名 `洱源产业园区邓川片区污水收集及处理项目`。
2. 合并接口新增保护：如果目标项目名本身已经坏成问号，会先从来源项目、同合同项目或该项目原始工作记录里找回正常名称，再执行合并。
3. 项目保存接口新增保护：如果有人把项目名保存成一串问号，系统会优先保留原来的正常名称，不再把项目名覆盖坏。

**触达文件**
- `src/src/app/api/projects/merge/route.js`
- `src/src/app/api/projects/route.js`
- `src/src/lib/projectNameRepair.js`

**验证**
- `cmd /c npm run build` 通过
- 直接核数据库，确认 `819` 当前项目名已恢复，相关工作记录也都重新显示在正确项目名下
- 用真实接口做了两组临时验证：
  - 把正常项目名改成 `????`，保存后项目名仍保持原来的正常名称
  - 把目标项目设成 `????`，再执行合并，合并完成后目标项目名会自动修回来源项目的正常名称
- 验证结束后，临时项目已清理，库里已不存在问号项目名

## 2026-04-15 - 项目详情页补全工作记录显示

**改动**
1. 项目管理的单独页面新增“全部工作记录”表，直接列出该项目下的完整工作记录。
2. 这张表会显示日期、检测内容、数量、人员、备注，以及这条记录有没有进入下方明细表。
3. 保留原来的“检测与报告记录”表不动，方便同时看原始工作记录和整理后的明细。

**触达文件**
- `src/src/app/api/projects/[id]/route.js`
- `src/src/app/master/projects/[id]/page.js`

**验证**
- `http://localhost:3000/api/projects/819` 实测返回 `11` 条工作记录、`10` 条检测明细
- `http://localhost:3000/master/projects/819` 页面请求返回 `200`
- `cmd /c npm run build` 通过

## 2026-04-15 - 项目详情页自动补齐缺失明细

**改动**
1. 项目详情接口现在会先检查这个项目下有没有“工作记录已经在项目里、但还没生成下方明细”的漏项。
2. 发现漏项后会先自动补齐，再把项目详情返回给页面。
3. 这次已经把 `819` 项目里 `2026-03-25` 的 `沉降布点 5点` 补进明细表。

**触达文件**
- `src/src/app/api/projects/[id]/route.js`

**验证**
- 实测访问 `http://localhost:3000/api/projects/819` 后，`819` 的明细数已从 `10` 条补到 `11` 条
- `2026-03-25` 这 4 条现在都能在项目详情里对上：`5335`、`5336`、`5340`、`5426`
- `cmd /c npm run build` 通过

## 2026-04-15 - 同名项目按子项区分并修复重复显示
**改动**
1. 新增统一的“项目名（子项）”识别规则，工作记录导入、编辑、拆分时都会优先按完整显示名匹配项目，不再把子项误归到主项目。
2. 项目管理和工作记录界面现在会直接显示完整项目名；有子项时显示为“项目名（子项）”，不再只显示主项目名。
3. 项目保存时允许“同名但不同子项”并存，但会拦住“名称和子项都完全一样”的重复项目。
4. 已把现有重复那条项目 `851` 修正为 `洱源产业园区邓川片区污水收集及处理项目（污水处理用房）`，和主项目 `819` 分开显示。
**触达文件**
- `src/src/lib/projectDisplayName.js`
- `src/src/lib/projectResolver.js`
- `src/src/app/api/projects/route.js`
- `src/src/app/api/worklog/route.js`
- `src/src/app/api/worklog/[id]/route.js`
- `src/src/app/api/worklog/[id]/split/route.js`
- `src/src/app/worklog/page.js`
- `src/src/app/master/projects/page.js`

**验证**
- `cmd /c npm run build` 通过
- 实测 `http://localhost:3000/api/projects`，确认 `819` 仍是主项目，`851` 已显示为 `污水处理用房` 子项
- 用临时项目实测：同名主项目和子项可以同时创建；同名同子项会被拦住；子项下工作记录编辑、拆分后仍留在原子项
- 验证结束后，临时项目和临时工作记录已全部清理
