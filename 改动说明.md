# 改动说明

本文件记录每次 Claude 会话对本仓库的改动，按时间倒序排列，便于在两台电脑之间接力开发。
每次完成一组改动（尤其是 commit/push 前）都会自动更新本文件。

约定：
- 「日期」使用 ISO 格式
- 「提交」指 commit 的短哈希
- 「触达文件」列出本次实际修改/新建的路径，便于切换机器后快速 review
- 「数据库迁移」标注是否需要在另一台机器执行 `npx prisma migrate dev`
- 「遗留/注意」列出未完成事项和本地需要手动执行的步骤

---

## 2026-04-08 — 清理测试数据 + 新增改动说明文件

**提交：** `(本次)`

**改动：**
1. 清理上一轮验证遗留：
   - 删除数据库中 id=1/2 的测试合同和项目（`test file/` 两份 PDF 走 OCR 流程创建的）
   - 删除 `contracts/` 目录下的测试 PDF
   - 删除一次性验证脚本 `src/verify-contracts.mjs`
2. 新建本文件 `改动说明.md`，约定后续每次改动都会自动更新此文件

**触达文件：**
- `改动说明.md`（新建）
- 删除：`src/verify-contracts.mjs`

**数据库迁移：** 无

**遗留/注意：**
- OCR 解析器对委托方识别不稳定、表格价目表识别率一般，属于 `src/src/lib/contractParser.js` 既有问题，未修改。
- `项目管理页面说明.md` 是用户的需求文档，未纳入版本控制（本来就 untracked）。

---

## 2026-04-08 — 合同管理链路（CRUD + OCR 上传 + 自动关联项目）

**提交：** `6d83902`

**背景：**
- 代码库里 `Contract` / `PriceItem` 表已定义，但没有任何 `prisma.contract.create` 的实际写入路径；UI 在调用 `/api/contracts` 但后端根本不存在。
- 需求要求「上传合同后按工程名自动建/关联项目，已有则拒绝重复」。

**改动：**
1. **新增 `POST/GET/DELETE /api/contracts`**（`src/src/app/api/contracts/route.js`）：
   - `POST`：接收合同 + 价目表数据，按 `projectName` 查 Project；存在且已有合同 → **409 拒绝**；存在且无合同 → 关联 + 调 `retroactiveCalculation()` 补算产值；不存在 → 新建项目并关联。也支持传 `projectId` 精确关联到指定项目。
   - `GET`：列出所有合同，include priceItems + projects。
   - `DELETE`：删合同前先解除项目关联。
2. **新增 `/contracts` 前端页面**（`src/src/app/contracts/page.js`）：
   - 文件上传 → `/api/upload` OCR → 结果自动回填表单
   - 工程名、合同信息、价目表均可编辑
   - 支持单价/面积两种计价方式
   - 合同列表展示（编号、委托方、工程名、关联项目、金额、删除）
   - 识别 URL query：`?projectId=&projectName=` 锁定到指定项目保存后跳回详情页
3. `.gitignore` 修正：`contracts/` → `/contracts/`，避免误伤 `src/src/app/contracts/` 和 `src/src/app/api/contracts/` 两个应用路由。
4. **项目列表按钮拆分**（`src/src/app/master/projects/page.js`）：
   - 「手动新建(含合同)」→ `/contracts?manual=1`，一次性填写工程名/合同信息/价目表
   - 「快速新建(仅名称)」→ 原有的简易表单（只建项目台账）
5. `/contracts?manual=1` 模式：页头文案切换、隐藏文件上传区块、保存后跳回 `/master/projects`

**触达文件：**
- `.gitignore`
- `src/src/app/api/contracts/route.js`（新建）
- `src/src/app/contracts/page.js`（新建）

**数据库迁移：** 无（复用已有的 `Contract` / `PriceItem` 表）

**验证：** 用 `test file/` 下两份真实合同（JC-2025-007、JC-2025-013）端到端走了一遍 `/api/upload` → `/api/contracts` → `/api/projects/[id]`，重名拒绝返回 409，均符合预期。测试数据已清理。

---

## 2026-04-07 — 项目详情页 + 检测记录覆盖层 + 工程名去重

**提交：** `6d83902`（与上条合并）

**背景：**
- 根据 `项目管理页面说明.md` 的需求：每个项目一个独立详情页，展示合同内容和检测记录表格；表格独立于 worklog 存储，用户可自由编辑，若与 worklog 原始记录不一致则高亮标记。

**改动：**
1. **Schema 新增 `ProjectDetectionRecord`**（`src/prisma/schema.prisma`）：
   - 用户可编辑字段：`sequence`, `testCategory`, `testItem`, `quantityText`, `detectDate`, `reportNo`, `reportEditor`, `mainTester`, `reviewer`, `approver`, `remarks`
   - src 快照字段：`srcTestItem`, `srcQuantityText`, `srcDetectDate`, `srcMainTester`（用于漂移检测）
   - `workLogId` unique + on delete set null（worklog 删除后覆盖层保留）
2. **新增 `src/src/lib/detectionRecordSync.js`**：
   - `syncDetectionRecordFromWorkLog()`：首次创建覆盖层 + 初始化 src 快照；已存在仅刷新 src 快照（不动用户编辑）
   - `computeDetectionDrift()`：比较 override 与 src，返回是否漂移
3. **Worklog POST/PUT 后调用 sync**（`src/src/app/api/worklog/route.js` 和 `src/src/app/api/worklog/[id]/route.js`）
4. **新增 `GET /api/projects/[id]`**（`src/src/app/api/projects/[id]/route.js`）：
   - 返回 project + contract(with priceItems) + detectionRecords(含 `isEdited` 标记)
5. **新增 `PATCH/DELETE /api/projects/[id]/detection-records/[rid]`**：单条覆盖层的内联编辑和删除
6. **新增详情页 `/master/projects/[id]/page.js`**：
   - 合同信息卡：客户 / 金额（单价模式自动汇总，面积模式取 `areaPricingAmount`）/ 计价方式 / 检测项目清单
   - 检测记录表：每个单元格可直接编辑，blur 保存；与工作记录原值不一致的字段用**橙色背景**高亮，hover 显示原值
7. **项目列表增加「详情」按钮**（`src/src/app/master/projects/page.js`）
8. **`POST /api/projects` 工程名去重**：已存在同名返回 409

**触达文件：**
- `src/prisma/schema.prisma`
- `src/prisma/migrations/20260407164248_add_project_detection_record/migration.sql`（新建）
- `src/src/lib/detectionRecordSync.js`（新建）
- `src/src/app/api/projects/route.js`
- `src/src/app/api/projects/[id]/route.js`（新建）
- `src/src/app/api/projects/[id]/detection-records/[rid]/route.js`（新建）
- `src/src/app/api/worklog/route.js`
- `src/src/app/api/worklog/[id]/route.js`
- `src/src/app/master/projects/page.js`
- `src/src/app/master/projects/[id]/page.js`（新建)

**数据库迁移：** **是** —— 另一台机器 `git pull` 后需要执行 `cd src && npx prisma migrate deploy`（或 `migrate dev`）。

**设计要点：**
- 覆盖层与 worklog 完全解耦：worklog 编辑只更新 src 快照，不覆盖用户在详情页的手动修改
- 工程名去重策略统一在 `POST /api/projects`、`POST /api/contracts`、worklog 的 `findOrCreateProject` 三个入口，防止绕路
