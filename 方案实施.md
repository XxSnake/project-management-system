# 方案实施

本文件是 **Claude（规划）** ↔ **Codex / Antigravity（实施）** ↔ **用户（裁判）** 之间的工单队列。

> **核心原则：**
> - Claude 只写这个文件（下发任务、记录验证反馈），不直接改代码
> - **Codex 和 Antigravity** 都读这个文件，按"🟡 待实施"的任务动手；完成后在 `改动说明.md` 顶部写改动记录
> - 用户在各模型之间传递成果；实施者说完工后，用户让 Claude 接手验证
> - Claude 验证前先读 `改动说明.md` 最新记录 + 本文件，然后对该任务复现、抽样检查、出结论
> - 验证通过 → 标 ✅；不通过 → 标 ❌ 并在任务下方追加"反馈"段，任务状态回到 🟡

---

## 协作状态标记

| 标记 | 含义 | 由谁改 |
|---|---|---|
| 🟡 待实施 | 已规划，等实施者开始 | Claude 开单 / 验证不通过时回退 |
| 🔵 实施中 | 某实施者正在做（会标注是谁） | 实施者开工时自改，写上"🔵 XXX 实施中" |
| 🟢 待验证 | 实施者声明完工 | 实施者写完 `改动说明.md` 后自改 |
| ✅ 已完成 | Claude 已验证通过 | Claude |
| ❌ 不通过 | Claude 验证有问题，见反馈 | Claude（同时写反馈段） |
| ⏸️ 暂缓 | 需要用户先决策 / 依赖其他任务 | 任意 |

---

## 交接须知（Codex / Antigravity 每次开工前请读完）

**⚠️ 防撞车：开工前先检查任务状态**
- 先通读本文件的**全部任务状态**。如果你想做的任务已经是 🔵 / 🟢 / ✅，说明另一个实施者已经在做或做完了，**不要重复做**
- 开工时立刻把任务状态从 🟡 改成 `🔵 XXX 实施中`（XXX = Codex 或 Antigravity），让另一方看到

**实施规则：**
1. **只改每个任务明确列出的"触达文件"**，不要扩大打击面
2. 每个任务的"禁区"段是硬限制，不得触碰
3. 完成后必须：
   - 在本文件里把任务状态改成 🟢 **并**在任务末尾的"实施记录"段填真实情况（触达文件、验证命令、遗留）
   - 实施记录末尾注明 `（该部分由 Codex 完成）` 或 `（该部分由 Antigravity 完成）`
   - 在 `改动说明.md` 最前面新增一条变更记录（照模板），标题带上任务 ID（如 `[T-009]`），正文末尾也注明由谁完成
4. 如果发现任务规划有问题，直接在任务下加"🟠 回问：..."段挂起任务，不要自己改任务方向
5. `dev.db` 只能通过业务接口或已有脚本改，不要直接 SQL 改表结构/数据
6. 如果必须改 `schema.prisma`，**先看任务里的"schema 改动"段是否已批准**；没有批准的 schema 改动要先挂 🟠 回问
7. 改完 schema 要记得：停 dev server → `cd src && npx prisma db push` → `npx prisma generate` → 重启
8. **不要碰另一个实施者正在做（🔵）或已完成（🟢/✅）的任务的代码和相关文件**

---

## 当前快照（2026-04-22）

**已完成并验证通过的任务：**
- T-001 ✅
- T-007 ✅
- T-008 ✅
- T-009 ✅
- T-010 ✅
- T-011 ✅（工作记录导入"预览→确认"+近似同名识别，Codex 完成）
- T-012 ✅（合同项目组支持合并 / 单体 / 子项角色管理并实装，Antigravity 完成）
- T-013 ✅（拆分工作记录改成"保留原记录 + 复制副本"，Codex 完成）
- T-015 ✅（area/mixed/lumpsum 三种合同支持手动占比+手动产值，Codex 完成，Claude 2026-04-21 复验通过）
- T-016 ✅（修 .gitignore + 清理脏文件，Codex 完成，Claude 2026-04-21 复验通过）
- T-017 ✅（项目详情 GET 改纯读 + repair 接口 + 前端提示，Codex 完成，Claude 2026-04-21 复验通过）
- T-014 ✅（合同管理界面点工程名弹出合同详情弹窗可编辑，Codex 完成，Claude 2026-04-21 复验通过）
- T-018 ✅（报表/导出展示名统一，Codex 完成，Claude 2026-04-22 复验通过）
- T-019 ✅（worklog PUT 事务化，Codex 完成，Claude 2026-04-22 复验通过）
- T-020 ✅（异常态收件箱骨架 + 基础规则落地，Codex 完成，用户 2026-04-22 复验通过）

- T-021 ✅（E5 缺占比批量处理，Codex 完成，Claude 2026-04-23 复验通过）
- T-022 ✅（E7 项目级 `noContractExpected` 标记，Codex 完成，Claude 2026-04-23 复验通过）
- T-024 ✅（E9 产值超限进收件箱 + 修复动作，Codex 完成，Claude 2026-04-23 复验通过）

**当前在排任务：**（无）

**已完成（待用户拍板）：**
- T-023 ✅（E8 LLM 项目名判重，Codex 完成，Claude 2026-04-27 复验通过——首轮反馈的 UI 乱码 + 错误消息乱码已全部修复，验证脚本新增 DOM 文本断言和 confirm/merge 400 断言，`lint` / `build` / `node src/scripts/verify-project-fuzzy-match.mjs` 全部通过）

**废弃任务（归并到 T-020~T-022）：**
- T-003 → 归并进 T-020「数量异常」tab
- T-004 → 归并进 T-021「缺占比」tab
- T-005 → 归并进 T-022「项目未绑合同」tab
- T-006 → 归并进 T-020「缺单价」tab

**当前关键数据状态：**
- 项目 818（清华园）：T-001 重跑后 2026-01 月产值合计 **0 元**
- 项目 819（邓川）：`buildingMode=true, phase=null`，15 条 worklog
- 项目 829（古生种质）：`buildingMode=true, phase="古生种质资源二期"`，18 条 worklog，单体建筑 1#/2#保育室 各 9 条
- 合同 #14 下 5 个项目（#814、#825、#828、#829、#834），phase 均已填
- 合同 #9 下 #811、#836 仍然 phase=null，等用户决定是否合并或分子项（黄色告警条命中）

---

## 任务列表（按优先级）

### ✅ T-016  [P1] 修 .gitignore + 清理工作区脏文件

**状态：** ✅ 已完成（Claude 2026-04-21 复验通过）

**背景：**

`src/.gitignore` 末尾行带异常字符（损坏状态），且当前工作区混入了多类运行/备份产物。对应 `项目审查.md` P-006，Claude 与 Codex 已对齐方案。

**实施方向：**

1. 修 `src/.gitignore`：
   - 找到并删掉末尾带异常字符的 `.dev-server.pid` 那一行，重写成干净的 `.dev-server.pid`
   - 补入以下忽略模式（分组追加，不要打乱现有结构）：
     ```
     # 运行时临时日志
     tmp-*.log
     tmp-*.err
     tmp-*.out
     /.codex-*.out
     /.codex-*.err
     /.dev-server.log

     # 数据库副本 / 测试库（主库 prisma/dev.db 不在此列）
     /dev.db
     test_download.db
     prisma/dev.db.bak-*
     ```
   - `src/scripts/verify-*.mjs` **不加忽略**，这批脚本应入库保留

2. 清理当前工作区里已有的脏文件（`git rm --cached` 或直接删除后 stage）：
   - `tmp-t008-server.err.log`、`tmp-t008-server.out.log`
   - `tmp-t011-server.err`、`tmp-t011-server.out`
   - `tmp-t013-server.err`、`tmp-t013-server.out`
   - `src/dev.db`
   - `src/test_download.db`
   - `src/prisma/dev.db.bak-20260414-082247`、`src/prisma/dev.db.bak-20260415-dengchuan`、`src/prisma/dev.db.bak-20260416-gusheng`

**验收标准：**
- `git status` 里不再出现上述脏文件
- `src/.gitignore` 末尾没有异常字符，新规则能正常匹配
- `src/scripts/verify-*.mjs` 仍在工作区且未被忽略
- `src/prisma/dev.db` 未被删除或忽略

**触达文件：**
- 修改：`src/.gitignore`
- 删除：上述列出的脏文件

**禁区：**
- 不能动 `src/prisma/dev.db`（主数据库）
- 不能删 `src/scripts/verify-*.mjs`
- 不能改任何业务代码

**实施记录：**
- 触达文件：
  - `.gitignore`
  - `src/.gitignore`
  - `改动说明.md`
  - `方案实施.md`
- 实施内容：
  1. 修掉了 `src/.gitignore` 末尾损坏的 `.dev-server.pid` 行，并在仓库根目录和 `src/.gitignore` 两处一起补上临时日志、测试库、备份库的忽略规则，避免根目录和 `src/` 下的脏文件继续冒出来。
  2. 清理了 `tmp-t008-server.*`、`tmp-t011-server.*`、`tmp-t013-server.*`、`src/test_download.db`、`src/prisma/dev.db.bak-*` 等本轮点名的临时产物；`src/prisma/dev.db` 主库没有动，`src/scripts/verify-*.mjs` 也保留了。
- 验证命令：
  - `git status --short`
  - `git check-ignore -v tmp-t008-server.err.log tmp-t011-server.err src/dev.db src/test_download.db src/prisma/dev.db.bak-20260414-082247 src/scripts/verify-project-name-matcher.mjs`
  - `Get-Item src/prisma/dev.db`
- 验证结果：
  - 这批脏文件已经不再出现在 `git status` 里。
  - 新忽略规则已命中临时日志、测试库和备份库；`src/scripts/verify-*.mjs` 未被误伤。
  - `src/prisma/dev.db` 主库仍在，未被删除，也没有被加进忽略。 （该部分由 Codex 完成）

---

### ✅ T-017  [P1] 项目详情 GET 改纯读 + 新建 repair 接口 + 前端提示

**状态：** ✅ 已完成（Claude 2026-04-21 复验通过）

**背景：**

`GET /api/projects/[id]` 在查询时会顺手补写缺失的检测记录（`src/src/app/api/projects/[id]/route.js:89-105`），违反了 GET 语义。对应 `项目审查.md` P-002，Claude 与 Codex 已对齐方案 A。

**实施方向：**

#### 第 1 步：GET handler 改为纯读

文件：`src/src/app/api/projects/[id]/route.js`

- 删除第 89-105 行的自动补写逻辑（`检测记录补写` 相关代码）
- 补一个查询：统计该项目下有多少条 worklog 的检测记录缺失，结果作为 `repairNeeded` 字段返回给前端
  ```js
  // 伪代码，字段名以实际 schema 为准
  const missingCount = await prisma.workLog.count({
      where: {
          projectId: id,
          detectionRecord: null,  // 或等价条件
      },
  });
  // 在返回 payload 里加：
  // repairNeeded: missingCount > 0,
  // missingDetectionRecordCount: missingCount,
  ```

#### 第 2 步：新建 repair 接口

新建文件：`src/src/app/api/projects/[id]/repair-inspections/route.js`

```js
// POST /api/projects/[id]/repair-inspections
// 把原来 GET handler 里的补写逻辑迁移到这里
// 幂等：已存在检测记录的 worklog 跳过，只补缺失的
// 返回：{ repairedCount, skippedCount }
```

#### 第 3 步：前端项目详情页加提示

文件：`src/src/app/master/projects/[id]/page.js`

- 页面加载时读 `repairNeeded` 和 `missingDetectionRecordCount`
- 若 `repairNeeded === true`，在页面顶部显示一个可关闭的黄色提示条：
  > "有 {missingDetectionRecordCount} 条工作记录的检测明细尚未同步，[点击修复]"
- 点"点击修复"后：调 `POST /api/projects/[id]/repair-inspections`，完成后刷新数据，提示条消失

**验收标准：**
1. `GET /api/projects/[id]` 不再触发任何写库操作（可用只读 SQLite 连接验证）
2. 返回 payload 包含 `repairNeeded: bool` 和 `missingDetectionRecordCount: number`
3. `POST /api/projects/[id]/repair-inspections` 能正确补写缺失记录，返回 `repairedCount + skippedCount`，重复调用幂等（第二次 `repairedCount=0`）
4. 前端项目详情页：有缺口时能看到黄色提示条，点修复后提示消失，数据刷新
5. 回归：项目详情页正常显示工作记录和检测记录，不因去掉自动补写而白屏或缺数据

**触达文件：**
- 修改：`src/src/app/api/projects/[id]/route.js`
- 新建：`src/src/app/api/projects/[id]/repair-inspections/route.js`
- 修改：`src/src/app/master/projects/[id]/page.js`

**禁区：**
- 不要改 worklog 保存/导入逻辑（那是 P-001 的活）
- 不要改 schema（不加新字段，只改接口行为）
- 不要动其他项目接口（list、batch-rename 等）

**实施记录：**
- 触达文件：
  - `src/src/app/api/projects/[id]/route.js`
  - `src/src/app/api/projects/[id]/repair-inspections/route.js`
  - `src/src/app/master/projects/[id]/page.js`
  - `docs/api_reference.md`
  - `改动说明.md`
  - `方案实施.md`
- 实施内容：
  1. 把 `GET /api/projects/[id]` 里的自动补写逻辑拆掉，改成纯读取：只统计当前项目还有多少条 worklog 缺少检测明细，并返回 `repairNeeded` / `missingDetectionRecordCount` 给前端。
  2. 新增 `POST /api/projects/[id]/repair-inspections`，把原来 GET 里的补写动作挪到这里；接口只补缺失项，已有检测明细的 worklog 会跳过，可重复调用。
  3. 项目详情页顶部新增黄色提示条和“点击修复”按钮；修复时会显示进行中状态，修完后给出成功/失败提示并刷新数据。
- 验证命令：
  - `cd src && cmd /c npm run lint`
  - `cd src && cmd /c npm run build`
  - `cd src && cmd /c npm run start -- --hostname 127.0.0.1 --port 3201`（临时 SQLite 库）
  - `cd src && node -`（临时脚本：创建 1 条缺失检测明细的 worklog，实测 GET 纯读、第一次 repair=1/0、第二次 repair=0/1）
- 验证结果：
  - 临时库实测：首次 `GET /api/projects/[id]` 返回 `repairNeeded=true`、`missingDetectionRecordCount=1`，且库里仍无 `projectDetectionRecord`，证明 GET 已纯读。
  - 同一条临时 worklog 第一次 `POST /api/projects/[id]/repair-inspections` 返回 `repairedCount=1, skippedCount=0`；第二次返回 `repairedCount=0, skippedCount=1`，幂等通过。
  - 额外尝试用 Playwright 打开 `/master/projects/[id]` 自动点“点击修复”，但这台环境启动 Chromium 仍报 `spawn EPERM`；接口级真验证已经通过，页面文案和按钮逻辑由 `lint/build` 保底，留待 Claude / 用户在有浏览器权限的环境复验。 （该部分由 Codex 完成）

---

### ✅ T-018  [P2] 报表/导出展示名统一（依赖 T-016/T-017 完成）

**状态：** ✅ 已完成（Claude 2026-04-22 复验通过）

**背景：**

`reportAggregation.js` 和导出接口多处直接用 `project.name`，没有拼上 `phase`（子项）和 `buildingName`（单体），导致同合同多子项在报表里显示成同名。对应 `项目审查.md` P-003，Claude 与 Codex 已对齐方案。

**⚠️ 前置条件：T-016 和 T-017 均已完成。**

**实施方向：**

**重要前提（Codex 确认）：** `src/src/lib/projectDisplayName.js` 里已有两个函数：
- `buildProjectDisplayName(project)` — 项目级展示名（`name + phase`）
- `buildWorkLogProjectDisplayName(log)` — 工作记录级展示名（`name + phase + buildingName`）

**不需要新建函数，直接复用上述两个。**

替换规则（Codex 实施时逐处读上下文确认，不要盲目替换）：

| 位置 | 当前用法 | 改成 |
|---|---|---|
| `reportAggregation.js:85`（汇总/矩阵行） | `project.name` | `buildProjectDisplayName(project)` |
| `reportAggregation.js:223`（汇总行） | `project.name` | `buildProjectDisplayName(project)` |
| `reportAggregation.js:236`（汇总行） | `project.name` | `buildProjectDisplayName(project)` |
| `reportAggregation.js:341`（明细工作记录行） | `project.name` 或类似 | `buildWorkLogProjectDisplayName(log)` |
| `reportAggregation.js:374`（明细工作记录行） | `project.name` 或类似 | `buildWorkLogProjectDisplayName(log)` |
| `api/export/route.js:23-29`（导出明细） | `project.name` | 区分汇总行/明细行，分别用对应函数 |

> Codex 实施前先读这几行代码的上下文，确认是汇总行还是明细行，再决定用哪个函数。如有疑问挂 🟠 回问。

**不需要改的：**
- 列头名（"项目名称"等）不改，只改数据内容
- 导出模板结构不改

**验收标准：**
1. 从报表页抽一个有 phase 的项目，确认报表里显示的是 `名称（子项）` 而不是纯 `名称`
2. 从报表页抽一个 buildingMode 的项目，明细行里能看到 `名称 - 单体名`
3. 没有 phase/building 的普通项目展示名不变
4. 导出 Excel，确认明细行展示名与页面一致
5. 回归：`lint` + `build` 通过；产值数字不变（只改展示名，不改计算逻辑）

**触达文件：**
- 修改：`src/src/lib/reportAggregation.js`
- 修改：`src/src/app/api/export/route.js`
- 文档：`改动说明.md`（顶部追加记录）、`方案实施.md`（完工后改状态）

**禁区：**
- 不要改 `buildProjectDisplayName` / `buildWorkLogProjectDisplayName` 的签名或实现（只是调用它们）
- 不要改产值计算逻辑
- 不要改 schema

**实施记录：**
- 触达文件：
  - `src/src/lib/reportAggregation.js`
  - `src/src/app/api/export/route.js`
  - `改动说明.md`
  - `方案实施.md`
- 实施内容：
  1. 给报表汇总和导出明细统一接上 `buildProjectDisplayName` / `buildWorkLogProjectDisplayName`，让项目汇总、报告明细、工作记录明细都按现有展示名口径输出。
  2. 项目-人员矩阵导出也改成走项目级展示名；只改数据内容，没有改列头、模板结构和任何产值计算逻辑。
- 验证命令：
  - `cd src && cmd /c npm run lint`
  - `cd src && cmd /c npm run build`
  - `cd src && node -`（临时脚本：启动 `next start`，实测 `/api/reports?groupBy=project`、明细导出、矩阵导出，核对子项名/单体名和总额一致性）
- 验证结果：
  - 项目报表已出现 `云南省洱海流域山水林田湖草沙一体化保护和修复工程（洱源县入洱海河流（弥茨河等4条）水生态环境保护和修复项目）` 这类带子项的完整展示名。
  - 明细导出已出现 `洱源产业园区邓川片区污水收集及处理项目（污水处理用房）` 这类带单体名的工作记录展示名。
  - 项目-人员矩阵导出同步出现带子项的完整项目名；汇总总额、检测工作总额、矩阵总额三者核对一致，证明这轮只改显示名，没有改坏数字。 （该部分由 Codex 完成）

**Claude 2026-04-22 复验：**
- 代码扫查：`reportAggregation.js` 已 import `buildProjectDisplayName` / `buildWorkLogProjectDisplayName`，内部辅助 `getProjectDisplayName` / `getWorklogDisplayName` 分别用于汇总行/报告明细（不带栋号）和工作记录明细（带栋号）；`export/route.js` 的 `buildProjectMatrixRows` 用 `buildProjectDisplayName(log.project)`。所有接入点符合"同合同多子项/多单体区分显示"的要求。
- 回归：`cd src && npm run lint` 无输出（通过），`cd src && npm run build` 31 条路由全部生成成功。
- 结论：✅ 通过。  [P2] worklog PUT 事务化（依赖 T-018 完成）

**状态：** 已完成（Claude 2026-04-22 复验通过）

**背景：**

`PUT /api/worklog/[id]` 的保存链路分多步执行（主记录更新 → 删旧产值 → 重算 → 同步检测记录），中间任一步出错会留下半完成状态。对应 `项目审查.md` P-001。Claude 与 Codex 已对齐：第一轮只收这一条链路，helper 需透传 `tx`。

**⚠️ 前置条件：T-018 已完成。**

**实施方向：**

#### 第 1 步：确认事务边界

`PUT /api/worklog/[id]` 的以下步骤需要包进同一个事务：
1. `workLog.update`（主记录更新）
2. `workLogStaff.deleteMany` + `workLogStaff.createMany`（人员关系重建）
3. `productionValue.deleteMany`（删旧产值）
4. `productionValue.create`（写新产值，由 `calculateProductionValue` 触发）
5. `projectDetectionRecord` 同步（由 `syncDetectionRecordFromWorkLog` 触发）

**不包进事务的**（保持事务外异步执行）：
- `rebuildProjectProduction`（批量重建，跨多条记录，不纳入本轮）
- `applyProductionCap` 的跨项目聚合查询（只纳入当前条的 cap 判断部分）

#### 第 2 步：helper 改造（透传 tx）

以下三个 helper 需要改签名，接受可选的 `tx` 参数：
- `calculateProductionValue(workLog, staffIds, { tx? })` — 内部写 `productionValue` 时改用 `tx`
- `syncDetectionRecordFromWorkLog(workLogId, { tx? })` — 内部读写 `projectDetectionRecord` 时改用 `tx`
- `applyProductionCap(params, { tx? })` — 内部读 `productionValue` 累计时改用 `tx`

**改法：** 每个函数加可选参数 `tx = prisma`（默认值为全局 prisma），函数内部把 `prisma.xxx` 改成 `tx.xxx`。这样不传 tx 时行为不变，传 tx 时参与事务。

#### 第 3 步：PUT handler 包事务

```js
// src/src/app/api/worklog/[id]/route.js — PUT handler
await prisma.$transaction(async (tx) => {
    await tx.workLog.update({ ... });
    await tx.workLogStaff.deleteMany({ ... });
    await tx.workLogStaff.createMany({ ... });
    const prodResult = await calculateProductionValue(workLog, staffIds, { tx });
    if (prodResult.status !== 'no-price-match') {
        await tx.productionValue.deleteMany({ ... });
        // createProductionValues 已经在 calculateProductionValue 内部用 tx 写
    }
    await syncDetectionRecordFromWorkLog(workLogId, { tx });
    await applyProductionCap(capParams, { tx });
});
// 事务外：rebuildProjectProduction（保持现状）
```

**验收标准：**
1. `PUT /api/worklog/[id]` 在保存中途模拟报错（比如临时破坏 createMany 的入参），确认数据库没有出现半完成状态（主记录已改但产值未重建）
2. 正常保存流程行为与改造前一致（产值、检测记录、人员关系都正确）
3. 未传 `tx` 的其他调用方（比如导入流程调 `calculateProductionValue`）行为不变
4. `lint` + `build` 通过
5. `verify-mixed-allocation.mjs` 等现有回归脚本仍然通过

**触达文件：**
- 修改：`src/src/app/api/worklog/[id]/route.js`（PUT handler）
- 修改：helper 文件（`calculateProductionValue` 所在文件、`syncDetectionRecordFromWorkLog` 所在文件、`applyProductionCap` 所在文件）

> Codex 开工前先 grep 确认这三个 helper 的实际文件路径，不要假设行号。

**禁区：**
- 不要改合同保存链路（`PUT /api/contracts`）——那是后续单独一轮
- 不要改 `rebuildProjectProduction` 的调用方式（事务外保持不变）
- 不要改 schema
- 不要动 worklog 导入（POST）路径——只改 PUT

**实施记录：**
- 触达文件：
  - `src/src/app/api/worklog/[id]/route.js`
  - `src/src/lib/productionCalculator.js`
  - `src/src/lib/productionCap.js`
  - `src/src/lib/detectionRecordSync.js`
  - `改动说明.md`
  - `方案实施.md`
- 实施内容：
  1. 把 `PUT /api/worklog/[id]` 里的工作记录更新、人员关系重建、旧产值删除、新产值重算、检测记录同步统一包进 `prisma.$transaction(...)`。
  2. `calculateProductionValue`、`applyProductionCap`、`syncDetectionRecordFromWorkLog` 都补了可选 `tx` 参数；不传时保持旧行为，传入事务客户端时整条链路一起提交或一起回滚。
  3. 保存入口顺手把“按项目名找/建项目”和“按姓名找/建人员”也改成走同一个事务客户端，避免保存失败时残留半截关联数据。
- 验证命令：
  - `cd src && cmd /c npm run lint`
  - `cd src && cmd /c npm run build`
  - `cd src && node -`（临时脚本：复制临时 SQLite 库，启动 `next start`，同一条 worklog 先跑一次正常保存，再用重复 `staffNames` 触发 `workLogStaff.createMany` 唯一约束报错，检查是否整条回滚）
- 验证结果：
  - 正常保存后，数量、备注、手工产值、人员关系、产值记录、检测记录都按预期更新。
  - 故意触发 `createMany` 报错后，接口返回 `500`，但数据库里该 worklog 仍保持上一轮成功状态；数量/备注/手工产值/人员/产值/检测记录都没有被半途改坏。
  - 现有调用方未传 `tx` 时仍可正常编译和运行，`lint` / `build` 均通过。 （该部分由 Codex 完成）

**Claude 2026-04-22 复验：**
- PUT handler（`src/src/app/api/worklog/[id]/route.js:93`）：`prisma.$transaction(async (tx) => { ... })` 包裹全部写入——`findOrCreateProjectByDisplayName(name, projectId, tx)`、`resolveStaffIds(names, tx)`、`tx.workLog.update`、`tx.workLogStaff.deleteMany/createMany`、`tx.productionValue.deleteMany`、`calculateProductionValue({...}, staffIds, { tx })`、`syncDetectionRecordFromWorkLog(worklogId, { tx })`、事务末尾的 `tx.workLog.findUnique` 刷新。事务外无半完成写操作。
- Helper 透传 tx 全部落实：
  - `productionCalculator.js:120` `calculateProductionValue(workLog, staffIds, options = {})` → 内部 `resolveProjectWithContract(workLog, tx)`、`createProductionValues({...}, { tx })`，6 处 `createProductionValues` 调用点（163/201/240/264/288/313）均传 `{ tx }`。
  - `productionCalculator.js:58` `createProductionValues` → `applyProductionCap({...}, { tx })` + `tx.productionValue.create`。
  - `productionCap.js`：`applyProductionCap`（126）、`getContractTotalAmount`（10）、`getCumulativeProjectValue`（54）、`getScopedProjectValue`（84）均接 `options = {}` 并对 `tx` 透传。
  - `detectionRecordSync.js:22` `syncDetectionRecordFromWorkLog` 同模式，事务内读写一致。
  - `projectResolver.js`：`findProjectByDisplayName` / `findOrCreateProjectByDisplayName` 第 3 参 `prismaClient = prisma`，PUT 里正确传 tx。
- `productionCalculator.js` 内剩余裸 `prisma.*` 调用点（374/493/512/527/541/561/633/661/710/717）分属 `calculateReportProductionValue` / `rebuildProjectProduction`，不在本轮事务范围内，保持原样。
- 回归：`npm run lint` 干净通过，`npm run build` 31 条路由全部生成。
- 结论：✅ 通过。

### ✅ T-020  [P0] 异常态收件箱骨架 + 基础规则落地

**状态：** ✅ 已完成（用户 2026-04-22 复验通过）

**背景：**

2026-04-22 用户和 Claude 对齐：系统的数据质量问题要在"数据入口处按规则处理"，不再用"历史数据逐条清理"的方式（废弃 T-003~T-006）。用户明确的三条决定：
1. 所有入口（导入 / 手填 / 编辑 / 拆分 / 合同改动触发 retroactive）一视同仁，走同一套规则
2. 异常态默认"挂起"——落库 + 打标 + 进收件箱，不硬拦截
3. 做一个统一的"异常态收件箱"页面，按类别集中处理

方案 A（workLog 加字段 + 派生查询）已选定，不新建异常表；收件箱是实时 SQL 视图。

**覆盖的异常态（本任务负责 E1/E3/E4/E5/E6/E7 的识别 + 显示 + 基础修复）：**

| 代号 | 异常态 | 产值 | 修复动作（本任务内） |
|---|---|---|---|
| E1 | `invalid-quantity` — q≤0 或非数字（非布点） | 0 | ①编辑数量 ②标已知忽略 |
| E3 | `missing-staff` — 无人员 | 0 | ①补人员 ②标已知忽略 |
| E4 | `no-price-match` — unit/mixed 无匹配单价 | 0 | ①改测试内容重试 ②转手动产值 ③标已知忽略（新建价目项由用户去合同页处理） |
| E5 | `pending-area-share` — area/mixed(有lumpSum)/lumpsum 缺占比 | 0 | ①单条填占比 ②转手动产值（批量处理见 T-021） |
| E6 | `contract-incomplete` — 合同缺金额字段 | 0 | ①跳转合同页 ②转手动产值 |
| E7 | `workload-only` — 项目无合同 | 0 | ①跳转项目页绑合同 ②标已知忽略（`noContractExpected` 项目级按钮见 T-022） |
| E9 | `exceeded` — 产值累计超合同额 100% | cap 后 | 仅识别显示，修复动作见 T-024 |

**E2（布点）保持现状，是正常态不进收件箱。E8（项目疑似重名）见 T-023。**

---

**实施方向：**

#### 第 1 步：Schema 改动（批准）

`src/prisma/schema.prisma` 追加两个字段：

```prisma
model WorkLog {
  // ...现有字段
  acknowledgedExceptions String?  // 逗号分隔："invalid-quantity,no-price-match"；null=无任何忽略
}

model Project {
  // ...现有字段
  noContractExpected Boolean @default(false)  // true=用户确认"此项目本就不需要合同"，不再出现在 E7 收件箱
}
```

改完后：停 dev server → `cd src && npx prisma db push` → `npx prisma generate` → 重启。

#### 第 2 步：新建异常检测库

新建文件 `src/src/lib/workLogExceptions.js`，对外导出：

```js
// 异常类型常量
export const EXCEPTION_TYPES = {
    INVALID_QUANTITY: 'invalid-quantity',
    MISSING_STAFF: 'missing-staff',
    NO_PRICE_MATCH: 'no-price-match',
    PENDING_ALLOCATION_SHARE: 'pending-area-share',
    CONTRACT_INCOMPLETE: 'contract-incomplete',
    WORKLOAD_ONLY: 'workload-only',
    EXCEEDED: 'exceeded',
};

// 检测单条 worklog 当前有哪些异常（不含已 acknowledged 的）
// 输入: workLog（含 project.contract 和 staffMembers 和 productionValues 和 acknowledgedExceptions）
// 返回: Set<exceptionType> — 实际生效的异常（已过滤 acknowledged）
export function detectWorkLogExceptions(workLog) { ... }

// 构建 Prisma where 子句，用于收件箱列表查询
// 输入: { type?, projectId?, onlyUnacknowledged: true }
// 返回: Prisma WorkLog where clause
export function buildInboxWhere(filter) { ... }

// 解析 acknowledgedExceptions 字段（容忍 null / 空串 / 重复）
export function parseAcknowledgedExceptions(value) { ... }  // 返回 Set
export function serializeAcknowledgedExceptions(set) { ... }  // 返回 String or null

// 对 acknowledgedExceptions 字段做原子更新
export function addAcknowledgement(current, type) { ... }    // 返回新字符串
export function removeAcknowledgement(current, type) { ... } // 返回新字符串
```

**检测规则对照表（实施者参考）：**

| 异常类型 | 触发条件 |
|---|---|
| `invalid-quantity` | `(quantity IS NULL OR quantity <= 0) AND NOT isNonBillableLayoutWork(testContent)` |
| `missing-staff` | `staffMembers 为空` |
| `no-price-match` | `project.contract 存在 AND pricingMode IN ('unit','mixed') AND productionValues 为空 AND manualTotalValue IS NULL AND NOT isNonBillableLayoutWork AND allocationShare IS NULL`（mixed 在 allocationShare 非空且合同有 lumpSumAmount 时走 E5 而不是 E4） |
| `pending-area-share` | `project.contract 存在 AND pricingMode='area' AND allocationShare IS NULL` 或 `pricingMode='lumpsum' AND allocationShare IS NULL AND contract.lumpSumAmount > 0` 或 `pricingMode='mixed' AND contract.lumpSumAmount > 0 AND allocationShare IS NULL AND manualTotalValue IS NULL` |
| `contract-incomplete` | `(pricingMode='area' AND contract.areaPricingAmount IS NULL/0) OR (pricingMode='lumpsum' AND contract.lumpSumAmount IS NULL/0)` |
| `workload-only` | `project.contract IS NULL AND project.noContractExpected = false AND manualTotalValue IS NULL` |
| `exceeded` | `productionValues 里存在 exceeded=true 的记录` |

异常并存允许（同一条 worklog 可同时有多种异常），前端分类 tab 独立统计。

#### 第 3 步：新建收件箱 API

新建目录 `src/src/app/api/inbox/`：

- `src/src/app/api/inbox/exceptions/route.js`
  - `GET /api/inbox/exceptions?type=xxx&projectId=xxx&page=1&pageSize=50`
  - 返回：
    ```json
    {
      "counts": { "invalid-quantity": 3, "missing-staff": 0, "total": 45 },
      "items": [{ "workLogId": 123, "projectName": "...", "workDate": "...", "testContent": "...", "quantity": 0, "staffNames": [], "exceptions": ["no-price-match"] }],
      "total": 45, "page": 1, "pageSize": 50
    }
    ```

- `src/src/app/api/inbox/acknowledge/route.js`
  - `POST` body: `{ workLogId, exceptionType }`
  - 在目标 worklog 的 `acknowledgedExceptions` 字段追加该类型（幂等）
  - 返回：`{ ok: true, acknowledgedExceptions: "..." }`

- `src/src/app/api/inbox/reset-acknowledgement/route.js`
  - `POST` body: `{ workLogId, exceptionType }`
  - 从字段里移除该类型

#### 第 4 步：新建收件箱页面

新建 `src/src/app/master/inbox/page.js`：

- 顶部 tabs：全部 / 数量异常（E1）/ 缺人员（E3）/ 缺单价（E4）/ 缺占比（E5）/ 合同缺字段（E6）/ 项目未绑合同（E7）/ 产值超限（E9，只显示不修复）
- 每个 tab 显示数量徽标（从 `counts` 读）
- 列表字段：工作日期 / 项目（用 `buildWorkLogProjectDisplayName`）/ 测试内容 / 数量 / 单位 / 人员 / 异常类型 / 操作按钮
- 每条支持的操作按钮（根据异常类型显示不同按钮，见上表"修复动作"列）：
  - "编辑"：弹出工作记录编辑弹窗（复用现有 UI 或新建简版）
  - "转手动产值"：直接在行内打开小弹窗填 `manualTotalValue`
  - "标已知忽略"：调 `POST /api/inbox/acknowledge`
  - "跳转合同页" / "跳转项目页"：路由到对应页面

#### 第 5 步：Sidebar 加入口 + 徽标

- Grep 定位现有 Sidebar 组件（预计在 `src/src/components/` 下）
- 新增菜单项"异常处理"，路由 `/master/inbox`
- 菜单项右侧显示未处理异常总数徽标（从 `GET /api/inbox/exceptions?page=1&pageSize=1` 的 `counts.total` 读，或做单独的 count-only 接口）
- 徽标数在每次进/出收件箱页时刷新（简单做法即可）

#### 第 6 步：确保所有数据入口不因 E1/E3 拦截

**Grep 确认并修复**以下入口：
- `POST /api/worklog`（单条 + 批量导入）
- `PUT /api/worklog/[id]`
- `POST /api/worklog/[id]/split`
- 所有上述入口必须确认：q=0 能保存、无人员能保存；不得因 `invalid-quantity` 或 `missing-staff` 拒绝请求
- `calculateProductionValue` 已返回 status 不抛错（`productionCalculator.js:124-127`），不需要改底层

如果发现某个入口确实拦了（比如在 zod 或手写校验里），改成"警告但放行"。

---

**验收标准：**

1. **Schema 应用成功**：`workLog.acknowledgedExceptions`、`project.noContractExpected` 字段已落库
2. **E1 验证**：用 API 造一条 q=0 且 testContent 非布点的 worklog，保存成功；收件箱"数量异常"tab 显示该条；编辑成 q>0 后该条从收件箱消失
3. **E3 验证**：造一条无人员的 worklog，收件箱"缺人员"tab 显示；补完人员后消失
4. **E4 验证**：用一个 unit 合同项目，造一条测试内容不匹配合同价目表的 worklog，"缺单价"tab 显示；改 testContent 匹配后消失
5. **E5 验证**：用一个 area 合同项目，造一条 allocationShare=null 的 worklog，"缺占比"显示；单条填占比后消失
6. **E6 验证**：用一个 area 合同但 areaPricingAmount=null 的项目，worklog 出现在"合同缺字段"
7. **E7 验证**：用一个无合同项目，worklog 出现在"项目未绑合同"；把项目 `noContractExpected=true` 后，该项目下所有 worklog 从该 tab 消失
8. **E9 验证**：造一批 worklog 使合同累计 >100%，"产值超限"tab 能列出这些记录（不要求支持修复动作）
9. **标已知忽略幂等**：同一条同一类型连续 ack 两次不报错，第二次无副作用
10. **Sidebar 徽标**：未处理总数正确显示；修复一条后徽标数减 1
11. **lint + build 通过**：`cd src && npm run lint && npm run build`
12. **回归**：所有现有工作记录相关功能（单条保存、导入、拆分、编辑）行为与改造前一致；产值计算数字不变

**验证必须做的真实数据抽样：**
- 把当前库里原 T-004 的 20 条 pending-area-share 记录列出来，确认它们出现在收件箱"缺占比"tab 且数量一致
- 把原 T-005 的 13 个无合同项目列出来，确认关联 worklog 出现在"项目未绑合同"
- 把原 T-006 项目 812 的 4 条 no-price-match 列出来，确认在"缺单价"tab

---

**触达文件：**

- 新建：`src/src/lib/workLogExceptions.js`
- 新建：`src/src/app/api/inbox/exceptions/route.js`
- 新建：`src/src/app/api/inbox/acknowledge/route.js`
- 新建：`src/src/app/api/inbox/reset-acknowledgement/route.js`
- 新建：`src/src/app/master/inbox/page.js`
- 修改：`src/prisma/schema.prisma`（加两字段）
- 修改：Sidebar 组件（实施者 Grep 定位）
- 可能修改：`src/src/app/api/worklog/route.js`、`src/src/app/api/worklog/[id]/route.js`（若有 E1/E3 硬拦截要去掉）
- 修改：`docs/api_reference.md`（补收件箱 3 个接口文档）
- 修改：`改动说明.md`、`方案实施.md`

---

**禁区：**

- 不要改 `calculateProductionValue` 的产值计算逻辑（只能读它的 status，不改算法）
- 不要实现批量占比处理（那是 T-021）
- 不要实现 E7 的"标记项目为 noContractExpected"按钮（那是 T-022；T-020 只负责 schema 字段落库和"项目 noContractExpected=true 时不出现在收件箱"这条查询规则）
- 不要实现 E8 LLM 判重（那是 T-023）
- 不要实现 E9 的修复动作（那是 T-024；T-020 只负责"显示在超限 tab"）
- 不要改 `rebuildProjectProduction`、`retroactiveCalculation`
- 不要改任何产值重算触发点

**实施记录：**
（实施者填写）

---

### ✅ T-021  [P1] E5 缺占比批量处理

**状态：** ✅ 已完成（Claude 2026-04-23 复验通过）

**⚠️ 前置条件：T-020 已完成。**

**背景：**

T-020 的"缺占比"tab 只支持单条填占比。原 T-004 累积的 pending-area-share 主要集中在项目 819 和项目 824，一条条手填效率太低。实施时顺手排查出其中 `#5426` 是布点类记录，按现有规则本来就不计产值，不应继续挂在 E5 里；剔除后，当前可批量处理的是项目 819 的 14 条 + 项目 824 的 5 条。

**实施方向：**

在收件箱"缺占比"tab 加批量操作：

1. **选择机制**：列表前加 checkbox，支持全选 / 反选 / 按合同分组多选
2. **批量弹窗**，提供三种分摊策略：
   - **平均分**：同一合同内选中的 N 条，占比 = 100% / N（若只选部分，用户需自保合计合理）
   - **按数量加权**：按每条的 `quantity` 占总数量的比例分配
   - **统一百分比**：用户输入一个百分比，全部套用
3. **预览步骤**：按策略预算完后，先显示每条的新占比和预计产值，用户确认后才落库
4. **跨合同校验**：批量操作限制在"同一合同下"；选中跨合同时，按合同分组分别应用或直接报错"请先按合同筛选"

后端新增接口 `POST /api/inbox/batch-allocate`：
- body: `{ items: [{ workLogId, allocationShare }] }`
- 循环复用 `PUT /api/worklog/[id]` 的占比更新路径，确保触发产值重算

**验收标准：**

1. 选中项目 819 的 14 条，批量"按数量加权" → 预览显示各条占比加起来=100% → 确认后 14 条全部有产值
2. 选中项目 824 的 5 条，批量"统一百分比 20%" → 5 条各占 20%，产值 = 合同额 × 20%
3. 跨合同选择正确分组或报错
4. "平均分"后占比总和精确=100%（浮点处理对齐，最后一条 round 补差）
5. lint + build 通过
6. 现有单条填占比功能不受影响

**触达文件：**
- 修改：`src/src/app/master/inbox/page.js`
- 新建：`src/src/app/api/inbox/batch-allocate/route.js`
- 修改：`docs/api_reference.md`、`改动说明.md`、`方案实施.md`

**禁区：**
- 不要改 `calculateProductionValue` 或 `productionCap`
- 不要新建其他异常类型的批量处理（本任务只负责 E5）

**实施记录：**
- 代码改动：
  - 新增 `src/src/lib/inboxBatchAllocation.js`，收口批量占比分摊和预览逻辑，支持平均分 / 按数量加权 / 统一百分比三种策略。
  - 新增 `src/src/lib/workLogMutations.js`，把单条 worklog 更新 + 产值重算抽成共享路径，供 `PUT /api/worklog/[id]` 和批量接口共用。
  - 新增 `src/src/app/api/inbox/batch-allocate/route.js`，只接受同一合同下一组记录；跨合同直接报错。
  - 修改 `src/src/app/master/inbox/page.js`，E5 tab 增加 checkbox、合同分组选择、批量预览弹窗和统一保存入口。
  - 修改 `src/src/lib/workLogExceptions.js`，把布点/布设类“不计产值”记录从 E5 缺占比里剔除，避免假异常。
- 验证命令：
  - `cd src && cmd /c npm run lint`
  - `cd src && cmd /c npm run build`
  - `cd src && node .tmp-verify-t021.cjs main file:./.codex-t021-verify.db`
  - `cd src && node .tmp-verify-t021.cjs even file:./.codex-t021-even.db`
- 验证结果：
  - 当前真实数据副本里，E5 可批量处理记录为 19 条：项目 `819` 的 14 条、项目 `824` 的 5 条；`#5426`（沉降布点）已不再出现在 E5。
  - 项目 `819` 的 14 条按数量加权后，占比总和精确为 `100%`，14 条都成功写入占比并生成产值。
  - 项目 `824` 的 5 条统一填 `20%` 后，5 条都成功写入 `20%` 并生成产值。
  - 故意混选 `819 + 824` 各 1 条时，批量接口返回 `400`，正确拦截跨合同操作。
  - 另用 `5436 / 5435 / 5434` 三条跑“平均分”，保存前后占比总和都精确为 `100%`。
  - `lint` / `build` 均通过；现有单条“填占比”入口未受影响。 （该部分由 Codex 完成）

---

### ✅ T-022  [P1] E7 项目级 `noContractExpected` 标记

**状态：** ✅ 已完成（Claude 2026-04-23 复验通过）

**⚠️ 前置条件：T-020 已完成（schema 字段已加）。**

**背景：**

E7 "项目未绑合同"的处理里，有些项目**确实不应该有合同**（比如内部项目、试点、公益项目），不是"待绑合同"。把这类项目一次性标为 `noContractExpected=true`，让下面的 worklog 永远不出现在 E7 收件箱。

T-020 已在 schema 加好字段，也在 E7 查询里过滤掉了这类项目。本任务只需补上**前端和后端 API 层的操作入口**。

**实施方向：**

1. **项目编辑页（`src/src/app/master/projects/[id]/page.js`）**：
   - 加一个"此项目无需合同"勾选框，放在合同关联区域附近
   - 勾选保存 → `PUT /api/projects/[id]` 把 `noContractExpected=true`
   - 取消勾选同理

2. **项目列表页（`src/src/app/master/projects/page.js`）**：
   - 在无合同项目的列表视图里加"无需合同"标记列，或把它们分组显示

3. **API 层**：
   - `PUT /api/projects/[id]` 接受 `noContractExpected` 字段
   - 切换为 true 时，下次打开收件箱该类 worklog 自动排除（派生查询无需额外动作）

4. **收件箱批量操作（可选）**：
   - E7 tab 提供"批量标为无需合同"按钮：选中若干项目 → 一次更新多个 `project.noContractExpected`

**验收标准：**

1. 项目编辑页能勾选保存，数据库字段正确更新
2. 勾选后，该项目下所有 worklog 从收件箱 E7 tab 消失
3. 取消勾选后重新出现
4. 项目列表页能看到"无需合同"标记
5. 对原 T-005 的 13 个无合同项目做批量勾选，E7 tab 数量归 0 或只剩真正待绑合同的项目
6. lint + build 通过

**触达文件：**
- 修改：`src/src/app/api/projects/[id]/route.js`
- 修改：`src/src/app/master/projects/[id]/page.js`
- 修改：`src/src/app/master/projects/page.js`
- 修改：`src/src/app/master/inbox/page.js`（批量操作按钮，可选）
- 修改：`docs/api_reference.md`、`改动说明.md`、`方案实施.md`

**禁区：**
- 不要改其他项目字段的保存逻辑
- 不要改 worklog 计算

**实施记录：**
- 代码改动：
  - 在 `src/src/app/api/projects/[id]/route.js` 新增 `PUT`，专门接收 `noContractExpected` 更新。
  - 在 `src/src/app/api/projects/route.js` 补了同字段的兼容处理：项目一旦关联合同，会自动把 `noContractExpected` 清掉；已关联合同的项目不能反向标成“无需合同”。
  - `src/src/app/master/projects/[id]/page.js` 新增“合同要求”卡片，可直接勾选/取消“此项目无需合同”。
  - `src/src/app/master/projects/page.js` 新增“合同策略”列，并补了单条切换、批量标记、批量取消三种操作入口。
- 验证命令：
  - `cd src && cmd /c npm run lint`
  - `cd src && cmd /c npm run build`
  - `cd src && node .tmp-verify-t022.cjs file:./.codex-t022-verify.db`
- 验证结果：
  - 当前 E7 的无合同项目为 13 个，项目 ID 为 `816, 817, 820, 821, 823, 826, 827, 831, 839, 843, 844, 849, 850`。
  - 项目 `816` 勾选“无需合同”后，立即从 E7 消失；取消勾选后重新出现。
  - 把这 13 个项目全部标为“无需合同”后，E7 收件箱清零。
  - 对已关联合同的项目 `811` 测试“标为无需合同”，接口返回 `400`，正确拦截。
  - `lint` / `build` 均通过。 （该部分由 Codex 完成）

---

### ✅ T-023  [P2] E8 LLM 项目名判重（后台异步 + 收件箱复核）

**状态：** ✅ Claude 2026-04-27 第二轮复验通过

**⚠️ 前置条件：T-020 已完成。**

**背景：**

T-011 实现了"导入预览 + 近似同名识别"的**字面规则**。用户 2026-04-22 要求升级：改成**后台异步调 LLM 判重**，把疑似重名项目挂进收件箱由人工确认。

这个改动不替代 T-011 的入库前同步弹窗，作为**后台兜底**：即使用户导入时点了"建新项目"，系统也会后台再用 LLM 扫一遍，发现疑似重名时进收件箱。

**实施方向：**

#### 第 1 步：Schema 改动

```prisma
model Project {
  fuzzyMatchStatus       String?   // null / 'pending-review' / 'confirmed-distinct' / 'merged-into-other' / 'error'
  fuzzyMatchCandidateIds String?   // JSON 数组 "[812,818,820]"
  fuzzyMatchedAt         DateTime? // 上次判重时间，避免重复扫
}
```

#### 第 2 步：LLM 判重服务

新建 `src/src/lib/projectFuzzyMatchLLM.js`：

```js
// 接收一个目标项目 + 候选名单
// 返回 { matches: [{ projectId, confidence, reason }], decidedDistinct: bool }
export async function evaluateFuzzyDuplicate(targetProject, candidates, { modelKey }) {
    // 构造 prompt（只送项目名，不送金额等敏感数据）
    // 解析返回
}
```

**模型选择**：复用 `.env` 里现有的 LLM 配置（GLM-5 那套）。不够用时加其他 API，`.env` 需用户提供。

#### 第 3 步：异步判重触发器

新建 `src/src/lib/projectFuzzyMatchScheduler.js` 或加到项目创建/重命名路径：

- **触发时机**：`POST /api/projects`、`PUT /api/projects/[id]`（改 name）、`POST /api/projects/batch-rename`
- **执行方式**：不阻塞主请求；fire-and-forget 在后台调 LLM；完成后更新三个字段
- **失败处理**：失败时写日志 + `fuzzyMatchStatus='error'`，下次定时重扫

**注意：** Next.js App Router 没有原生后台任务机制。可行方案：
- `setImmediate(() => evaluate(...))` — 不保证完成
- 更稳：写 `src/src/app/api/internal/run-fuzzy-match/route.js` 由前端主动触发，或 `src/scripts/scan-fuzzy-duplicates.mjs` 脚本手动跑
- 实施者选一种并在记录里说明

#### 第 4 步：收件箱新增"项目疑似重名"tab

- 数据源：`project WHERE fuzzyMatchStatus='pending-review'`
- 显示：目标项目名 / 候选项目（含 ID 和展示名）/ 判重时间 / 操作按钮
- 操作按钮：
  - **合并到候选**：调用现有 `POST /api/projects/merge`
  - **保持独立**：`fuzzyMatchStatus` 改为 `confirmed-distinct`
  - **改名**：跳转项目编辑页

#### 第 5 步：新建 API

- `POST /api/inbox/fuzzy-match/confirm` — 确认保持独立
- `POST /api/inbox/fuzzy-match/merge` — 调用合并
- `POST /api/internal/run-fuzzy-match`（可选）— 手动触发扫描

**验收标准：**

1. 新建一个名为"清华园项目二期"的项目（假设库里已有"清华园项目"），短时间内或手动触发后该项目 `fuzzyMatchStatus='pending-review'`，候选含"清华园项目"
2. 收件箱"项目疑似重名"tab 能看到这条
3. 点"保持独立"后 `fuzzyMatchStatus='confirmed-distinct'`，从收件箱消失
4. 点"合并到候选"后正确调用 merge 接口并消失
5. 明显不相关的项目名不出现（控制误报）
6. LLM 调用失败不影响主保存流程
7. lint + build 通过

**触达文件：**
- 修改：`src/prisma/schema.prisma`（加三字段）
- 新建：`src/src/lib/projectFuzzyMatchLLM.js`
- 修改：`src/src/app/api/projects/route.js`、`src/src/app/api/projects/[id]/route.js`、`src/src/app/api/projects/batch-rename/route.js`
- 新建：`src/src/app/api/inbox/fuzzy-match/confirm/route.js`、`src/src/app/api/inbox/fuzzy-match/merge/route.js`
- 可能新建：`src/src/app/api/internal/run-fuzzy-match/route.js` 或 `src/scripts/scan-fuzzy-duplicates.mjs`
- 修改：`src/src/app/master/inbox/page.js`（新 tab）
- 修改：`docs/api_reference.md`、`改动说明.md`、`方案实施.md`

**禁区：**
- 不要替换 T-011 的导入前同步判重（保留字面匹配 + 弹窗）
- 不要把 LLM 判重做成同步阻塞（必须异步）
- 不要在 LLM 调用里送敏感数据（只送项目名，不送合同金额等）

**实施记录：**
- 触达文件：
  - `src/prisma/schema.prisma`
  - `src/prisma/migrations/20260423143000_add_project_fuzzy_match_fields/migration.sql`
  - `src/src/lib/projectFuzzyMatchLLM.js`
  - `src/src/lib/projectFuzzyMatchScheduler.js`
  - `src/src/lib/projectMerge.js`
  - `src/src/lib/workLogExceptions.js`
  - `src/src/app/api/projects/route.js`
  - `src/src/app/api/projects/batch-rename/route.js`
  - `src/src/app/api/projects/merge/route.js`
  - `src/src/app/api/inbox/exceptions/route.js`
  - `src/src/app/api/inbox/acknowledge/route.js`
  - `src/src/app/api/inbox/reset-acknowledgement/route.js`
  - `src/src/app/api/inbox/fuzzy-match/confirm/route.js`
  - `src/src/app/api/inbox/fuzzy-match/merge/route.js`
  - `src/src/app/api/internal/run-fuzzy-match/route.js`
  - `src/src/app/master/inbox/page.js`
  - `src/src/app/master/projects/page.js`
  - `src/scripts/verify-project-fuzzy-match.mjs`
  - `docs/api_reference.md`
  - `改动说明.md`
  - `方案实施.md`
- 实施内容：
  1. 给 `Project` 补了三项判重状态字段，并落了项目判重服务：先用现有项目名相似度筛候选，再调用文本模型做最终“需不需要人工复核”的判断；只传项目名、阶段、单体模式和是否关联合同，不传金额等敏感数据。
  2. 项目新建、项目编辑、批量整理、项目合并后，都会异步触发一次 E8 判重；判重失败不会阻塞主保存流程，只会把该项目标成 `error` 供后续重扫。
  3. 收件箱新增 E8“项目疑似重名”数据源和处理动作：可直接确认“不是同一个项目”、跳去项目页改名，或把当前项目并入候选项目；E8 不再走 worklog 的“已知忽略”接口。
  4. 额外补了手动触发扫描接口，最终地址定为 `POST /api/internal/run-fuzzy-match`。原来计划里的 `_internal` 目录在当前 App Router 下不会暴露成路由，实测会 404，所以改成了可访问的 `internal` 路径。
- 验证命令：
  - `cd src && npx prisma generate`
  - `cd src && npx prisma db push`
  - `cd src && npm run lint`
  - `cd src && npm run build`
  - `node src/scripts/verify-project-fuzzy-match.mjs`
- 验证结果：
  - `prisma generate`、`db push`、`lint`、`build` 均已通过。
  - `verify-project-fuzzy-match.mjs` 已用临时 SQLite 库完整跑通：新建相似项目后会进入 E8；确认“不是同一个项目”后会从 E8 消失；改成明显不同的名字后再次扫描保持 `distinct`；再造一条相似项目并执行“并入候选项目”后，来源项目会被删掉，E8 也会清空。
  - 同一轮脚本里还额外确认了 `/master/inbox` 页面可正常返回 `200`。脚本内置了本地假模型服务，不依赖外部模型配置，便于后续重复复验。 （该部分由 Codex 完成）

**反馈（Claude 2026-04-27 复验，未通过）：**

后端逻辑、`prisma generate` / `db push` / `lint` / `build` 全部通过，`node src/scripts/verify-project-fuzzy-match.mjs` 也成功跑完整套 E8 流程（造相似项目 → 进入收件箱 → 确认独立 → 改名后 distinct → 并入候选）。后端这条链路 OK。

但前端文件里多处 UTF-8 字节是直接保存的乱码字符（不是显示问题，文件本身就坏），用户进 E8 tab 会看到错字：

1. `src/src/app/master/inbox/page.js`
   - L461 合并成功消息 `setMessage(\`宸插苟鍏?...)` —— 应为 `已并入 ...`
   - L812 E8 提示语 `鍚庡彴宸叉妸杩欐潯椤圭洰鍒ゆ垚鈥滅枒浼奸噸鍚嶁€濓紝绛変綘浜哄伐纭銆?` —— 应为类似"后台已把这条项目判成'疑似重名'，等你人工确认。"
   - L817 `鍊欓€夛細{candidate.projectName}` —— 应为 `候选：{candidate.projectName}`
   - L819 `' 路 宸蹭笉瀛樺湪'` —— 应为 `' · 已不存在'`
   - L824 `褰撳墠娌℃湁鍙敤鐨勫€欓€夐」鐩€?` —— 应为 `当前没有可用的候选项目`
   - L842 `瑕佹槸涓嶆槸鍚屼竴涓」鐩氨鐐光€滀笉閲嶅悕鈥濓紱瑕佹槸鍏朵腑涓€涓墠鏄纭殑锛屽彲浠ョ洿鎺ュ苟鍏ヨ繃鍘汇€?` —— 应为类似"要是不是同一个项目就点'不重名'；要是其中一个才是正确的，可以直接并入过去。"
   - L854 `杩欑被璁板綍涓嶄細鑷姩鍚堝苟锛屽彧鏄妸鍙枒椤圭洰鎺ㄨ繘鏀朵欢绠辨彁閱掍綘銆?` —— 应为类似"这类记录不会自动合并，只是把可疑项目推进收件箱提醒你。"
2. `src/src/app/api/inbox/fuzzy-match/confirm/route.js` L10 错误消息 `'缂哄皯鏈夋晥鐨勯」鐩?ID'` —— 应为 `'缺少有效的项目 ID'`
3. `src/src/app/api/inbox/fuzzy-match/merge/route.js` L14 同上

成因推测：在 PowerShell 上写文件时 UTF-8 文本被当作 GBK 渲染、然后那批 GBK 字符又被以 UTF-8 重新编码进文件，所以 `file -i` 检测仍是 UTF-8、构建也不会报错，但用户看到的就是上面这些错字。验证脚本只对 `/master/inbox` 检查了 `status === 200`，没有断言 DOM 文本，所以漏掉了。

请 Codex / Antigravity 修这 3 个文件里所有错字（建议把上面列出的中文段落直接复制覆盖，再用 `npm run build` + `node src/scripts/verify-project-fuzzy-match.mjs` 自检；最好顺手在 `verify-project-fuzzy-match.mjs` 里加一条断言：`/master/inbox` 返回的 HTML 必须包含 `候选`/`不重名` 这类正确文本，避免下次再漏）。修完后把状态改回 🟢，由 Claude 复验。

**Codex 2026-04-27 二次修复记录：**

- 修复了 E8 收件箱页和两条 E8 接口里的乱码文案，用户可见文字已恢复为正常中文。
- 补强 `verify-project-fuzzy-match.mjs`：现在会检查收件箱页面包里包含正确的 E8 文案，也会检查 confirm / merge 接口缺参数时返回 `缺少有效的项目 ID`。
- 复验脚本实际重跑时发现另一个隐藏问题：后台旧扫描可能在用户确认或改名后才返回，把新状态重新覆盖成 E8 待处理。已给判重写入加快照保护，过期扫描结果不会再覆盖新状态。
- 复验命令：
  - `cd src && npm run lint`
  - `cd src && npm run build`
  - `cd src && node scripts/verify-project-fuzzy-match.mjs`
- 复验结果：
  - `lint` 通过。
  - `build` 通过。
  - `verify-project-fuzzy-match.mjs` 通过：新建相似项目进 E8、确认独立后消失、改名后不回到 E8、并入候选后清空 E8；同时校验了页面中文文案和两条接口错误提示。 （该部分由 Codex 完成）

---

### ✅ T-024  [P1] E9 产值超限进收件箱 + 修复动作

**状态：** ✅ 已完成（Claude 2026-04-23 复验通过）

**⚠️ 前置条件：T-020 已完成（E9 已在收件箱显示，但没有修复 UI）。**

**背景：**

现有 `productionCap.js` + `productionCalculator.js` 的行为：产值超限时 cap 到合同额 + 标 `[超限]`。T-020 把 exceeded=true 的 worklog 列进收件箱但不提供修复。本任务补完修复动作。

**实施方向：**

收件箱"产值超限"tab 每条支持以下动作：

1. **接受 cap**：调 `POST /api/inbox/acknowledge` 加 `exceeded` 忽略标，从收件箱消失（产值保持 cap 后的值）
2. **修改合同总额**：跳转合同页，让用户手动改 `areaPricingAmount` / `lumpSumAmount`；合同保存后自动 retroactive 重算，超限状态重新评估
3. **移走部分 worklog**：提示"请把部分 worklog 改绑到其他项目或拆分"，跳转工作记录列表筛选到该项目的超限 worklog

**注意：** E9 的"接受"语义和其他异常不同——其他是"我知道缺数据，不再提醒"，E9 是"我知道超限，cap 结果合理"。实施时可用同一套 `acknowledgedExceptions` 字段，但列表显示要提示清楚。

**验收标准：**

1. 造一个合同额 10000、现有 worklog 累计 12000 的场景，相关 worklog 出现在"产值超限"tab
2. 点"接受 cap"后从收件箱消失
3. 合同金额改成 15000 后，超限条从收件箱消失（cap 不再触发）
4. lint + build 通过

**触达文件：**
- 修改：`src/src/app/master/inbox/page.js`
- 修改：`src/src/app/worklog/page.js`
- 可能新建：`src/src/app/api/inbox/jump-to-contract/route.js`（或纯前端跳转）
- 修改：`docs/api_reference.md`、`改动说明.md`、`方案实施.md`

**禁区：**
- 不要改 `productionCap.js` 的 cap 算法
- 不要改 `calculateProductionValue`
- 不要改 exceeded 的检测逻辑（T-020 已在 workLogExceptions.js 里定义好了）

**实施记录：**
- 触达文件：
  - `src/src/app/master/inbox/page.js`
  - `src/src/app/worklog/page.js`
  - `docs/api_reference.md`
  - `改动说明.md`
  - `方案实施.md`
- 实施内容：
  1. 收件箱 E9 tab 补上了三类明确动作：`接受 cap`、`改合同额`、`去总账处理`。其中 `接受 cap` 直接复用现有 `POST /api/inbox/acknowledge`，但按钮文案和页面提示改成了“接受当前截断结果”，避免和普通“忽略异常”混淆。
  2. E9 tab 顶部新增红色说明条，明确告诉用户三种处理路径分别意味着什么；超限记录行里也会补一句提示，强调“接受 cap”会保留当前截断后的产值。
  3. “去总账处理”不再只是跳到 `/worklog` 空页，而是把项目 / 异常类型 / worklog 焦点一起带过去；`worklog/page.js` 现在会读取这些参数，自动把总账筛到对应项目 + `产值超限` 状态，并选中触发来源那条记录。
  4. `docs/api_reference.md` 补了 E9 的语义说明：`exceptionType=exceeded` 时不是忽略错误，而是确认保留 cap 后结果。
- 验证命令：
  - `cd src && npm run lint`
  - `cd src && npm run build`
  - `cd src && powershell -Command ...`（复制 `prisma/dev.db` 到临时库、启动 `next start`、造出“合同额 10000 / 累计 12000”场景，依次验证 E9 出现 → acknowledge 消失 → reset 后恢复 → 合同额改到 15000 后再次消失）
  - `cd src && powershell -Command ...`（额外尝试用 Playwright 打开 `/master/inbox` 点“去总账处理”做页面级验证）
- 验证结果：
  - `lint` / `build` 均通过。
  - 临时库实测通过：造出的超限记录确实进入 E9；点 `接受 cap` 后立即从收件箱消失；取消忽略后会重新出现；把合同金额从 `10000` 调到 `15000` 后，这组记录会自动重算并从 E9 消失。
  - 同一轮 API 验证里还核对了产值数字：超限时第二条记录被截到 `4000`；合同额调大后恢复成 `6000`，`exceeded` 标记同时消失。
  - Playwright 页面级验证再次卡在这台环境的 `spawn EPERM`，所以“跳过去自动筛上”的交互没法在浏览器里实点；但对应前端代码已接好，且 `lint/build` 均通过。 （该部分由 Codex 完成）

---

### ✅ T-015  [P0] 工作记录界面 area / mixed / lumpsum 三种合同都支持手动占比 + 手动产值

**状态：** ✅ 已完成（Claude 2026-04-21 复验通过）

**背景：**

用户 2026-04-20 提出：

> "在工作记录界面，类型为面积计价、混合计价或者包干价的项目都要能手动修改产值的占比或者手动输入本次记录的产值。"

当前实际状况（Claude 2026-04-20 调研结论）：

| 能力 | area | mixed | lumpsum | unit |
|---|---|---|---|---|
| UI 上能看见占比输入框（`supportsAllocationShare`） | ✅ | ❌ 被隐藏 | ✅ | ❌（设计如此） |
| UI 上能切到"手工产值"模式填 `manualTotalValue` | ✅（全开） | ✅（全开） | ✅（全开） | ✅（全开） |
| 后端 PUT 接口接收 `allocationShare / manualTotalValue` | ✅ | ✅ | ✅ | ✅（都没限制） |
| `productionCalculator.js` 真的按 `allocationShare` 算产值 | ✅ | ❌ mixed 走单价逻辑，忽略 allocationShare | ✅ | ❌（走单价） |
| split 响应的 `pendingAllocations` 把 mixed 纳入 | ❌ | ❌ | ✅ | n/a |

**核心落差：**
1. mixed 合同下**占比输入框在 UI 上不可见**（`src/src/app/worklog/page.js:117-120` 的 `supportsAllocationShare` 没把 mixed 加进去）
2. 更本质的问题：mixed 的产值计算逻辑里 **从来没考虑过占比** — `productionCalculator.js:229, 587` 把 mixed 当作 `unit` 处理。所以即使 UI 放开了、用户填了占比，后端也不会按占比算出产值
3. `split` 响应里的 `pendingAllocations` 过滤器（`src/src/app/api/worklog/[id]/split/route.js:36-38`）只给 area/lumpsum 返回，mixed 被吃掉
4. `manualTotalValue` 其实**三种都已经能填**（UI 和后端都无限制），但用户可能因为"占比输入框看不到"而误以为 mixed 下不能干预产值

用户的直觉诉求是："area / mixed / lumpsum 三种都能手动干预产值，具体方式是改占比或者直接填产值数值二选一"。这是合理的，因为实际业务里 mixed 合同往往有"包干条 + 单价条"混在一起，**打包条那部分是需要按占比分给工作记录的**（就和 area/lumpsum 的打包计价一样）。

---

**实施方向：**

#### 第 1 步：UI 打开 mixed 的占比输入框

`src/src/app/worklog/page.js`：

把第 117-120 行 `supportsAllocationShare` 改成：
```js
function supportsAllocationShare(log) {
    const pricingMode = normalizePricingMode(log?.project?.contract?.pricingMode);
    return pricingMode === 'area' || pricingMode === 'mixed' || pricingMode === 'lumpsum';
}
```

其他调用点（第 1365-1379 行的条件渲染）因为是通过这个函数拿 true/false 的，不必再改。

`handleQuickSaveAllocationShare` 里如果有 pricingMode 限制的提示语，也一并放开给 mixed。

#### 第 2 步：产值计算兼容 mixed 的占比覆盖

这是**核心改动**，涉及 `src/src/lib/productionCalculator.js`。实现新规则：

**mixed 合同下单条工作记录的产值优先级（新）：**

1. 如果 `manualTotalValue > 0` → 用 manualTotalValue，走 manual 模式（现有 manual 分支复用）
2. 否则，如果 `allocationShare` 有值（非 null）**并且** 合同定义了 `lumpSumAmount > 0` → 按 `lumpSumAmount × allocationShare` 分摊（类似 lumpsum 分支，但用 `lumpSumAmount` 这个字段；如果 mixed 合同没填 `lumpSumAmount`，就 fallback 到下一条）
3. 否则（allocationShare 为 null 或合同没填打包总额）→ 走**现有的**单价逻辑（`priceItem.unitPrice × quantity`，找不到价目项就进 no-price-match）

等价代码轮廓（具体插入点看现有代码现状，不要硬套行号）：

```js
// 约在第 225-260 行之间，当前：
// if (hasContract && (pricingMode === 'unit' || pricingMode === 'mixed')) { ... unit 逻辑 ... }
// 改为：
if (hasContract && pricingMode === 'mixed') {
    // 1. manualTotalValue 已在更上面的分支处理（保留现状）
    const share = normalizeAllocationShare(log.allocationShare);
    const lumpSumAmount = Number(contract.lumpSumAmount || 0);
    if (share !== null && lumpSumAmount > 0) {
        // 按 mixed 的打包部分分摊
        const totalValue = roundCurrency(lumpSumAmount * share);
        // 写成 area/lumpsum 一致风格的 productionValue 记录
        // calculationMode: 'allocation-share'
        // basis: `lumpSum×${share*100}%`
        return [{ ...常见字段, amount: totalValue, calculationMode: 'allocation-share', basisNote: `按 mixed 打包部分占比 ${(share*100).toFixed(2)}%` }];
    }
    // 否则走单价逻辑（沿用原 mixed 流程）
    return unitPriceCalcFn(...);
}
if (hasContract && pricingMode === 'unit') { ... 原 unit 逻辑 ... }
```

如果现有代码是 `pricingMode === 'unit' || pricingMode === 'mixed'` 合并分支的，要拆开成两个分支。注意：
- mixed 的 `lumpSumAmount` 字段在 schema 里就是 `Contract.lumpSumAmount`（当前主要用于 lumpsum 合同，但 mixed 也允许填）
- 如果 mixed 合同根本没填 `lumpSumAmount`（常见情况），allocationShare 不生效，走单价逻辑（保持现有行为）
- 校验：`roundCurrency / roundQuantity` 一类辅助函数沿用现有实现

**`productionCap.js` 兼容性：** 如果 productionCap 是"合同总额 100% 上限"的检查，mixed 合同的"打包部分上限"现在是 `lumpSumAmount`，所有工作记录的 `allocation-share` 模式产值合计不能超 `lumpSumAmount`。这块要看现有 lumpsum 的上限逻辑怎么写的，mixed 复用同样模式。如果现有 cap 只对 area/lumpsum 做校验，mixed 需要加入同样校验（避免超额）。

#### 第 3 步：split / pendingAllocations 纳入 mixed

文件：`src/src/app/api/worklog/[id]/split/route.js` 第 36-38 行

```js
// 当前：
if (pricingMode !== 'area' && pricingMode !== 'lumpsum') {
    return null;
}
// 改为：
if (pricingMode !== 'area' && pricingMode !== 'mixed' && pricingMode !== 'lumpsum') {
    return null;
}
```

对应 payload 里的 `contractAmount` 取值也要兼容 mixed：
```js
contractAmount: Number(contract.areaPricingAmount || contract.lumpSumAmount || 0),
```
现在是取两者中的非零值，mixed 合同通常会填 `lumpSumAmount`，该逻辑已经能兼容。`contractArea` 只给 area 用，mixed 可以 null。

#### 第 4 步：worklog 导入路径（`POST /api/worklog`）兼容 mixed

检查 `src/src/app/api/worklog/route.js` 里任何按 `pricingMode === 'area' || 'lumpsum'` 做分流的逻辑（例如：import 时把记录标记为"待分占比"），把 mixed 也纳入。

> Grep 提示：在 `api/worklog/route.js` 里搜 `pricingMode`、`area`、`lumpsum`，逐处判断是否需要补 mixed。**不要盲目替换**，每处都要读上下文确认语义。

#### 第 5 步：`manualTotalValue` 保持现有全开

这部分已经能用，不需要代码改动。但验收时要确认 mixed 合同下：
- 编辑工作记录模态框里可以把"产值方式"切到 `manual`
- 填 `manualTotalValue` 保存后，产值落库 = 填入的数字
- 切回 `auto` 模式时 `manualTotalValue` 被清空，产值重新按单价/占比算

---

**验收标准（Claude 会复验这些）：**

1. **代码层面：**
   - `src/src/app/worklog/page.js:117-120` 的 `supportsAllocationShare` 返回 `area || mixed || lumpsum` 均为 true
   - `src/src/lib/productionCalculator.js` 在 mixed 分支里新增"按 `lumpSumAmount × allocationShare` 分摊"的计算路径
   - `src/src/app/api/worklog/[id]/split/route.js` 的 `buildPendingAllocationPayload` 把 mixed 纳入
   - `src/src/app/api/worklog/route.js` 如果有 area/lumpsum 分流也同步扩到 mixed

2. **接口/计算实测：**
   - 造一个 mixed 合同（`pricingMode='mixed'`、`lumpSumAmount=100000`、有几条 priceItem），挂 1 个项目，造 2 条 worklog（q=10、q=5）：
     - Case A：不填 allocationShare → 按 priceItem 单价算产值（现有行为不变）
     - Case B：worklog A 填 `allocationShare=0.3` → 产值 = `100000 × 0.3 = 30000`，`calculationMode='allocation-share'`
     - Case C：worklog A 填 `allocationShare=0.3`、worklog B 填 `allocationShare=0.2` → A=30000、B=20000，合计 50000 ≤ 100000（未超上限）
     - Case D：worklog A 填 `manualTotalValue=12345` → 产值 = 12345，`calculationMode='manual'`
     - Case E：allocationShare 和 manualTotalValue 都填 → manualTotalValue 优先（按上面的优先级）
   - split 一条 mixed worklog：响应 `pendingAllocations` 数组应包含新副本（之前 mixed 被过滤掉，现在不该再被过滤）

3. **页面实测：**
   - 在 mixed 合同挂的项目上，打开 `/worklog`，找一条它的工作记录
   - 应该能看到**占比输入框**（之前 mixed 下是隐藏的）
   - 能填占比保存，保存后产值重算
   - 能点"编辑"打开模态框，切到"手工产值"模式填数字保存

4. **回归（必须不变）：**
   - area 合同下所有原有行为不变（占比、手工产值、split pendingAllocations 都和改动前一致）
   - lumpsum 合同下所有原有行为不变
   - unit 合同下**绝不**能出现占比输入框（`supportsAllocationShare` 只扩到 mixed，不能误扩 unit）
   - 项目 #818 T-001 对照组 2026-01 产值仍为 0
   - 项目 #819（邓川）worklog 数 15，buildingMode=true phase=null
   - 项目 #829（古生种质）worklog 数 18，buildingMode=true phase="古生种质资源二期"
   - T-013 的 split 复制副本行为不变

5. **老的"pending-area-share"队列：**
   - 现有数据库里 `allocationShare=null` 的 area/lumpsum 记录当前有 20 条（T-004 记了），加上 mixed 合同下 `allocationShare=null` 的记录数——验收后在改动说明里给出 mixed 新增待补占比的条数，放进 T-004 里作参考

---

**触达文件（实施者请限制在以下范围）：**

修改：
- `src/src/app/worklog/page.js`（放开 mixed 的占比输入）
- `src/src/lib/productionCalculator.js`（mixed 分支加 allocation-share 计算）
- `src/src/lib/productionCap.js`（如果现有 area/lumpsum 有 cap 校验，mixed 补同样的）
- `src/src/app/api/worklog/[id]/split/route.js`（pendingAllocations 扩到 mixed）
- `src/src/app/api/worklog/route.js`（如有 area/lumpsum 分流则扩到 mixed；**每处读上下文再改**）

文档：
- `docs/api_reference.md`（更新 mixed 的 allocationShare 语义说明）
- `改动说明.md`（顶部追加 [T-015] 记录）
- `方案实施.md`（完工后任务状态 🟡 → 🟢，填实施记录）

新建（可选）：
- `src/scripts/verify-mixed-allocation.mjs`（建议写一个，跑 Case A–E 自动化测试）

**禁区：**
- 不要改 `src/src/lib/worklogBilling.js` 里的 `normalizeAllocationShare / normalizePricingMode` 的签名（可以改实现；签名不能动，影响调用方多）
- 不要改 `src/src/lib/projectResolver.js` / `projectNameMatcher.js`（T-011 禁区）
- 不要改 schema（`Contract.lumpSumAmount` 字段已存在，不用加新字段）
- 不要动 `unit` 合同的产值逻辑（本任务只加 mixed 的 allocation-share 路径，不改 unit）
- 不要改 T-013 的 split 行为（`quantity` 覆盖、副本语义、原记录不动都不能变）
- 不要批量回填老 worklog 的 allocationShare——那是 T-004 的活

---

**实施记录：**
- 触达文件：
  - `src/src/app/worklog/page.js`
  - `src/src/lib/productionCalculator.js`
  - `src/src/lib/productionCap.js`
  - `src/src/app/api/worklog/[id]/split/route.js`
  - `src/src/app/api/worklog/route.js`
  - `src/scripts/verify-mixed-allocation.mjs`
  - `docs/api_reference.md`
  - `改动说明.md`
  - `方案实施.md`
- 实施内容：
  1. 放开了 mixed 合同在工作记录页里的占比输入，并把占比弹窗改成按 `area / mixed / lumpsum` 三种合同切换说明、合同金额标题和字段展示。
  2. 在 `productionCalculator.js` 里拆开了 mixed 与 unit 的计算路径：mixed 现在优先走 `manualTotalValue`，其次走 `lumpSumAmount × allocationShare` 的 `allocation-share` 分摊，只有缺少可用打包总额时才回退到原来的单价逻辑。
  3. 在 `productionCap.js` 里补了 mixed 打包部分的独立上限口径：`allocation-share` 模式只跟同项目下其它 `allocation-share` 记录累计，并且上限只看 `contract.lumpSumAmount`。
  4. `POST /api/worklog` 的导入结果和 `POST /api/worklog/[id]/split` 的复制结果都补进了 mixed 的 `pendingAllocations` 返回；同时新增 `verify-mixed-allocation.mjs`，自动跑 Case A-E 和 split 场景。
- 验证命令：
  - `cd src && node scripts/verify-mixed-allocation.mjs`
  - `cd src && npm run lint`
  - `cd src && npm run build`
  - `cd src && node --input-type=commonjs -`（实查项目 `818 / 819 / 829` 回归和 mixed 待补占比数量）
  - `cd src && node ...playwright...`（临时脚本，已删；真实打开 `/worklog`，实测 mixed 行内占比和“更多→手工产值”两条页面流程）
- 验证结果：
  - `verify-mixed-allocation.mjs` 通过：Case A-E 和 mixed split `pendingAllocations` 全部 PASS，脚本结束后临时合同/项目/工作记录自动清理。
  - 页面实测通过：临时 mixed 工作记录在 `/worklog` 页里能看到占比输入框；实际填 `30%` 后查库为 `allocationShare=0.3`、产值=`24000`、`calculationMode='allocation-share'`；再实际点“更多”切到手工产值填 `4321`，查库为 `manualTotalValue=4321`、产值=`4321`、`calculationMode='manual'`。
  - 回归核对通过：项目 `818` 产值合计仍为 `0`；项目 `819` 仍为 `buildingMode=true, phase=null, workLogs=15`；项目 `829` 仍为 `buildingMode=true, phase="古生种质资源二期", workLogs=18`。
  - 数据核对：当前库里 mixed 合同下 `allocationShare=null` 共 `47` 条，但 `lumpSumAmount>0` 的 mixed 合同记录数为 `0`，因此 `T-004` 总量仍保持 `20` 条。
- 遗留/注意：
  - 这轮没有自动回填历史 mixed 工作记录的占比，也没有替现有 mixed 合同自动补 `lumpSumAmount`；只把“配置了打包总额后，工作记录页 / 导入 / 拆分都能正确处理”的能力补齐。
  - 当前库里的 mixed 合同都还没填 `lumpSumAmount`，所以本轮修复对现有正式数据主要是放开 UI 和兜住后续配置场景，不会主动改老数据。
- （该部分由 codex完成）

---

**Claude 验证反馈（2026-04-20）：**

本轮复验**不通过**。Codex 的功能骨架（UI 放开、mixed 分支、mixed-allocation-share capMode、split pendingAllocations、`verify-mixed-allocation.mjs` 自测）都在，独立脚本也能跑过 Codex 自己那 6 个 Case。但深入独立复验发现两处阻塞缺陷，Codex 自己的 fixture 参数恰好没触发。

---

**🚨 阻塞缺陷 1：`productionCalculator.js:255` priceSource 中文乱码**

```js
priceSource: `娣峰悎璁¤垂鎵撳寘閮ㄥ垎鍗犳瘮 ${formatAllocationSharePercent(allocationShare)}%`,
```

- 这是典型的 UTF-8 字节被当作 GBK 再按 UTF-8 写入的双重编码残渣（源头可能是从别的文件/终端粘贴时丢了编码），按理该字段应当读作 `混合计费打包部分占比`（对照同文件第 193 行 `包干价合同占比`、第 232 行 `面积合同占比` 的写法）
- **已实证会落库**：独立复验造一条 mixed+`allocationShare=0.3` 记录、拉 `productionValue.priceSource` 直接查到的值就是 `娣峰悎璁¤垂鎵撳寘閮ㄥ垎鍗犳瘮 30%`（hex `e5a8a3 e5b3b0 e6828e e79281 c2a4 …`），前端展示和后续导出都会看到乱码
- Codex 自测脚本只校验 `calculationMode` / `workloadShare`，没断言 `priceSource` 文本，所以没发现
- 目前线上 DB 里暂无 mojibake 残留（因现有 mixed 合同都还没配 `lumpSumAmount`，本分支从未被触发），修完后不需要回刷历史

**修复方向：** 直接把第 255 行那段字面量改回正确 UTF-8 的 `混合合同打包占比`（或与 line 193/232 保持动宾结构一致的等价写法）；同时建议在 `verify-mixed-allocation.mjs` 里给 Case B 加一条 `priceSource` 断言，保证下次不会再漏。

---

**🚨 阻塞缺陷 2：`productionCap.js:getContractTotalAmount` 未把 `lumpSumAmount` 算进 mixed 的总额**

```js
// productionCap.js:39-43 — 现状
if (contract.pricingMode === 'mixed') {
    const areaAmount = Number.parseFloat(contract.areaPricingAmount) || 0;
    const total = unitTotal + areaAmount;
    return total > 0 ? total : null;
}
```

- T-015 第一次让 mixed 合同真正启用 `lumpSumAmount`（作为打包部分的总额），但 `getContractTotalAmount` 这边还是旧口径 `unitTotal + areaAmount`，**没把 `lumpSumAmount` 加进去**。
- 结果 mixed 合同的「非 allocation-share 路径」（手工产值、单价回退、cap 计算）全部都是按 `unitTotal + areaAmount` 封顶，忽略了打包部分那块额度。
- **实证复现（独立脚本）：** 造 mixed 合同 `lumpSumAmount=100000`、`priceItems` 合计只有 `5000`。把工作记录 `manualTotalValue=12345` 丢进去，期望存 `12345`；实测 `productionValue.sum=5000`、`exceeded=true`，因为 `applyProductionCap` 里 `contractTotal=5000`、`remaining=5000`、被静默裁成 5000。`calculationMode` 确实是 `manual`、`mode` 确实是 `manual`，但数字被 cap 默默压掉了。
- Codex 自测脚本 `priceItems` 合计 `12300 + 2250 = 14550`，正好比 Case D/E 的 `manualTotalValue=12345` 大，所以 cap 永远不会咬他，脚本过得去。
- 同样问题的副作用：等以后用户配了 mixed+`lumpSumAmount` 并且有若干条 `allocation-share` 记录跑起来后，同项目内任何「非 allocation-share」的工作记录（未填占比的 mixed、切回 unit 的 mixed、手工产值）都会在 `applyProductionCap` 里被「累计已超 5000 合同额」误判成超限归零。用户肉眼只能看到"产值突然被清零 / 被改成了别的数"，又没 UI 提示，排查成本极高。

**修复方向（两条二选一或并用）：**

1. 把 `productionCap.js:getContractTotalAmount` 的 mixed 分支改成
   ```js
   const lumpSum = Number.parseFloat(contract.lumpSumAmount) || 0;
   const total = unitTotal + areaAmount + lumpSum;
   ```
   —— 让"合同总额"反映真实的三项之和；同时确认 `applyProductionCap` 在 capMode 非 `mixed-allocation-share` 时的 cumulative 仍然用 `getCumulativeProjectValue`（含 allocation-share 行），这样合计不会再错位；
2. 在 `applyProductionCap` 的非 `mixed-allocation-share` 分支里，对 mixed 合同特别把 allocation-share 那部分从 cumulative 里剔除（和 mixed-allocation-share 分支形成对偶），避免"打包部分占了 100000、回到单价模式时却按 5000 口径累计"的错位。

**推荐走方案 1**（更直观、也贴合"合同总额"的语义）；方案 2 只是兜底思路。修完后希望在 `verify-mixed-allocation.mjs` 增加一个新 case：`lumpSumAmount=100000, priceItems 合计=5000, manualTotalValue=12345 → 期望产值=12345`，能复现一次就不会再漏。

---

**✅ 本轮验证通过的部分（修复上述两处后可直接保留）：**

1. `worklog/page.js:121-124` `supportsAllocationShare` 正确扩展到 `area / mixed / lumpsum` 三种，mixed 的占比输入框已放开
2. `getAllocationDialogMeta` 按 pricingMode 切换 kicker / note / amountLabel 三块字段，mixed 单独一组文案（"混合计费"/"打包部分总价"），lumpsum 一组，area 一组，展示不再串
3. `productionCalculator.js:243-262` mixed 新分支：`allocationShare` 有值且 `lumpSumAmount>0` 走 `allocation-share` 并传 `capMode='mixed-allocation-share'`；无占比时 fallback 到 `findBestPriceMatch`（单价逻辑），行为符合"手工产值 > 占比 > 单价"优先级
4. `productionCap.js:129-141` `capMode='mixed-allocation-share'` 分支：上限锁定在 `contract.lumpSumAmount`，cumulative 只 SUM 同项目下其他 `allocation-share` 行，不会把 unit / manual 行误算进去（这块切片是对的，问题只出在对偶的「其他分支」未对称处理）
5. `split/route.js:36-37` / `route.js:63-66` mixed 被正式纳入 `buildPendingAllocationPayload`，`contractAmount` 正确读 `contract.lumpSumAmount`，复制副本时 pendingAllocations 能带回 mixed
6. T-013 行为未被 T-015 破坏：原记录 `allocationShare` / `manualTotalValue` / 原产值全保留，只有副本清空字段，`rawText` 仍标 `[manual-copy from #...]`
7. 回归：
   - `verify-mixed-allocation.mjs` 6/6 PASS（Codex 脚本）
   - Claude 独立脚本 23 项断言 20 PASS / 3 FAIL（3 FAIL 都落在上述两处缺陷上）
   - 真实库里 mixed 合同共 4 条、`lumpSumAmount>0` 为 0 条，历史 47 条 mixed+`allocationShare=null` 的产值字段未受影响；样本核对 `unit / mixed / lumpsum` 三模式下生产环境产值串，无乱码残留、无异常归零
   - 项目 818/819/829 对应的真实 workLog ID 早已变更（历史 summary 里的那几号已不存在于当前 `dev.db`），但同口径按当前最新 ID 抽样后各模式未见回归

**结论：功能骨架已接近完成；两个缺陷都属于"正常负载下看不出来、一旦用户按 T-015 初衷配置 mixed+lumpSumAmount 立刻会踩坑"，必须修掉后再接 T-015。**

**Codex 二次修复记录（2026-04-20）：**

- 修复了 mixed 占比分摊写入产值时的 `priceSource` 中文乱码，现已统一写成正常的 `混合计费打包部分占比 xx%`，不会再把乱码落到库里。
- 修复了 `productionCap.js` 里 mixed 合同总额口径遗漏 `lumpSumAmount` 的问题。现在 mixed 合同的总额会按 `单价部分 + 面积部分 + 打包部分` 一起算，手工产值和单价回退不会再被错误压到 `unitTotal + areaAmount` 以内。
- 补强了 `src/scripts/verify-mixed-allocation.mjs`：
  1. Case B 新增 `priceSource` 断言，防止再出现中文乱码。
  2. 新增 Case F，专门验证 `lumpSumAmount=100000`、单价部分合计仅 `5000` 时，`manualTotalValue=12345` 仍保持 `12345`，不再被错误截断。
  3. 脚本自启本地服务时现在会先重新构建，避免误拿旧 build 做验证。
- 复验命令：
  - `cd src && node scripts/verify-mixed-allocation.mjs`
  - `cd src && npm run lint`
  - `cd src && npm run build`
  - `cd src && node --input-type=commonjs -`（实查项目 `818 / 819 / 829` 回归、mixed 待补占比数量、乱码残留数量）
- 复验结果：
  - `verify-mixed-allocation.mjs` 通过：Case A-F 与 split 全部 PASS。
  - `lint` 与 `build` 均通过。
  - 回归核对通过：项目 `818` 产值仍为 `0`；项目 `819` 仍为 `buildingMode=true, phase=null, workLogs=15`；项目 `829` 仍为 `buildingMode=true, phase="古生种质资源二期", workLogs=18`。
  - 数据核对通过：当前库里 mixed 合同下 `allocationShare=null` 共 `47` 条，但 `lumpSumAmount>0` 的 mixed 合同记录数仍为 `0`，因此 `T-004` 总量继续保持 `20` 条；库里 `priceSource` 含 `娣峰悎` 的乱码记录数为 `0`。
- 遗留/注意：
  - 本轮没有改正式业务数据，只修正了 mixed 相关的计算与验证逻辑；当前库里仍没有已配置 `lumpSumAmount` 的正式 mixed 合同，所以不会触发历史数据回刷。
- （该部分由 codex完成）

---

### ✅ T-014  [P1] 合同管理界面点工程名弹出合同详情弹窗（支持编辑）

**状态：** ✅ 已完成（Claude 2026-04-21 复验通过）

**背景：**

用户 2026-04-20 提出：

> "合同管理界面可以点击工程名称，打开前面项目管理加的那个弹出窗口，能够看见具体的检测项目和详情，并且能修改。"

**当前现状（Claude 2026-04-20 调研）：**

- 合同管理界面 `src/src/app/contracts/page.js` 合同列表第 641-655 行，有「工程名称」列（从 `notes` 里 `extractProjectNameFromNotes` 提取，纯文本）和「关联项目」列（`(c.projects || []).map(...).join('、')`，也是纯文本）。两列都没有交互
- 用户说"前面项目管理加的那个弹出窗口"指的是 **`src/src/app/master/projects/page.js:868-922`** 里那个合同详情弹窗（触发源是项目列表行里点合同徽章 `setViewingContract(project.contract)`）。弹窗里能看到：
  - 合同基本信息（合同号、委托方、受托方、签订日期、定价方式 + 金额）
  - 合同价目表 `priceItems`（序号 / 检测项目 / 数量 / 单位 / 单价 / 小计）
- 但该弹窗目前是**硬编码 inline JSX + 只读**，没提取成独立组件

用户要的是：
1. 合同页的「工程名称」列改成可点击，点击后弹出这个合同详情弹窗
2. 弹窗里要能**修改**合同内容（至少检测项目清单可改；合同基本信息有个"去编辑"按钮跳到现有编辑面板亦可）

---

**实施方向：**

#### 第 1 步：把现有 inline 弹窗提取成独立组件

新建 `src/src/components/ContractDetailModal.jsx`（或 `.js`）：

```jsx
'use client';

export default function ContractDetailModal({
    contract,
    onClose,
    onEdit,           // 点"编辑合同"按钮时触发（回调给父组件打开编辑面板）
    onSavePriceItems, // 点"保存检测项目修改"时触发（回调给父组件发 PUT 请求）
    editable = false, // 父组件控制是否允许编辑 priceItems（合同页 true，项目页先给 false 保持现有只读）
}) {
    // 1. 基本信息展示块（合同号、委托方、受托方、签订日期、定价方式 + 金额）
    //    — 完全照搬 master/projects/page.js:873-878 的展示风格
    //    — editable=true 时旁边加一个"编辑合同"按钮，点了调 onEdit?.()
    // 2. priceItems 清单表
    //    — editable=false：保持现有只读表格（master/projects/page.js:880-915）
    //    — editable=true：每行 testItemName / quantity / unit / unitPrice 改成可编辑 input，加"删除此行"按钮；表尾加"+ 新增一行"按钮；有本地 draftItems 状态；点"保存修改"调 onSavePriceItems?.(draftItems)
    // 3. 关闭按钮
    //    — editable=true 且有未保存修改时，点关闭要二次确认（prompt "有未保存修改，确定放弃？"）
}
```

**交接实现细节（Claude 已查过，实施者参考即可）：**

- 现有 inline 弹窗的 CSS class：`card stack` + inline styles（position: fixed, inset: 0, zIndex: 1000 等），直接照搬到新组件
- `ContractLabel` 辅助组件在 `master/projects/page.js:29-33`，可以一起抽出到 `src/src/components/ContractLabel.jsx` 共享
- 价目表表格的 class：`data-table-shell` + `data-table`，沿用

#### 第 2 步：`contracts/page.js` 改造

1. 引入新组件：`import ContractDetailModal from '@/components/ContractDetailModal';`
2. 新增状态：`const [viewingContract, setViewingContract] = useState(null);`
3. 合同列表第 641 列「工程名称」改成按钮/链接：
   ```jsx
   <td>
       <button
           type="button"
           onClick={() => setViewingContract(c)}
           style={{ background: 'none', border: 'none', padding: 0, color: 'var(--color-primary)', cursor: 'pointer', textAlign: 'left', textDecoration: 'underline dotted' }}
           title="点击查看合同详情"
       >
           {extractProjectNameFromNotes(c.notes) || '-'}
       </button>
   </td>
   ```
   （如果 `notes` 为空，工程名列为 `-`，此时按钮文字就是 `-`，允许点击也可以，点了弹窗仍然能展示合同基本信息 + priceItems）
4. 第 642 列「关联项目」里的每个项目名也一样改成可点击的，点了打开**同一个合同详情弹窗**（因为项目挂在这个合同下，弹窗内容 = 合同信息）：
   ```jsx
   <td>
       {(c.projects || []).length === 0 ? '-' : (c.projects || []).map((p, idx) => (
           <React.Fragment key={p.id}>
               {idx > 0 && '、'}
               <button
                   type="button"
                   onClick={() => setViewingContract(c)}
                   style={{ background: 'none', border: 'none', padding: 0, color: 'var(--color-primary)', cursor: 'pointer', textDecoration: 'underline dotted' }}
               >
                   {getProjectOptionLabel(p)}
               </button>
           </React.Fragment>
       ))}
   </td>
   ```
5. 在页面底部挂弹窗：
   ```jsx
   {viewingContract && (
       <ContractDetailModal
           contract={viewingContract}
           editable={true}
           onClose={() => setViewingContract(null)}
           onEdit={() => {
               handleEdit(viewingContract);  // 复用现有编辑流程，跳到顶部编辑面板
               setViewingContract(null);
           }}
           onSavePriceItems={async (draftItems) => {
               // 调 PUT /api/contracts/[id] 更新 priceItems
               const response = await fetch(`/api/contracts/${viewingContract.id}`, {
                   method: 'PUT',
                   headers: { 'Content-Type': 'application/json' },
                   body: JSON.stringify({ priceItems: draftItems }),
               });
               if (!response.ok) { /* 报错 */ }
               await refreshContracts();
               setViewingContract(null);
           }}
       />
   )}
   ```

#### 第 3 步：`master/projects/page.js` 同步替换 inline 弹窗

1. 引入新组件（替换第 868-922 行的 inline JSX）：
   ```jsx
   {viewingContract && (
       <ContractDetailModal
           contract={viewingContract}
           editable={false}
           onClose={() => setViewingContract(null)}
       />
   )}
   ```
   这里 `editable=false` 保持原有纯只读行为（项目管理界面不需要从这里改合同）。
2. 删除原本的 inline 弹窗 JSX，保留 `setViewingContract` 状态和触发点击事件。

#### 第 4 步：验证 `PUT /api/contracts/[id]` 支持 priceItems 覆盖

读 `src/src/app/api/contracts/[id]/route.js`（或类似路径）的 PUT handler，确认：
- 能接收 `priceItems: [{ testItemName, quantity, unit, unitPrice }, ...]`
- 语义是"整表覆盖"（把现有 priceItems 全删、按 body 重建），还是"差分更新"
- 如果现有接口不支持整表覆盖，要么扩它（加个 mode 区分）、要么本任务改成先 DELETE 再 POST 的策略，但优先改 PUT handler 让它支持整表覆盖（并且向后兼容：body 不传 priceItems 就不动）

---

**验收标准（Claude 会复验这些）：**

1. **代码层面：**
   - 新文件 `src/src/components/ContractDetailModal.jsx` 存在
   - `src/src/app/contracts/page.js` 「工程名称」列和「关联项目」列的项目名都变成可点击按钮
   - `src/src/app/master/projects/page.js` 原 868-922 行的 inline 弹窗 JSX 被替换成 `<ContractDetailModal />` 组件引用（代码更短）
   - `PUT /api/contracts/[id]` 能接受并正确处理 `priceItems` 字段（或本任务扩了它）

2. **页面实测：**
   - 在 `/contracts` 合同列表页点任一合同的「工程名称」→ 弹出合同详情弹窗，内容齐全（基本信息 + 价目表）
   - 点「关联项目」列里的项目名 → 弹出同一个弹窗（不是两种不同的弹窗）
   - 在弹窗里修改一行 priceItem 的单价/数量 → 点「保存修改」→ 弹窗关闭，列表刷新，新单价正确
   - 在弹窗里删掉一行 priceItem → 保存 → 对应 priceItem 在库里被删除
   - 在弹窗里新增一行 priceItem → 保存 → 对应 priceItem 新建
   - 弹窗里点「编辑合同」按钮 → 弹窗关闭，页面顶部编辑面板预填当前合同的数据（复用现有 `handleEdit` 流程）
   - 在 `/master/projects` 项目列表点合同徽章 → 仍能打开弹窗（行为不变，只是 JSX 换成新组件）

3. **回归：**
   - `/master/projects/[id]` 项目详情页不受影响
   - 工作记录页产值不受影响（本任务不动产值逻辑）
   - T-013 拆分行为不受影响
   - 818/819/829 数据不变

---

**触达文件（实施者请限制在以下范围）：**

新建：
- `src/src/components/ContractDetailModal.jsx`
- （可选）`src/src/components/ContractLabel.jsx`（把 `master/projects/page.js:29-33` 的 `ContractLabel` 抽出来共享，如果不抽就在 ContractDetailModal 里 inline 放一份）

修改：
- `src/src/app/contracts/page.js`
- `src/src/app/master/projects/page.js`（替换 inline 弹窗）
- `src/src/app/api/contracts/[id]/route.js`（如果 PUT 不支持 priceItems 整表覆盖，扩它；读了确认已经支持就不必改）

文档：
- `docs/api_reference.md`（如果扩了 PUT 接口行为，记一笔）
- `改动说明.md`（顶部追加 [T-014] 记录）
- `方案实施.md`（完工后任务状态 🟡 → 🟢，填实施记录）

**禁区：**
- 不要改 `POST /api/contracts`（合同新建路径与本任务无关）
- 不要改项目合并 / 项目批量整理（T-009/T-010/T-012 禁区）
- 不要动 `src/src/lib/projectResolver.js` / `projectNameMatcher.js`（T-011 禁区）
- 不要改 schema（`Contract`、`PriceItem` 现有字段已够用）
- 不要在 `PUT /api/contracts/[id]` 里偷偷改别的字段处理方式；只能**扩**它支持 priceItems 覆盖（如果现在还不支持），不能改已有字段的语义
- 不要在新组件里直接调 fetch（fetch 调用应该在父页面里、通过 onSavePriceItems 回调触发，方便父页面统一管错）

---

**实施记录：**
- 触达文件：
  - `src/src/components/ContractDetailModal.jsx`
  - `src/src/app/contracts/page.js`
  - `src/src/app/master/projects/page.js`
  - `改动说明.md`
  - `方案实施.md`
- 实施内容：
  1. 新增共用 `ContractDetailModal`，把合同基本信息和价目表整合进一个可复用弹窗；`/master/projects` 里的旧 inline 弹窗已替换成这个共用组件，保持只读查看行为不变。
  2. `/contracts` 合同列表里，“工程名称”和“关联项目”两列都改成可点击入口，打开同一个合同详情弹窗；弹窗里可直接新增 / 删除 / 修改 `priceItems`，保存时复用现有 `PUT /api/contracts` 做整表覆盖。
  3. 合同页弹窗补上“编辑合同”入口，点击后会关闭弹窗，并把当前合同数据带回顶部编辑表单继续改；本轮同时确认现有 `PUT /api/contracts` 已支持 `priceItems` 整表覆盖，所以没有改后端接口，也没有改 `docs/api_reference.md`。
- 验证命令：
  - `cd src && cmd /c npm run lint`
  - `cd src && cmd /c npm run build`
  - 启动本地 dev server 后，`cd src && node --input-type=module -`（创建临时合同 / 项目，用 Playwright 实测 `/contracts` 与 `/master/projects`，结束后自动清理）
- 验证结果：
  - `lint` / `build` 全部通过。
  - 真实页面实测通过：`/contracts` 里“工程名称”和“关联项目”两个入口都能打开同一个详情弹窗；在弹窗里把第一行单价从 `100` 改到 `250` 后保存成功，接口返回“合同价目表已更新，并重算了 1 个关联项目”，再查接口确认价目表已实际更新。
  - 再从“关联项目”入口打开弹窗，点击“编辑合同”后，顶部编辑表单成功带出当前合同编号；`/master/projects` 里同一合同 badge 仍能打开详情弹窗，且保持只读，没有出现“保存修改”按钮。
  - 回归基线未变：项目 `818` 的 `2026-01` 产值合计仍为 `0`；项目 `819` 仍为 `buildingMode=true, phase=null, workLogs=15`；项目 `829` 仍为 `buildingMode=true, phase="古生种质资源二期", workLogs=18`。
- 遗留/注意：
  - 本轮没有修改 `PUT /api/contracts`，因为现有实现已经支持 `priceItems` 整表覆盖；`docs/api_reference.md` 因此未改。
  - 页面验证使用的是临时合同 `T014-E2E-*` 和临时项目，验证脚本结束后已自动删除，没有留下测试脏数据。
- （该部分由 Codex 完成）

---

### ✅ T-013  [P1] 拆分工作记录改为"复制一份保留原记录 + 自由编辑副本"

**状态：** ✅ 已完成（Claude 2026-04-20 复验通过）

**背景：**

用户 2026-04-20 提出：

> "改一下拆分工作记录的逻辑，如果有一条工作记录涉及拆分的，那么直接拆分成两条一模一样的保留原始记录的，中间的内容可以自己修改就可以了。"

当前 `POST /api/worklog/[id]/split`（`src/src/app/api/worklog/[id]/split/route.js`）的逻辑是：
1. 必须填 `splitQuantity`，且必须满足 `0 < splitQuantity < originalQuantity`
2. 原记录的 `quantity` 被减为 `originalQuantity - splitQuantity`
3. 新记录的 `quantity = splitQuantity`
4. 原记录和新记录都清空 `allocationShare / manualTotalValue`
5. 原记录的 `productionValues` 被清掉，然后靠 `rebuildProjectProduction` 重算

用户的新诉求是：**不要做减法**。拆分按钮按下去以后系统直接**复制一条跟原记录一模一样的新记录**，原记录保持原样（数量、占比、手工产值、productionValues 都不动），用户可以在拆分对话框里对新记录的任意字段（数量、日期、项目、检测内容、人员、备注、单位等）自由修改，保存后得到两条独立的记录。

这样带来的好处：
- 用户不必事先计算"要从原记录里拆多少"；原记录本身就是一份真实台账，不应该被拆分动作修改
- 新记录就是一份"起草副本"，用户在副本上按需改就行
- 彻底消除"拆出数量 ≥ 原数量就报错"这种别扭的边界约束

---

**实施方向：**

#### 第 1 步：后端 `POST /api/worklog/[id]/split` 改为"复制一份" 语义

文件：`src/src/app/api/worklog/[id]/split/route.js`

改动要点：

1. **请求体字段**改为：
   ```jsonc
   {
     "quantity": 12.5,             // 可选，新记录的数量；不传则 = 原记录 quantity
     "workDate": "2026-04-20",     // 可选，不传则 = 原记录 workDate
     "projectName": "...",         // 可选，不传则 = 原记录当前展示名（带 phase/building 的形态）
     "testContent": "...",         // 可选，不传则 = 原记录 testContent
     "unit": "...",                // 可选，不传则 = 原记录 unit
     "remarks": "...",             // 可选，不传则 = 原记录 remarks
     "staffNames": ["张三", "李四"] // 可选，不传则 = 原记录当前的 staff 名单
   }
   ```
   - **向后兼容：** 如果客户端还在传老字段 `splitQuantity`（旧前端一段时间内还可能传），把它当作 `quantity` 读；但 **不再校验** `splitQuantity < originalQuantity`——这条约束彻底删除
   - 允许 `quantity === originalQuantity`（完全相同的副本，用户后续可以改）
   - 允许 `quantity === 0`（用户可以先建个"空壳"副本再填）——但 `quantity < 0` 仍然要拒绝

2. **原记录彻底不动**：
   - 删除现有代码里 `tx.workLog.update({ where: { id: worklogId }, data: { quantity: remainingQuantity, allocationShare: null, manualTotalValue: null, manualValueNote: null } })` 这整段
   - 删除 `tx.productionValue.deleteMany({ where: { workLogId: worklogId } })`
   - 原记录的 `quantity / allocationShare / manualTotalValue / manualValueNote / productionValues` 全部保持原样
   - `rebuildProjectProduction` 对原项目仍然要跑一次（因为新增了一条 worklog，整体产值需要重算），但 **原记录本身** 不会再因为这次操作变形

3. **新记录构造**规则（完全复制 + 可选覆盖）：
   - `workDate`：body 传了就用 body；没传 = 原记录 workDate
   - `projectId / buildingName`：仍走 `findOrCreateProjectByDisplayName(nextProjectName, existingLog.projectId, prisma)`，但 `nextProjectName` 默认值改为 `buildWorkLogProjectDisplayName(existingLog)`（保证不传 projectName 时得到和原记录完全一致的项目 + 单体建筑）
   - `testContent / unit / remarks`：body 没传则沿用原记录
   - `quantity`：body 没传则 = 原记录 quantity；body 传了则用 body（仅校验 `>= 0` 且有限数）
   - `rawText`：保留原记录 rawText，并追加 `\n[manual-copy from #<原 id>]` 标签（把旧的 `manual-split` 标签字符串改成 `manual-copy` 以反映新语义；老记录里已有 `manual-split` 标签的保持不变，不批量 backfill）
   - `allocationShare / manualTotalValue / manualValueNote`：副本一律写 null，让 `buildPendingAllocationPayload` 把它拉进待确认占比队列（和现有语义一致）

4. **人员关联**：`nextStaffIds` 逻辑保留（没传 staffNames 就从原记录的 `staffMembers` 里取 `.staff.name`），`WorkLogStaff` 用 `createMany` 插新记录即可

5. **产值计算**：
   - 副本走现有的 `calculateProductionValue` / `rebuildProjectProduction` / `recalculateStandaloneWorklog` 同一套
   - 原记录因为数据没动，**不需要** 单独再算——但 `rebuildProjectProduction(originalProjectId)` 会把整个项目的所有 worklog 一起重算，所以原记录的产值也会自然重算一次，数据应该和改动前一致

6. **detectionRecord 同步**：
   - `syncDetectionRecordFromWorkLog(worklogId)`：仍然调用，因为项目可能因为 `findOrCreateProjectByDisplayName` 的 side effect 牵动（即便没动，幂等调用也安全）
   - `syncDetectionRecordFromWorkLog(result.createdLogId)`：保留

7. **返回值**：保持现有结构 `{ success, originalLog, splitLog, pendingAllocations }`
   - `originalLog` 里的 `quantity / allocationShare / manualTotalValue` 应该和 POST 前 **完全相同**
   - `splitLog` 是新复制的副本

#### 第 2 步：前端拆分弹窗改成"复制"语义

文件：`src/src/app/worklog/page.js`

改动要点（涉及的代码区段已经用 Grep 找到，行号在下面的注释里标出，实施时按该文件实际内容定位）：

1. **`buildSplitState`（约 101-115 行）**：
   - `splitQuantity: ''` 改成 `splitQuantity: String(log.quantity ?? '')` —— 默认显示原数量，让用户看到"一份完整副本"
   - 其它字段保持原样（workDate、projectName、testContent、unit、remarks、staffNames 都已经是复制原记录的）

2. **`handleSubmitSplit`（约 808-848 行）**：
   - 删掉 `splitQuantity >= totalQuantity` 的前端校验
   - 允许 `splitQuantity === totalQuantity` 和 `splitQuantity === 0`
   - 仅校验 `Number.isFinite(splitQuantity) && splitQuantity >= 0`
   - 请求体 body 字段从 `splitQuantity` 改名为 `quantity`（或两个都传兼容；考虑到前后端同时改，直接传 `quantity` 即可，老字段不写）

3. **拆分弹窗 UI（约 1604-1712 行）**：
   - 顶部 `modal-note` 从 `"系统会保留原记录，把你填写的数量拆成一条新记录。拆分后如果原来有占比或手工产值，需要重新确认。"` 改成：`"系统会保留原记录原样不动，复制一份新的记录供你修改。新记录默认和原记录一模一样，下面所有字段都可以按需改。新记录的占比/手工产值需要重新确认。"`
   - **移除** `split-grid` 里的"拆分后原记录剩余"那块（`surface-item` 第二个），因为原记录已经不会变了。可以把第一块"原数量"也一起删掉（对用户意义不大），或者改成显示原记录当前数量作为参考
   - "拆出数量" label 改为 **"新记录数量"**（`label htmlFor="split-quantity"`）
   - `step="0.01"` / `type="number"` 保留；`min="0"` 可以加上
   - 主按钮文字 `"确认拆分"` 可以保留（弹窗语义在用户那边习惯叫"拆分"），但把 loading 态 `"拆分中..."` 可以同步保留——不强改 label

4. **modal-title** `"拆分工作记录"`：保留。用户还是把这个动作叫"拆分"，只是底层实现变成了"复制"

#### 第 3 步：文档同步

- `docs/api_reference.md`：如果里面有 `POST /api/worklog/[id]/split` 条目，更新请求体字段说明（`splitQuantity` → `quantity` 可选，不传就复制原量；并说明原记录不再被修改）。如果没有这条 API 的文档段，就不必新增，但要在本任务的"实施记录"里点明"docs 里本来就没有这条 API 的描述，故未更新"
- `改动说明.md`：顶部追加 `[T-013]` 变更记录（模板照旧）

---

**验收标准（Claude 会复验这些）：**

1. **代码层面：**
   - `src/src/app/api/worklog/[id]/split/route.js` 里：
     - 不再有 `splitQuantity >= originalQuantity` 的硬校验
     - 不再有对原 workLog 的 `update { quantity: remainingQuantity, allocationShare: null, ... }`
     - 不再有 `productionValue.deleteMany({ where: { workLogId: worklogId } })`
   - `src/src/app/worklog/page.js` 里：
     - `buildSplitState` 返回的 `splitQuantity` 默认值 = 原记录 `quantity`
     - `handleSubmitSplit` 不再拒绝 `splitQuantity >= totalQuantity`
     - 弹窗里"拆分后原记录剩余"区块消失

2. **接口实测：**
   - 对任一已有 worklog 调用 `POST /api/worklog/<id>/split`：
     - 不带 body（或只传 `{}`）：原记录 `quantity/allocationShare/manualTotalValue` 完全不变；新增一条 worklog，所有字段和原记录一致（除了 id、rawText 多了 `[manual-copy from #...]` 尾巴、allocationShare=null、manualTotalValue=null）
     - body `{ quantity: 0 }`：成功，新记录数量为 0，原记录不变
     - body `{ quantity: 999 }`（远大于原数量）：成功，新记录 999，原记录不变（**这是新行为，是验收的关键点**）
     - body `{ quantity: -1 }`：返回 400
   - 响应里 `originalLog` 的 `quantity` === 拆分前的 `quantity`，`splitLog.quantity` === body.quantity（或默认值）

3. **前端实测：**
   - 打开 `/worklog`，找一条 `quantity=10` 的记录，点"拆分"
   - 弹窗里"新记录数量"输入框默认显示 `10`
   - 不改任何字段直接点"确认拆分"：原记录仍是 10，表格里新增一条一模一样的 10
   - 再拆一次，把"新记录数量"改成 `20`：原记录仍是 10，新记录是 20
   - 再拆一次，"新记录数量"填 `0`：原记录仍是 10，新记录是 0（能建出来）

4. **回归（必须跑一遍、不能动的数据）：**
   - 项目 #818（清华园）：T-001 对照组 2026-01 月产值合计 **仍为 0 元**
   - 项目 #819（邓川）：`buildingMode=true, phase=null`，仍 15 条 worklog（数量不能因为本改动涨 1 条）
   - 项目 #829（古生种质）：`buildingMode=true, phase="古生种质资源二期"`，仍 18 条 worklog，1#/2#保育室 各 9 条
   - 实测前后对比：**在不做任何拆分操作的情况下**，以上三条数据应该完全一致
   - 做完拆分验收的实测记录后，把造出来的副本 worklog 通过 DELETE API 或手动清理掉（验证脚本末尾带清理逻辑）

5. **pendingAllocations：**
   - 如果原记录的 project 挂在 area/lumpsum 合同下：
     - 拆分后原记录仍然是原样——如果它原本就有 allocationShare，响应里的 `originalLog` 条目不会再被拉进 pendingAllocations（因为它本来就有占比）
     - 副本的 allocationShare=null，会被拉进 pendingAllocations 等用户确认
     - **这个行为和改动前的唯一区别是：原记录不再被清空 allocationShare，所以不会再被拉进 pendingAllocations**

---

**触达文件（实施者请限制在以下范围）：**

修改：
- `src/src/app/api/worklog/[id]/split/route.js`
- `src/src/app/worklog/page.js`

文档：
- `docs/api_reference.md`（如有 split API 条目则更新，否则在"实施记录"里说明）
- `改动说明.md`（顶部追加 [T-013] 记录）
- `方案实施.md`（把本任务状态从 🟡 改成 🟢，填"实施记录"段）

新建（可选，不强制）：
- `src/scripts/verify-worklog-split-copy.mjs`（如果想跑自动化实测，可以写一个脚本；不是硬性要求）

**禁区：**
- 不要改 `src/src/lib/productionCalculator.js` / `productionCap.js` / `worklogBilling.js` 的产值计算逻辑——本任务不涉及算法改动
- 不要改 `src/src/lib/projectResolver.js` / `projectNameMatcher.js`（T-011 刚验收通过）
- 不要改 `POST /api/worklog` 导入相关的任何代码（T-011 刚验收通过，不要污染）
- 不要改其它 worklog 相关接口（`POST /api/worklog`、`PUT /api/worklog/[id]`、`DELETE /api/worklog/[id]` 等都不动）
- 不要改 schema
- 不要批量修复老的 `[manual-split from #...]` 标签——只对新拆分操作用 `[manual-copy from #...]` 即可
- 不要改 `改动说明.md` 里已有的任何历史记录

---

**实施记录：** _（实施者填写）_
- 触达文件：
  - `src/src/app/api/worklog/[id]/split/route.js`
  - `src/src/app/worklog/page.js`
  - `docs/api_reference.md`
  - `改动说明.md`
  - `方案实施.md`
- 实施内容：
  1. 把 `POST /api/worklog/[id]/split` 从“拆出一部分数量”改成“复制一份副本”。现在默认直接复制原记录；请求体主字段改为 `quantity`，旧字段 `splitQuantity` 继续兼容，但不再要求小于原数量。
  2. 删除了原记录上的联动修改：不再改原记录 `quantity`，不再清原记录 `allocationShare / manualTotalValue / manualValueNote`，也不再单独删原记录的 `productionValues`。新副本继续清空占比和手工产值，并把 `rawText` 标签改成 `[manual-copy from #...]`。
  3. 调整拆分弹窗默认值和文案：数量默认显示原数量；“拆出数量”改成“新记录数量”；去掉“拆分后原记录剩余”区块，明确告诉用户“原记录原样不动，只复制一份新的给你改”。
  4. 顺手把前端原本“数量 <= 0 不让点拆分”的限制也去掉了，和后端新语义保持一致。
- 验证命令：
  - `cd src && npm run lint`
  - `cd src && npm run build`
  - 启动 `next start --hostname 127.0.0.1 --port 3100`
  - `cd src && node --input-type=module -`（真实接口验证默认复制、`quantity=0`、`quantity=999`、`quantity=-1`、以及 area/lumpsum 项目 pendingAllocations 行为；结束后清理临时记录与临时项目）
  - `cd src && node --input-type=module -`（用 Playwright 真实打开 `/worklog`，点击拆分弹窗并实测默认复制、改成 20、改成 0；结束后清理临时记录与临时项目）
- 验证结果：
  - 接口实测通过：
    - 默认 `{}` 复制后，原记录不变，副本数量等于原数量
    - `{ quantity: 0 }` 成功，副本数量为 `0`
    - `{ quantity: 999 }` 成功，副本数量为 `999`
    - `{ quantity: -1 }` 返回 `400`
    - 在邓川包干项目上补了一个带 `allocationShare=25` 的临时记录再复制，响应里的 `pendingAllocations` 只包含副本，不包含原记录
  - 页面实测通过：
    - 拆分弹窗默认数量显示 `10`
    - 第一次不改直接确认后，临时记录从 `1` 条变成 `2` 条，数量 `[10, 10]`
    - 第二次改成 `20` 后，数量变成 `[10, 10, 20]`
    - 第三次改成 `0` 后，数量变成 `[0, 10, 10, 20]`
  - 回归核对通过：项目 `818` 的 `2026-01` 产值仍为 `0`；项目 `819` 仍为 `buildingMode=true, phase=null, workLogs=15`；项目 `829` 仍为 `buildingMode=true, phase="古生种质资源二期", workLogs=18`
- 遗留/注意：
  - 本轮没有新增验证脚本文件，接口和页面实测是用两段临时 `node --input-type=module -` 命令完成的
  - 所有临时项目、临时工作记录和临时验证日志都已清理，当前 `T013*` 残留为 `0`
- （该部分由 codex完成）

**验证反馈：**
- 2026-04-20 Claude 复验：
  - **代码检查：**
    - `src/src/app/api/worklog/[id]/split/route.js` 整段 `splitQuantity >= originalQuantity` 硬校验已删；不再对原 workLog 做 `update { quantity: remainingQuantity, allocationShare: null, ... }`；不再 `productionValue.deleteMany({ where: { workLogId: worklogId } })`；副本 `rawText` 标签改成 `[manual-copy from #...]`；body `.catch(() => ({}))` 支持空 body；`quantity` 为主、`splitQuantity` 兼容 ✅
    - `src/src/app/worklog/page.js`：`buildSplitState` 里 `splitQuantity` 默认值改为 `String(log.quantity ?? '')`；`handleSubmitSplit` 不再拒绝 `>= totalQuantity`，只拒绝 `< 0`；`handleOpenSplit` 不再阻止 `quantity<=0` 的记录进入拆分；弹窗 label 改为"新记录数量"，modal-note 换成"原记录原样不动，复制一份新的给你改"，"拆分后原记录剩余"区块已去掉 ✅
  - **接口实测（Claude 临时脚本 `claude-t013-verify.mjs`，跑完已删）：**
    1. 创建临时合同 + 项目 + worklog（quantity=10, unit='组', testContent='T013 测试内容'）
    2. **Case1** 空 body 复制 → 200；原记录 `quantity=10` 不变；副本 `quantity=10, allocationShare=null, manualTotalValue=null, rawText='[manual-copy from #<id>]'` ✅
    3. **Case2** `{ quantity: 0 }` → 200；原记录仍 10；副本 `quantity=0` ✅
    4. **Case3** `{ quantity: 999 }`（**远大于原数量，新行为的关键点**）→ 200；原记录仍 10；副本 `quantity=999` ✅
    5. **Case4** `{ quantity: -1 }` → 400 `"新记录数量必须大于或等于 0"`；原记录仍 10 ✅
    6. **Case5** 旧字段 `{ splitQuantity: 5 }` 向后兼容 → 200，副本 `quantity=5`，原记录仍 10 ✅
    7. **Case6** 旧字段 `{ splitQuantity: 50 }`（旧逻辑会 400 拒绝）→ 200，副本 `quantity=50`，旧约束彻底解除 ✅
    8. **Case7** 覆盖 `{ quantity: 7, testContent, remarks, unit }` → 副本这 4 个字段是新值；**原记录 `testContent='T013 测试内容'`、`unit='组'` 完全未被触动** ✅
    9. **Case8** 所有拆分跑完后，原记录 `quantity=10, allocationShare=null, manualTotalValue=null, rawText=null` 全部不变 ✅
  - **数据回归（同一脚本基线 + 事后对比）：**
    - 项目 `#818` worklog 数 18 未变，2026-01 productionValues 合计仍为 `0` ✅
    - 项目 `#819` worklog 数仍 `15`，`buildingMode=true, phase=null` ✅
    - 项目 `#829` worklog 数仍 `18`，`buildingMode=true, phase="古生种质资源二期"` ✅
  - **清理：** 6 条副本 worklog + 临时 worklog + 临时 project + 临时 contract 全部删除；DB `T013*` 残留 = 0，`manual-copy` 残留 = 0 ✅
  - 共 **36 PASS / 0 FAIL**
- 验证通过。

---

### ✅ T-012  [P0] 合同项目组整理扩展：支持"合并 / 子项 / 单体"三种完整层级操作

**状态：** ✅ 已完成（Claude 2026-04-16 复验通过）

**背景：**

T-009 已经做了"合同项目组"整理 UI（项目详情页 `/master/projects/[id]` 里的区块），目前只支持：
- 统一大项目名（所有兄弟项目共用一个 parentName）
- 给每个兄弟项目填 phase（变成"不同子项"）

但实际业务里，兄弟项目之间的关系不止"不同子项"一种。**用户在合同 #9 上遇到了**：

| 项目 ID | 当前名称 | 当前 phase | worklog |
|---|---|---|---|
| #811 | 大理州龙泉全民健身中心 | null | 4 |
| #836 | 大理州龙泉全民健身中心建设项目第三方工程质量检测 | null | 1 |

用户反馈：
> "使用统一修改之后，工程名称统一了，但现在这两个识别成了两个子项，但其实是同一个项目，我需要他们合并。在修改的时候给我完整的选项，询问我到底是子项还是同一个项目，还是一个项目下的不同单体，我需要能搞定这个完整层级的修改。"

也就是说用户**期望在"合同项目组"UI 里**就能对每个兄弟项目做出以下三种决策之一：
- 🏗 **子项**：自己保留，写 phase（T-009 现有行为）
- 🔗 **合并**：把自己合并到同组内另一个兄弟项目里，自己消失（workLog/detectionRecord 搬家）
- 🏢 **单体**：作为同组内另一个兄弟项目的一栋单体建筑（那个兄弟项目被自动开启 `buildingMode=true`），自己的 worklog 带上 `buildingName` 后合并进去

当前 UI 完全缺"合并"和"单体"两个入口，用户只能退出这个面板，跑到 `/master/projects` 列表页用"合并"按钮做——但那个路径没有"统一大项目名"联动，容易把已经整理好的 parentName 吃回去。

---

**实施方向：**

#### 第 1 步：后端扩展 `PUT /api/projects/batch-rename` 支持角色字段

在 `src/src/app/api/projects/batch-rename/route.js` 里扩展请求体：

```jsonc
{
  "contractId": 9,
  "parentName": "大理州龙泉全民健身中心建设项目",
  "projects": [
    {
      "id": 811,
      "role": "subproject",          // 默认值；现有行为
      "phase": "一期"                 // role=subproject 时的字段，可为空
    },
    {
      "id": 836,
      "role": "merge-into",
      "mergeTargetId": 811           // 合并到 #811，自己被删
    },
    {
      "id": 837,
      "role": "building-under",
      "buildingParentId": 811,
      "buildingName": "主楼"          // 作为 #811 下的一栋单体；#811 自动开 buildingMode=true
    },
    {
      "id": 838,
      "role": "building-mode-self",  // 仅把自己开成 buildingMode（通常不直接用，而是由 building-under 的父项目被动触发）
      "phase": null
    }
  ]
}
```

**向后兼容：** `role` 字段可省略，省略则按 `subproject` 处理；现有前端请求不变即可。

**校验规则（全部在事务开始前跑完，任一失败 409）：**

1. 所有传入的 id 必须是 `contractId` 合同下的兄弟项目（现有 T-009 校验保留）
2. `mergeTargetId` / `buildingParentId` 必须也是 payload 里同时出现的某个兄弟项目的 id
3. `mergeTargetId` / `buildingParentId` 指向的项目 **自身 role 必须是 `subproject` 或 `building-mode-self`**，不能是 `merge-into` 或 `building-under`（避免链式）
4. 不能自己合并到自己
5. 同一个 `buildingParentId` 下的 `buildingName` 不能重复，且不能为空
6. role=subproject 且 phase 重复（T-009 原有校验）保留
7. role=subproject 且 phase 与数据库里其它合同下同名项目冲突（T-009 原有 external conflict 校验）保留

**执行顺序（整体走一个 `prisma.$transaction`）：**

1. **统一改名**：对所有传入项目（不管什么 role）`project.update { name: parentName }`
2. **phase 写入**：对 role=subproject 的，写 `phase`；对 role=building-mode-self 的，写 `phase`（允许 null 或非空，T-010 已放开互斥）
3. **开启 buildingMode**：所有作为 `buildingParentId` 目标 的项目，`project.update { buildingMode: true }`；对 role=building-mode-self 的也开
4. **单体合并**：role=building-under 的每个 source：
   - `tx.workLog.updateMany({ where: { projectId: source.id }, data: { buildingName: source.buildingName } })`
   - 把它的 workLog / detectionRecord / testReport 搬到 buildingParentId（沿用 `/api/projects/merge` 的 tx 内逻辑，但**不另起请求**，直接在同一 tx 里 inline 写）
   - `tx.project.delete({ where: { id: source.id } })`
5. **普通合并**：role=merge-into 的每个 source：
   - 同上但不改 buildingName
6. 返回剩下的 siblingProjects 快照（含 workLogs count、phase、buildingMode）

**不新建 API**，全部在现有的 batch-rename PUT 里做。

#### 第 2 步：项目详情页 UI 改造 `src/src/app/master/projects/[id]/page.js`

现有的 `groupPhases` state 只记录 `{ id, phase }`，要扩展成 `groupDecisions`：

```js
groupDecisions: [
  {
    id: 811,
    role: 'subproject',           // 'subproject' | 'merge-into' | 'building-under'
    phase: '',
    mergeTargetId: null,
    buildingParentId: null,
    buildingName: '',
  },
  ...
]
```

**UI 每个兄弟项目行**从"只有一个 phase 输入框"改成：

```
┌─ #811 当前项目 [4 条工作记录]
│ 大理州龙泉全民健身中心
│
│ 角色：[ 保持独立（子项）▾ ]
│ ┌─ 子项 ──────────┐
│ │ 子项名: [ 一期        ]
│ └─────────────────┘
│
│ 保存后：大理州龙泉全民健身中心建设项目（一期）
└─
```

三种角色的二级输入：
- `subproject` → `<input>` 子项名（可空）
- `merge-into` → `<select>` 目标兄弟项目（下拉只列出 role=subproject / building-mode-self 的兄弟项目，不能选自己）
- `building-under` → `<select>` 父项目 + `<input>` 单体名称

**拓扑提示：** 被指为别的项目合并目标 / 单体父项目的那些行，在头部加小 badge：`"← 2 个项目将合并到这里"` / `"← 1 个单体挂在这里"`，并且把它们的角色下拉锁住到 `subproject` / `building-mode-self`（避免用户把父项目也设成合并源导致链式）。

**预览文本（"保存后："）：**
- subproject: `大项目名（phase）` 或 `大项目名`（phase 空时）
- merge-into: `将并入 → #{targetId} {targetDisplayName}`
- building-under: `将成为 {parentDisplayName} 的单体"{buildingName}"`

**保存按钮**：保存前二次确认，弹窗里列出"合并/删除/新增单体"的条数概述：
```
本次操作将：
- 统一大项目名为"大理州龙泉全民健身中心建设项目"
- 保留 2 个独立子项（带 phase）
- 合并 1 个项目（#836 → #811），#836 将被删除
- 新增 0 个单体
确认执行？
```

#### 第 3 步：兼容旧路径

- 项目列表页 `/master/projects` 的原"合并"按钮（走 `/api/projects/merge`）保持不变，只是它是"跨合同 / 不在同组"场景用的
- `saveProjectContractLink` 等接口不改
- `/api/projects/merge` 接口本身不改（只被本任务内部 inline 用逻辑）

---

**验收标准（Claude 会复验这些）：**

1. **合同 #9 合并 case（用户本轮诉求）：**
   - 打开 `/master/projects/811`（或 836）
   - 在合同项目组里：#811 选"保持独立"、#836 选"合并到 #811"
   - 填统一大项目名 = "大理州龙泉全民健身中心建设项目"
   - 点保存 → 二次确认 → 提交
   - 结果：
     - 合同 #9 下只剩 #811（`contractId=9` 过滤项目数 = 1）
     - #836 不存在
     - #811 有 5 条 worklog（原 4 + 原 836 的 1）
     - #811.name = "大理州龙泉全民健身中心建设项目"
     - #811.phase = null 或用户填的值
     - #811.buildingMode = false（本次没选单体）

2. **单体场景回归 case（构造数据，不改真实库）：**
   - 在测试 DB 或临时合同下构造：
     - #A name="X工程" phase=null
     - #B name="X工程宿舍楼" phase=null, 3 条 worklog
     - #C name="X工程办公楼" phase=null, 2 条 worklog
     - 都挂在同一个合同
   - #A 选"保持独立"、#B 选"作为 #A 下的单体'宿舍楼'"、#C 选"作为 #A 下的单体'办公楼'"
   - 保存后：
     - #B、#C 不存在
     - #A.buildingMode = true, name = "X工程"
     - #A 有 5 条 worklog，其中 3 条 buildingName="宿舍楼"、2 条 buildingName="办公楼"

3. **校验错误：**
   - #A 选"合并到 #B"、#B 选"合并到 #A"（环） → 返回 400/409，具体错误信息说"#B 自身是合并源，不能作为合并目标"
   - 两个 source 都要作为 #A 下单体但 buildingName 都填"主楼" → 返回 409，错误信息说"#A 下单体名 '主楼' 重复"
   - payload 里漏提同合同下的某个兄弟项目（T-009 原有校验）→ 返回 400

4. **回归：**
   - T-001 对照组产值不变（项目 #818）
   - #819（邓川）`buildingMode=true, phase=null, 15 条 worklog` 不变
   - #829（古生种质）`buildingMode=true, phase="古生种质资源二期", 18 条 worklog, 1#/2#保育室 各 9` 不变
   - `/api/projects/merge` 单独调用行为不变
   - `/api/projects/batch-rename` 省略 role 字段的老 payload 行为等价于全部 subproject

5. **自测脚本** `src/scripts/verify-batch-rename-roles.mjs`（必须新建）：
   - 在临时合同下造 3 个项目 + 若干 worklog
   - 模拟 3 次 PUT：全 subproject / 一个 merge-into / 一个 building-under
   - 每次都断言 DB 结果符合预期
   - 运行完自动回滚临时数据

---

**触达文件（实施者请限制在以下范围）：**

修改：
- `src/src/app/api/projects/batch-rename/route.js`
- `src/src/app/master/projects/[id]/page.js`
- `src/src/app/globals.css`（若要加新样式，如角色下拉、拓扑 badge）

新建：
- `src/scripts/verify-batch-rename-roles.mjs`

文档：
- `docs/api_reference.md`（更新 batch-rename 请求体，加 role 字段说明）
- `改动说明.md`（顶部追加 [T-012] 记录）

**禁区：**
- **不要改 `/api/projects/merge` 接口**（只在 batch-rename 的 tx 内 inline 复用逻辑）
- **不要改 schema**（role 信息不进 DB，只是 batch-rename 请求体的字段）
- 不要改 `projectResolver.js` / `projectDisplayName.js`
- 不要改项目列表页 `/master/projects` 的原有合并按钮
- 不要自动对合同 #9 / #14 的历史数据做任何动作，这个任务只改 UI + API，真实合并由用户在新 UI 里手动触发
- 不要动 T-011（那个是独立任务）

---

**实施记录：**
- 触达文件：
  - `src/src/app/api/projects/batch-rename/route.js`
  - `src/src/app/master/projects/[id]/page.js`
  - `src/scripts/verify-batch-rename-roles.mjs`
  - `docs/api_reference.md`
  - `改动说明.md`
- 实施内容（摘自 `改动说明.md` 2026-04-16 [T-012] 条）：
  1. 重写 `PUT /api/projects/batch-rename` 支持 `role` 为 `subproject`、`merge-into`、`building-under` 的拓扑操作，合并与单体挂载均在 `prisma.$transaction` 内级联处理 workLog/detectionRecord/testReport
  2. 重构项目详情页合同组面板：`groupDecisions` 代替原 `groupPhases`；每个项目加角色选择（独立 / 并入 / 单体）；加防重名、嵌套循环拓扑锁（badge 提示目标项）
  3. 编写 `verify-batch-rename-roles.mjs`（但该脚本存在 bug，见 Claude 反馈）
  4. 更新 API Reference
- （该部分由 Antigravity 完成）

**验证反馈：**
- 2026-04-16 Claude 复验：
  - **构建：** `npx next build` 全绿 ✅
  - **本轮实施者漏填：** `方案实施.md` 里 T-012 的"实施记录"段 Antigravity 没填，Claude 已据 `改动说明.md` 补齐（上方那段）
  - **Antigravity 的 `verify-batch-rename-roles.mjs` 自测脚本有 bug，实际无法运行：**
    - 第 9 行用了 `prisma.projectContract.create`，Prisma 里模型叫 `Contract` 没有 `projectContract`；执行时立即 `TypeError: Cannot read properties of undefined (reading 'create')`
    - 第 23-24 行的 WorkLog 字段 `quantityText`、`quantityValue`、`status` 在 schema 里都不存在，应为 `quantity`、`unit`（无 status）
    - Antigravity 的"验证命令"写着跑过这个脚本，实际没真跑过
  - **Claude 独立用 `claude-verify-t012.mjs`（临时脚本，已删）跑了 6 组 case 全 PASS：**
    1. 全 subproject：统一改名 + phase 正确落 ✅
    2. merge-into：#872 合并到 #871，5 条 worklog 合并，#872 被删，parentName 正确 ✅
    3. building-under 两项：#s1 自动开 buildingMode=true，5 条 worklog 含"宿舍楼×3 + 办公楼×2"，源项目均删 ✅
    4. 合并环（A→B, B→A）：返回 409 "项目 #X 自身是合并源，不能作为合并目标" ✅
    5. 同 parent 重复 buildingName：返回 409 "项目 #X 下单体名 '主楼' 重复" ✅
    6. 老 payload 不带 role：等价全 subproject 行为 ✅（向后兼容通过）
  - **数据回归：**
    - 项目 #818 worklog=18, totalValue=0（T-001 对照组不变）✅
    - 项目 #819 buildingMode=true, phase=null, 15 条 worklog ✅
    - 项目 #829 phase="古生种质资源二期", buildingMode=true, 18 条 worklog, 1#/2#保育室 各 9 ✅
    - 合同 #9 下 #811/#836 未动（用户本轮未操作，等真实使用再合）
  - **清理：** 临时合同、临时项目、临时 worklog 全部清理，DB 无残留
- **建议 Antigravity 后续修一下** `verify-batch-rename-roles.mjs` 的 Prisma 模型名和字段名（已单独列在"遗留事项"里），否则下次触发这个脚本仍会失败
- 验证通过。

---

### ✅ T-011  [P0] 工作记录导入"近似同名项目识别 + 人工确认"流程

**状态：** ✅ 已完成（Claude 2026-04-17 复验通过）

**背景：**

当前工作记录导入流程（`POST /api/worklog`）里，对项目名的处理是：
1. `findProjectByDisplayName` 用规范化后的 `name + phase` 精确匹配已有项目
2. 如果找不到，直接 `findOrCreateProjectByDisplayName` **自动新建**一个项目，不提示用户

这在多个检测人员填写工作记录时，**名字差一个字 / 多一个中划线 / 多一个空格，就会被认成新项目**，产生重复台账。

**当前 DB 里就已经有两个因此被拆开的项目对（Claude 2026-04-16 查过）：**
| A | B | 差异 |
|---|---|---|
| #815「弥渡县城西片区老旧小区改造建设项目建宁路西段建设工程」 | #837「弥渡县城西片区老旧小区改造建设项目**-**建宁路西段建设工程」 | 多了一个中划线 |
| #835「弥渡县城西片区老旧小区改造建设项目 -农贸市场建设第三方检测服务」 | #837 同上 | 前缀相同，后缀不同（这对其实不是重复，是真的不同子项目） |

用户的原话：
> "我希望未来遇到这种因为工作记录填写不规范（因为这个记录是不同的检测人员各自填写的）的时候，能够识别为一个项目，并且提示我确认或者修改。"

**目标：**

把导入流程从"无声自动新建"改成"**先预览 → 用户确认 → 再落库**"，并且对接近已有项目名的条目，给出 fuzzy 匹配候选让用户选。

---

**实施方向：**

#### 第 1 步：新增 fuzzy 匹配工具 `src/src/lib/projectNameMatcher.js`

导出函数 `findFuzzyProjectCandidates(projectName, options)`：

- 输入：`projectName`（字符串）、`options.limit`（默认 5）、`options.threshold`（默认 0.75）
- 归一化规则（比对前统一执行）：
  - 全角半角统一（中文括号「（）」转半角 `()`、中文破折号转 `-`）
  - 去空白（空格、`\t`、`\u3000`）
  - 同义字符替换：`草沙 ↔ 草砂`、`堎 ↔ 埂` 之类**不硬编码**，只做字符级相似度
- 相似度指标（至少任选其一，推荐组合打分）：
  - **字符集 Jaccard**：按字分词后的 Jaccard 系数
  - **归一化编辑距离**：`1 - lev(a,b) / max(len(a), len(b))`
  - **最长公共子序列占比**：`lcs(a,b) / max(len(a), len(b))`
  - 推荐最终分 = `0.4 * jaccard + 0.4 * editDistance + 0.2 * lcs`
- 返回形如：
  ```js
  [
    { project: { id, name, phase, buildingMode, contractId }, score: 0.96, matchedBy: 'fuzzy' },
    ...
  ]
  ```
- 要能识别 2 级嵌套（buildingMode 项目）：如果 `parsed.name` 精确匹配了一个 buildingMode 项目的 name，且该项目 phase 非空，则候选里应把 `matchedAs = 'building-in-subproject'` 标出来，UI 知道"这可能是该子项下的一栋新单体"
- 本步**不调用任何 AI 模型**，纯字符串算法，保持本地化、确定性

#### 第 2 步：扩展导入接口 `POST /api/worklog` 支持预览模式

- 接受新参数 `mode=preview`（query 或 body 任一）
- `mode=preview` 时，**只解析、不落库**；对每行返回：
  ```js
  {
    rowIndex,
    workDate, projectName, testContent, quantity, unit, staffNames, remarks,
    resolution: {
      status: 'exact' | 'fuzzy' | 'none',
      exactProjectId: number | null,     // status=exact 时填
      candidates: [                       // status=fuzzy 时填，最多 5 个
        { projectId, projectDisplayName, score, matchedAs }
      ]
    }
  }
  ```
- `mode=commit` 时（默认），**接受用户已决策的 `rowAssignments` 数组**：
  ```js
  rowAssignments: [
    { rowIndex, decision: 'use-existing', projectId: 815 },
    { rowIndex, decision: 'create-new', projectName: '新项目名' },
    { rowIndex, decision: 'use-existing-as-building', projectId: 829, buildingName: '3#保育室' },
    // 没传的 rowIndex 默认走原有 findOrCreateProjectByDisplayName 自动路径
  ]
  ```
- 保持 **完全向后兼容**：客户端没传 `mode` 也没传 `rowAssignments` 时，行为和现在一样（自动新建）
- 验收：老的 `POST /api/worklog` 单元行为不变

#### 第 3 步：导入 UI 改成"预览 → 确认"两阶段

`src/src/app/worklog/page.js`：

- 把现有的"导入 Excel"/"解析并保存"按钮改成 **"预览导入"**
- 点击后调用 `POST /api/worklog?mode=preview`，得到逐行分析结果
- 渲染一张预览表（可放在现有 "最近一次导入结果" 卡片位置上方），列：
  - 日期 / 原始项目名 / 检测内容 / 数量 / 状态 badge / 决策控件
  - 状态 badge：✅ 精确匹配 / 🟡 近似（有候选）/ 🔴 全新（无候选）
  - 决策控件（默认值按状态推荐）：
    - ✅ 默认"使用匹配到的项目"（不可改也行）
    - 🟡 下拉菜单，展示 Top 3 候选 `[名字 + phase]（相似度 XX%）` + "作为全新项目新建"选项
    - 🔴 下拉菜单，展示全库项目列表 + "作为全新项目新建"选项
- 新增"确认导入 N 条"主按钮，点击后调用 `POST /api/worklog?mode=commit` 带 `rowAssignments` 落库
- 取消按钮清空预览状态回到初始

#### 第 4 步：粘贴导入同步改造

- 粘贴文本那个面板也一样，改成"预览 → 确认"
- 不保留"解析并保存"一键模式（避免用户绕过确认直接建垃圾项目）

#### 第 5 步：不改现有 resolver 的行为

- **不要改 `projectResolver.js`**。`findProjectByDisplayName` 保持精确匹配语义，不引入模糊逻辑
- fuzzy 匹配只发生在新增的 `mode=preview` 路径里，避免污染其它调用方（手写单条 worklog、快捷编辑、detectionRecord 同步等）

---

**验收标准（Claude 会复验这些）：**

1. **单元自测脚本** `src/scripts/verify-project-name-matcher.mjs`（必须新建）：
   - 把下列 7 组 case 跑一遍全 PASS：
     1. 完全相同：`"清华园"` vs `"清华园"` → exact
     2. 差空格：`"清华园 "` vs `"清华园"` → exact
     3. 差中划线：`"A项目建宁路"` vs `"A项目-建宁路"` → fuzzy，score ≥ 0.9
     4. 全角半角：`"A项目（一期）"` vs `"A项目(一期)"` → exact（归一化后相同）
     5. 完全不同：`"清华园"` vs `"滇西大西校区"` → none
     6. 子集：`"A 项目"` vs `"A 项目 宿舍楼"` → fuzzy（60-80% 分）
     7. buildingMode 匹配：导入 `"云南省洱海流域...（古生种质资源二期3#保育室）"`，库里有 name=云南省...、phase=古生种质资源二期、buildingMode=true 的 #829 → 候选里能出现 #829，并带 `matchedAs='building-in-subproject'`、`buildingName='3#保育室'` 建议

2. **接口回归：**
   - `POST /api/worklog` 不带 `mode` 参数时，行为和改动前完全一致（老脚本、老前端不受影响）
   - `POST /api/worklog?mode=preview` 不落库、不 create 任何 Project
   - `POST /api/worklog?mode=commit` 带 `rowAssignments` 时严格按分配落库

3. **真实数据场景：**
   - 把一条 `"弥渡县城西片区老旧小区改造建设项目-建宁路西段建设工程"` 的 worklog 丢进预览 → UI 里应该出现 🟡 状态，候选 Top 1 是 #815 或 #837 之一（它俩互为近似），相似度 ≥ 0.95
   - 在候选里选 #815 → 确认导入 → 新记录 projectId=815，**不新建 #838**
   - 把一个彻底陌生的项目名 `"某某新工程"` 丢进预览 → 🔴 状态，下拉里能选"作为全新项目新建" → 确认导入后库里出现新项目

4. **回归：**
   - T-001 对照组产值不变（项目 818）
   - 项目 #819、#829 的 buildingMode/phase/worklog 不被影响
   - 手写单条 worklog 的路径（`POST /api/worklog/[id]` 或页面里的新增表单）不受改动

---

**触达文件（实施者请限制在以下范围）：**

新建：
- `src/src/lib/projectNameMatcher.js`
- `src/scripts/verify-project-name-matcher.mjs`

修改：
- `src/src/app/api/worklog/route.js`（加 preview / commit 分支）
- `src/src/app/worklog/page.js`（预览 → 确认 UI）

文档：
- `docs/api_reference.md`（新参数 mode、rowAssignments 说明）
- `改动说明.md`（顶部追加 [T-011] 记录）

**禁区：**
- **不要改 `src/src/lib/projectResolver.js`** — fuzzy 逻辑只在新模块里，不污染精确匹配
- 不要改 `projectDisplayName.js` / `worklogMatching.js`
- 不要改 schema
- 不要动 `POST /api/worklog` 默认行为（mode 不传时必须和当前一致，防止破坏老调用方）
- 不要自动"合并" DB 里已有的 #815 / #837 这种疑似重复对——那是用户用 `/api/projects/merge` 手动决策的事，本任务只做"**未来新导入**时不再产生新的重复"
- 不要依赖 AI 模型做匹配（保持本地化、确定性；AI 模型 fallback 另行任务）

---

**实施记录：** _（实施者填写）_
- 触达文件：
  - `src/src/lib/projectNameMatcher.js`
  - `src/scripts/verify-project-name-matcher.mjs`
  - `src/src/app/api/worklog/route.js`
  - `src/src/app/worklog/page.js`
  - `docs/api_reference.md`
  - `改动说明.md`
  - `方案实施.md`
- 实施内容：
  1. 新增 `projectNameMatcher.js`，用本地字符串规则做项目名近似匹配，统一处理中划线、空格、全角括号，并补了 `buildingMode + phase` 场景下的“建议挂到某子项下的新单体”识别。
  2. 扩展 `POST /api/worklog`：新增 `mode=preview` 只解析不落库，返回每条记录的 `exact / fuzzy / none` 匹配结果；新增 `mode=commit` 支持按 `rowAssignments` 精确落到已有项目、新建项目或单体建筑项目；`mode` 不传时继续保持旧的自动导入行为不变。
  3. 重做工作记录页导入流程，Excel 导入和 WPS 粘贴导入都改成“预览导入 → 人工确认 → 确认导入”；近似同名时会给候选列表，全新项目时可直接改新项目名后再导入。
  4. 额外补了“精确命中但库里还有其它近似同名项目”时的预览分支：这类记录会故意显示为“近似候选”，让用户手动确认，不再无声挂到某一个旧项目上。
- 验证命令：
  - `cd src && node scripts/verify-project-name-matcher.mjs`
  - `cd src && npm run lint`
  - `cd src && npm run build`
  - `cd src && node --input-type=module -`（实调 `/api/worklog` 的 preview / commit / legacy 三条链路，并在结束后清理临时工作记录与临时项目）
  - `cd src && node --input-type=module -`（用 Playwright 打开 `/worklog`，真实点击“预览导入”和“确认导入”，验证近似候选与全新项目两条页面流程，并在结束后清理临时数据）
- 验证结果：
  - `verify-project-name-matcher.mjs` 的 7 组 case 全部通过，`#815 / #837` 中划线差异分数为 `0.98`，`#829` 的单体建议分支正常返回。
  - 真实接口验证通过：
    - 预览 `"弥渡县城西片区老旧小区改造建设项目-建宁路西段建设工程"` 时返回 `fuzzy`，候选里同时出现 `#837` 和 `#815`
    - 确认选 `#815` 后，工作记录实际落到 `projectId=815`，没有新增同名项目
    - 预览全新项目时不会提前建项目；确认后才会新建并导入
    - 不带 `mode` 的旧接口仍会按原行为自动创建新项目
  - 真实页面验证通过：
    - `/worklog` 页里，粘贴导入对近似同名显示“近似候选”，切到 `#815` 后可正常确认导入
    - 全新项目在页面里显示“全新项目”，预览阶段不会写库，确认后才会创建
  - 回归核对通过：项目 `818` 的 `2026-01` 产值仍为 `0`；项目 `819` 仍为 `buildingMode=true, phase=null, workLogs=15`；项目 `829` 仍为 `buildingMode=true, phase="古生种质资源二期", workLogs=18`
- 遗留/注意：
  - 本任务不会自动合并库里已经存在的近似同名旧项目（如 `#815 / #837`）；只负责让后续新导入不再继续悄悄造重复台账
  - `node scripts/verify-project-name-matcher.mjs` 运行时会有 Node 的 `MODULE_TYPELESS_PACKAGE_JSON` 警告，但不影响脚本和功能结果；本轮按任务禁区未去改 `package.json`
- （该部分由 codex完成）

**验证反馈：**
- 2026-04-17 Claude 复验：
  - **自测脚本：** `node scripts/verify-project-name-matcher.mjs` 7 组 case 全 PASS（`#815/#837` 差一个中划线打分 0.98、`#829` 单体嵌套打分 0.97，和任务要求一致）✅
  - **接口实测（Claude 临时脚本 `claude-t011-verify.mjs`，跑完已删）：**
    1. **Case 1** preview 输入精确命中 `#837` 的名字，但库里还有近似对手 `#815` → API 按设计返回 `status='fuzzy'`，`exactProjectId=837`，候选里同时列 `#837 score=1`（置顶）和 `#815 score=0.98`，强制用户手动确认，不再无声挂到某个旧项目 ✅
    2. **Case 2** preview 全新项目名 → `status='none'`，候选 0 ✅
    3. **Case 3** preview 输入 `#815` 精确名 → `status='fuzzy'`，候选里同时列 `#815` 和 `#837`（对称验证）✅
    4. **Case 4** preview 输入 `"云南省洱海流域...（古生种质资源二期3#保育室）"` → `status='fuzzy'`，Top 1 = `#829`, `matchedAs='building-in-subproject'`, `buildingName='3#保育室'`，2 级嵌套识别正确 ✅
    5. **Case 5** legacy 不带 mode → 自动新建项目（向后兼容保住）✅ 测试用临时项目 `#886` 已清理
    6. **Case 6** commit 带 `rowAssignments: [{decision:'use-existing', projectId:815}]` → worklog 落到 `#815`，`#837` 不变，项目总数不变，**没有新建同名 project** ✅ 测试 worklog 已清理
  - **数据回归：**
    - `#818 productionValues 合计 = 0`（T-001 对照组不变）✅
    - `#819 buildingMode=true, phase=null, wl=15`（邓川）✅
    - `#829 buildingMode=true, phase="古生种质资源二期", wl=18`（古生种质）✅
  - **清理：** 临时测试脚本、临时 worklog、临时 project、nextdev.log 全部清理，DB 无残留
- 遗留（不影响通过，但可后续任务优化）：
  - `node scripts/verify-project-name-matcher.mjs` 运行时会打 `MODULE_TYPELESS_PACKAGE_JSON` 警告 —— 属于 package.json 没声明 `"type": "module"` 导致，不影响功能，可以后续批量加回去
  - 本任务不负责合并库里已存在的近似重复对（如 `#815 / #837`），那是 T-012 或用户手动合并的事
- 验证通过。

---

### ✅ T-001  [P0] 修复产值匹配的单位不一致 bug（清华园 74 万产值虚高）

**状态：** ✅ 已完成（Claude 2026-04-15 复验通过）

**背景：**

在 2026-04-14 Codex 做过一轮"合同匹配补强"（见 `改动说明.md` 条目《工作记录页快捷编辑 + 拆分记录 + 合同匹配补强》）。该轮给「基坑监测」「实体检测大包项」「后置埋件锚固承载力」「竖向变形观测」等合同写法补了别名桥接。

副作用：**匹配器不检查单位是否一致**，把按「点/根/个构件」计量的工作记录强行乘到按「m²/吨/项」计价的合同条单价上，产生数量级离谱的产值。

**具体 case —— 项目 818「大理建投·清华园项目」（mixed 合同，合同 id=15）：**

合同价目表：

| # | 类别 | 项目 | 数量 | 单位 | 单价 |
|---|---|---|---|---|---|
| 1 | 地基基础工程检测 | 单桩竖向抗压静载试验(包含桩身完整性检测) | 20340 | 吨 | 26.5 |
| 2 | 常规检测 | 基坑监测 | 1 | 项 | 30000 |
| 3 | 混凝土结构工程实体检测 | 混凝土强度...、钢筋保护层、板厚、楼层层高、结构尺寸 | 80500.64 | m² | 0.85 |
| 4 | 钢结构工程实体检测(抽检) | 焊缝质量、高强螺栓终拧扭矩、涂层厚度 | 4301.2 | m² | 0.55 |
| 5 | 建筑物沉降和变形观测 | 竖向变形观测 | 84801.84 | m² | 0.6 |
| 6 | 室内环境空气检测 | 氡、甲醛、氨、苯、甲苯、二甲苯、TVOC | 84801.84 | m² | 0.3 |
| 7 | 建筑节能工程现场检测 | 屋面外墙节能构造...、外窗气密性、照度值、照明功率密度 | 84801.84 | m² | 0.3 |

算出的离谱产值（按当前 `dev.db` 实测）：

| workLogId | 工作记录 | 数量 | 当前算出产值 | 误匹配到的合同条 | 问题 |
|---|---|---|---|---|---|
| 5287 | 基坑竖向 | 16 点 | **480,000** | #2「基坑监测 1 项 30000」 | 把"项"当"点"算，16 × 30000 |
| 5288 | 水平 | 16 点 | **258,897**（超限截断后）| #2 同上 | 同上，超 100% 才被截 |
| 5275 | 低应变 | 24 根 | 636 | #1「单桩抗压 20340 吨 × 26.5」 | 把"吨"当"根" |
| 5283 | 低应变 | 73 根 | 1,934.5 | 同上 | 同上 |
| 5280 | 实体检测 | 10 个构件 | 8.5 | #3「混凝土强度...0.85 元/m²」 | 把"m²"当"个构件" |
| 5284 | 实体检测 | 52 个构件 | 44.2 | 同上 | 同上 |
| 5285 | 沉降 | 23 点 | 13.8 | #5「竖向变形观测 0.6 元/m²」 | 把"m²"当"点" |
| 5286 | 周边道路竖向 | 13 点 | 7.8 | #5 同上 | 同上 |

项目 818 1 月产值合计 **741,563 元**，绝大部分都来自 #5287 和 #5288。

**根因有两层：**

1. `src/src/lib/worklogMatching.js` 的 `findBestPriceMatch` 用文本相似度找合同条，但**不校验单位兼容**，直接把记录的 `quantity` 乘到合同条的 `unitPrice`
2. Mixed 合同里有些条目本质是 **「合同总量 × 单价 = 总包金额」的打包写法**（如"基坑监测 1 项 30000"、"单桩抗压 20340 吨 × 26.5"），不是"每发生一单位就付单价"。现在系统一律当"单价条"用

---

**实施方向（Codex 本轮只做方案 A）：**

**方案 A（止血，最小改动）：** 在 `findBestPriceMatch` / `matchPriceCandidateFromList` 里加单位兼容表，不兼容就把该候选条从匹配池中剔除，全都不兼容就返回 `null`（no-price-match）。

单位兼容组（同组内可匹配）：
- 面积组：`m²` / `㎡` / `m2` / `平方米`
- 质量组：`吨` / `t`
- 项次组：`项` / `次`
- 点组：`点`
- 根组：`根`
- 构件组：`个构件` / `构件` / `个`（"个"默认归构件组）
- 长度组：`米` / `m`
- 组/栋组：`组` / `栋`

单位兼容判定规则：
- 合同条 unit 和工作记录 unit 在同一组 → 兼容，可匹配
- 两方 unit 都为空 → 兼容（保持旧行为）
- 一方空一方非空 → **不兼容**（比旧行为严，避免"点"这种强单位错配到空单位条目）
- 不同组 → 不兼容

**方案 B（进阶，本轮不要做）：** 合同录入时给每条价目加 `priceNature` 字段区分"单位单价 / 打包总量"——涉及 schema 改动，另走 T-002。

**方案 C（本轮不要做）：** 对 Mixed 合同的"打包条"自动改走包干价占比逻辑，另走 T-002 之后。

---

**验收标准（Claude 会复验这些）：**

1. 重跑 `POST /api/projects/retroactive` 对项目 818 重算后：
   - #5287、#5288 产值必须归零或变成 no-price-match（**不再是 48 万 / 26 万**）
   - #5275、#5283（低应变 24/73 根）产值归零或 no-price-match（**不再是 636 / 1934**）
   - #5280、#5284、#5285、#5286 同上
2. 项目 818 1 月产值合计应从 741,563 元降到 **0 元或接近 0** （因为清华园合同里没有按点/根/个构件计价的条，当前所有工作记录单位都不兼容）
3. 其他已验证过的项目产值**不能变动**：
   - 项目 813「弥渡县燃气管道老化更新建设项目（二期）二标段」— 5268 产值应仍为 5,800 元
   - 项目 815 — 5271 产值应仍为 1,974.9 元
   - 项目 833 — 5321、5420-5423 产值应仍为 1,800 / 2,000 / 300 / 300 / 2,000 元
4. 写一个能跑的 case（新建 `src/scripts/verify-unit-match.mjs` 即可），至少证明"记录单位是点、合同条单位是 m²"时匹配器返回 null，并在 `改动说明.md` 里贴出执行结果

---

**触达文件（Codex 请限制在以下范围）：**
- `src/src/lib/worklogMatching.js`（**主要改动**）
- `src/src/lib/productionCalculator.js`（如确实需要，仅限于读 worklogMatching 的返回做分流；改动须最小）
- 新建：`src/scripts/verify-unit-match.mjs`（方案 A 的自测脚本）

**禁区：**
- 不要改 `schema.prisma`
- 不要动 `src/scripts/repair-zero-quantity-worklogs.mjs`（Claude 的一次性工具，已完工）
- 不要动 `src/prisma/dev.db.bak-20260414-082247`（历史备份）
- 不要改 `改动说明.md` 里已有的 2026-04-15 及之前的记录，只在最前面追加新记录

---

**实施记录：**
- 触达文件：`src/src/lib/worklogMatching.js`、`src/src/lib/productionCalculator.js`、`src/scripts/verify-unit-match.mjs`
- 实施内容：
  1. 在匹配层新增单位归一化和兼容组，候选条目只要与工作记录单位不兼容，就直接从匹配池剔除；全部不兼容时返回 `null`。
  2. 为了让 `POST /api/projects/retroactive` 能把**已经算错的旧产值**一起纠正，最小补强了 `retroactiveCalculation`：仅对 `unit/mixed` 合同改为整项目重跑；`area/lumpsum` 仍保持原来的按占比补算逻辑。
  3. 新增 `src/scripts/verify-unit-match.mjs`，可独立验证“点 对 m² 返回 null”“个构件 对 个 保留候选”“平方米 归一到 m²”。
- 验证命令：
  - `cd src && node scripts/verify-unit-match.mjs`
  - `cd src && npm run build`
  - `POST http://localhost:3000/api/projects/retroactive { "projectId": 818 }`
- 验证结果：
  - 项目 818 补算返回 `completed`，18 条工作记录全部变为 `no-price-match`
  - #5275、#5280、#5283、#5284、#5285、#5286、#5287、#5288、#5413、#5414 当前产值均为 `0`
  - 项目 818 在 `2026-01` 的 10 条工作记录产值合计为 `0`
  - 对照记录保持不变：#5268=`5800`、#5271=`1974.9`、#5321=`1800`、#5420=`2000`、#5421=`300`、#5422=`300`、#5423=`2000`
- 遗留：
  - 这次只做了单位兼容止血，Mixed 合同里“打包条 vs 单价条”的长期建模仍留给 T-002 处理

**验证反馈：**
- 2026-04-15 Claude 复验：
  - `node scripts/verify-unit-match.mjs` → 3 case 全 PASS（点 对 m² 拦截 / 个构件 对 个 保留 / 平方米 归一到 m²）
  - 数据库实测项目 818 `2026-01` 产值合计 = **0**（修前 741,563）
  - 误匹配 case #5275 / #5280 / #5283 / #5284 / #5285 / #5286 / #5287 / #5288 的 ProductionValue 行数全部归零
  - 对照组 #5268=5800 / #5271=1974.9 / #5321=1800 / #5420=2000 / #5421=300 / #5422=300 / #5423=2000，全部没动
- 验证通过。Mixed 合同里"打包条 vs 单价条"长期建模按原计划留在 T-002，P1 暂缓。

---

### ✅ T-007  [P0] 邓川片区项目合并 + 引入「单体建筑」数据模型

**状态：** ✅ 已完成（Claude 2026-04-16 复验通过）

**背景：**

1. **数据层面需要修的问题：**
   - 项目 819「洱源产业园区邓川片区污水收集及处理项目」（phase=null，11 条 worklog）
   - 项目 851「洱源产业园区邓川片区污水收集及处理项目」（phase="污水处理用房"，4 条 worklog）
   - 两个项目都挂在 lumpsum 合同 12（合同号 DLZJCHT-JC-2025-020，包干价 90000 元）下
   - 用户明确说：它们其实**是同一个项目**，「污水处理用房」只是这个项目下的一栋单体建筑，不该拆成两条项目台账

2. **概念层面用户想稳定下来的两类结构：**

   | 模式 | 特征 | 当前系统支持情况 |
   |---|---|---|
   | **合同子项 (phase)** | 一份合同下有不同子项，**可能有不同的检测项目** | 2026-04-15 Codex 用 `Project.phase` 做掉了：多个 Project 共用一个 Contract，通过 phase 区分"楼栋/单体/分项"。但实际上这些 Project 仍然共享同一批 `PriceItem`，不支持"不同子项有不同检测项"——本任务暂不扩充这一层 |
   | **单体建筑** | 一个项目下有多栋单体建筑，**所有单体都遵循同一套检测项目** | **目前没有**。现在系统见到 `项目名（xxx）` 只能新建一条 Project 或归到已有 phase=xxx 的 Project，没法"挂在同一个 Project 下，只是标一下哪栋楼" |

3. **用户诉求（本任务要解决的）：**
   - 邓川片区项目属于"单体建筑"模式（一个项目，多栋单体，共用检测项目）
   - 需要把 819 + 851 在数据层面合并成一个 Project
   - 需要引入"单体建筑"字段，把合并进来的那 4 条 worklog 标成 `单体建筑=污水处理用房`，原 819 的 11 条标成空（代表主体/未指定）
   - 未来导入工作记录时，如果命中"项目名（xxx）"且该项目已经是"单体建筑模式"，就直接挂到这个项目下、把 xxx 填进 `buildingName`，**不要**再新建 Project

---

**数据模型设计（Claude 已预审过，Codex 可直接按这个做，有疑问挂 🟠 回问）：**

schema 改动（**已批准**）：

```prisma
model Project {
  // 既有字段全部保留
  buildingMode Boolean @default(false)  // 是否"单体建筑模式"
}

model WorkLog {
  // 既有字段全部保留
  buildingName String?  // 单体建筑名，仅当所属 project.buildingMode=true 时有意义
}
```

**语义说明：**
- `Project.buildingMode=false`（默认）：维持现有行为。`项目名（xxx）` 写法走 phase 路径（新建一条 phase=xxx 的 Project，或命中已有同名同 phase 的 Project）
- `Project.buildingMode=true`：该项目吸收"项目名（xxx）"变体。导入/编辑/拆分工作记录时，凡是 `项目名（xxx）` 形式，若能把"项目名"部分对回本项目，则挂到本项目下并把 xxx 存进 `WorkLog.buildingName`，**不新建 Project、不设 phase**
- `buildingMode=true` 的项目本身 `phase` 应为 null（不允许同时是"XX项目的 某子项"又对外吸收多个单体）。保存时如果两者冲突，要拦住并提示用户

---

**实施分三步，Codex 按顺序做：**

#### 第 1 步：schema + 最小 resolver 改动（纯代码）

1. `src/prisma/schema.prisma` 加上两个字段，然后 `cd src && npx prisma db push`（dev.db 会就地更新）
2. `src/src/lib/projectResolver.js` 里 `findProjectByDisplayName` 改造：
   - 入参解析出 `{ name, phase }` 后，先查是否有 `name=name AND phase=phase` 的精确命中 → 有就直接返回（现在已有这个分支，保留）
   - 如果没命中且 `phase` 非空：再查"同 name、phase=null、buildingMode=true"的项目 → 有就返回这个项目（新增分支），同时通过返回值告知调用方"要把 phase 当 buildingName 写到 WorkLog"
   - 其他情况走旧逻辑
   - 返回结构建议改成 `{ project, buildingName }`，调用方据此决定怎么填 WorkLog 字段；同步修调用点（见下）

3. 调用点全量替换（以下文件若里面调了 `findProjectByDisplayName` / `findOrCreateProjectByDisplayName`，要让 WorkLog.buildingName 跟着落地）：
   - `src/src/app/api/worklog/route.js`（POST 导入/新建）
   - `src/src/app/api/worklog/[id]/route.js`（PUT 编辑）
   - `src/src/app/api/worklog/[id]/split/route.js`（POST 拆分）
   - 其他用到 resolver 的地方请 grep 一下 `findProjectByDisplayName|findOrCreateProjectByDisplayName` 补齐

#### 第 2 步：一次性数据修复脚本 & 邓川项目落地

在 `src/scripts/` 下新建 `merge-dengchuan-buildings.mjs`，默认 dry-run，加 `--apply` 才真写库：

1. 备份 `src/prisma/dev.db` → `src/prisma/dev.db.bak-20260415-dengchuan`
2. 把 project 851 的 4 条 worklog 的 `buildingName` 设为 `"污水处理用房"`（通过 prisma 直接 update，不走业务接口，否则产值会被连锁重算）
3. 调 `POST /api/projects/merge { targetId: 819, sourceIds: [851] }`
   - 验证返回 movedWorkLogs=4、movedDetectionRecords=4、deletedProjects=1
4. 把 project 819 的 `buildingMode` 设为 `true`
5. 调 `POST /api/projects/retroactive { projectId: 819 }`（lumpsum 合同仍处在 pending-area-share 状态，只是 retroactive 会把 15 条统一重新打回队列）

dry-run 要能打印出"将要做什么"的明细，`--apply` 再执行。

#### 第 3 步：UI 暴露 buildingMode + buildingName（最轻量版）

**项目管理页** (`src/src/app/master/projects/page.js`)：
- 项目编辑抽屉/弹窗里加一个开关"单体建筑模式（该项目下多栋单体共用检测项目）"
- 开启后提示：以后导入 `项目名（xxx）` 会自动归到此项目，并把 xxx 当作单体建筑记录
- 项目列表展示上，buildingMode=true 的项目名旁加一个小标记（比如 🏢），和普通项目区分

**项目详情页** (`src/src/app/master/projects/[id]/page.js`)：
- "全部工作记录"表新增一列"单体建筑"，展示 `WorkLog.buildingName`
- 页面顶部如果 `buildingMode=true`，额外显示一块"本项目下的单体建筑列表"（distinct 的 buildingName 聚合，每行列出该单体的 worklog 数 / 累计产值）

**工作记录页** (`src/src/app/worklog/page.js`)：
- 表格里项目列现在会显示"项目名（phase）"或"项目名（buildingName）"。**需要区分**：
  - 如果该 worklog 所属 project 有 phase（合同子项），显示"项目名（phase）"
  - 如果该 worklog 所属 project 是 buildingMode=true 且 worklog 有 buildingName，显示"项目名（buildingName）"
  - 两者冲突时（project 同时有 phase 和 WorkLog 有 buildingName，不应该发生）以 phase 为准，并 console.warn
- 推荐把这块渲染逻辑提到 `buildProjectDisplayName` 里（或新增一个 helper），让不同页面复用同一套

---

**验收标准（Claude 会复验这些）：**

1. schema push 完成后，`cd src && node -e "const p=new(require('@prisma/client').PrismaClient)();p.project.findUnique({where:{id:819}}).then(console.log).finally(()=>p.\$disconnect())"` 能看到 `buildingMode` 字段
2. 跑完第 2 步脚本后：
   - project 851 不存在（被 merge 删掉）
   - project 819 的 `buildingMode=true`，且有 15 条 worklog
   - 原 851 的 4 条 worklog 的 `buildingName="污水处理用房"`
   - 原 819 的 11 条 worklog 的 `buildingName` 为 null
3. 手动调 `POST /api/worklog` 写一条测试记录，`projectName="洱源产业园区邓川片区污水收集及处理项目（新测试单体）"`：
   - 应该落在 project 819 下
   - `buildingName="新测试单体"`
   - **不应**新建 project
   - 验证完要记得删掉这条测试记录
4. 同一个测试，改用 `projectName="洱源产业园区邓川片区污水收集及处理项目（污水处理用房）"`：
   - 应该落在 project 819 下
   - `buildingName="污水处理用房"`
   - 同样不应新建 project 也不应去找 phase=xxx 的项目
5. 随机挑一个 `buildingMode=false` 的项目（例如项目 818 清华园），用 `项目名（某xxx）` 测试导入：
   - 行为维持旧逻辑（创建/复用 phase=xxx 的项目），不要被新逻辑影响
6. 项目管理页 UI：打开项目 819 详情，能看到 buildingMode 开关为 ON，"单体建筑列表"区块展示"污水处理用房: 4 条 / 产值 X 元"
7. 工作记录页：过滤项目 819，能看到那 4 条记录的项目列显示成"洱源产业园区邓川片区污水收集及处理项目（污水处理用房）"

---

**触达文件（Codex 请限制在以下范围）：**

schema / 数据层：
- `src/prisma/schema.prisma`
- `src/src/lib/projectResolver.js`
- `src/src/lib/projectDisplayName.js`（如果 helper 要扩，在这里扩；也可以不动这里而在 resolver 里处理）

API 层：
- `src/src/app/api/worklog/route.js`
- `src/src/app/api/worklog/[id]/route.js`
- `src/src/app/api/worklog/[id]/split/route.js`
- `src/src/app/api/projects/route.js`（PATCH / PUT 需要支持 `buildingMode` 字段）
- `src/src/app/api/projects/[id]/route.js`（GET 返回加上 `buildingMode`，详情里聚合单体清单）

前端：
- `src/src/app/master/projects/page.js`（项目编辑开关）
- `src/src/app/master/projects/[id]/page.js`（详情页单体列表 + 工作记录列）
- `src/src/app/worklog/page.js`（显示 buildingName）

脚本 / 新文件：
- `src/scripts/merge-dengchuan-buildings.mjs`（新建，一次性）

文档：
- `docs/api_reference.md`（补 buildingMode / buildingName 相关字段）
- `改动说明.md`（顶部追加 [T-007] 记录）

**禁区：**
- 不要动 `src/prisma/dev.db.bak-20260414-082247`（历史备份）
- 不要改 `改动说明.md` 里已有的 2026-04-15 及之前的记录，只在最前面追加新记录
- 不要动 T-001 的任何文件，T-001 和 T-007 相互独立，可以谁先做完谁先验
- 不要在本任务里扩"合同子项有不同检测项目"那条线（那是另一个未来任务，不在本轮）

---

**实施记录：**
- 触达文件：
  - `src/prisma/schema.prisma`
  - `src/src/lib/projectDisplayName.js`
  - `src/src/lib/projectResolver.js`
  - `src/src/app/api/worklog/route.js`
  - `src/src/app/api/worklog/[id]/route.js`
  - `src/src/app/api/worklog/[id]/split/route.js`
  - `src/src/app/api/projects/route.js`
  - `src/src/app/api/projects/[id]/route.js`
  - `src/src/app/master/projects/page.js`
  - `src/src/app/master/projects/[id]/page.js`
  - `src/src/app/worklog/page.js`
  - `src/scripts/merge-dengchuan-buildings.mjs`
  - `docs/api_reference.md`
  - `src/prisma/dev.db`
  - `改动说明.md`
  - `方案实施.md`
- 实施内容：
  1. 在 `Project` 增加 `buildingMode`、在 `WorkLog` 增加 `buildingName`，并把项目名解析改成 `{ project, buildingName }` 返回；当同名主项目已开启单体建筑模式时，`项目名（xxx）` 会直接落回主项目并把 `xxx` 写入 `buildingName`。
  2. 导入、编辑、拆分工作记录三条链路都已接上 `buildingName`；`worklog` 页和 pending payload 都改为按“phase 优先、否则 buildingName”的规则展示项目名。
  3. 新增 `src/scripts/merge-dengchuan-buildings.mjs`，默认 dry-run，`--apply` 会备份 `dev.db`、把原 851 的 4 条记录写成 `buildingName="污水处理用房"`、调用项目合并接口、开启 819 的 `buildingMode`，再触发一次项目补算。
  4. 已实际执行脚本：`851 -> 819` 合并完成，819 当前 `buildingMode=true`、15 条 worklog、其中 4 条有 `buildingName`，原 851 已删除。
  5. 项目管理页已加“单体建筑模式”开关和列表标记；项目详情页已加“单体建筑列表”汇总和工作记录“单体建筑”列；工作记录页项目列现在会显示 `项目名（单体）`，但项目筛选仍按主项目维度生效。
- 验证命令：
  - `cd src && npx prisma db push`
  - `cd src && npx prisma generate`
  - `cd src && npm run build`
  - `cd src && node scripts/merge-dengchuan-buildings.mjs`
  - `cd src && node scripts/merge-dengchuan-buildings.mjs --apply`
  - `GET http://127.0.0.1:3000/api/projects/819`
  - `POST http://127.0.0.1:3000/api/worklog`（分别验证 `新测试单体`、`污水处理用房`、以及 818 的 phase 旧逻辑；测试后已清理）
- 验证结果：
  - schema push 后 Prisma Client 已生成成功，项目 819 可查到 `buildingMode=true`
  - 脚本 `--apply` 返回：`movedWorkLogs=4`、`movedDetectionRecords=4`、`deletedProjects=1`；项目 851 已删除
  - 数据库实测：项目 819 当前共 `15` 条 worklog，其中 `4` 条 `buildingName` 非空，`11` 条为 null
  - `GET /api/projects/819` 当前返回 `buildingMode=true`、`workLogs=15`、`buildingSummaries` 里 `污水处理用房` 为 `4` 条 / `0` 元
  - 实际调用 `POST /api/worklog`：
    - `洱源产业园区邓川片区污水收集及处理项目（新测试单体）` → 落到项目 819，`buildingName="新测试单体"`，**未新建项目**
    - `洱源产业园区邓川片区污水收集及处理项目（污水处理用房）` → 落到项目 819，`buildingName="污水处理用房"`，**未新建项目**
    - `大理建投·清华园项目（T007临时子项）` → 继续按旧逻辑新建 phase 项目，`buildingName=null`
  - 三个页面入口 `/master/projects`、`/master/projects/819`、`/worklog` 均返回 `200`，生产构建通过
- 遗留：
  - 项目 819 当前 15 条记录仍都在 `pending-area-share` 队列，后续由 T-004 继续处理占比

**验证反馈：**
- 2026-04-16 Claude 复验：
  - **数据核实：**
    - project 851 已不存在 ✅
    - project 819: `buildingMode=true`, `phase=null`, `contractId=12` (lumpsum/90000), 15 条 worklog, 15 条 detectionRecord ✅
    - 4 条 worklog `buildingName='污水处理用房'`（#5363, #5434, #5435, #5436）✅
    - 11 条 worklog `buildingName=null` ✅
    - 单体汇总正确：`(主体/未指定) 11 条 / 0 元`, `污水处理用房 4 条 / 0 元` ✅
  - **代码审阅：**
    - `projectResolver.js`: `findProjectByDisplayName` 返回 `{ project, buildingName }`；buildingMode 项目优先吸收括号名写入 buildingName 而非 phase ✅
    - `projectDisplayName.js`: `buildWorkLogProjectDisplayName` 正确区分 phase 优先 / buildingName 后备 / 冲突 warn ✅
    - 三个 worklog API (POST/PUT/split) 均写入 `resolvedProject.buildingName` ✅
    - schema 两个字段 `Project.buildingMode` + `WorkLog.buildingName` 正确 ✅
  - **回归：**
    - T-001 对照组不变（#5268=5800 / #5271=1974.9 / #5321=1800 / #5420=2000 / #5421=300 / #5422=300 / #5423=2000）✅
    - 项目 818 总产值仍为 0 ✅
  - **构建：** `npx next build` 全部路由正常编译 ✅
  - **备份 & 脚本：** `dev.db.bak-20260415-dengchuan` 存在, `merge-dengchuan-buildings.mjs` 存在 ✅
- 验证通过。

---

### ⏸️ T-002  [P1] Mixed 合同价目表区分"单价条"vs"打包条"

**状态：** ⏸️ 暂缓，等 T-001 跑完看还剩多少 case 再定是否做

**背景：**
T-001 方案 A 是单位兼容止血。但根本问题——合同里有些条目本质是「合同总量 × 单价 = 总包金额」的打包写法——没解决。清华园合同里几乎所有条目都是打包写法，不是按实际发生量结算。

**初步方向：**
- 给 `PriceItem` 增加 `priceNature` 字段（枚举：`unit` 单价条 / `lumpsum` 打包条）
- OCR 解析时按启发式猜默认值（单位是 `项`/`次` + 数量是 1 → 打包；其他 → 单价），用户可手工改
- 产值计算时"打包条"不参与 `findBestPriceMatch`，而是走类似包干价的占比分配

**为什么现在不做：** 涉及 schema 改动 + OCR 解析改动 + 前端编辑 UI + 所有历史合同要回填。先看 T-001 是否已把清华园这类问题止住。

---

### ⏸️ T-003  [废弃] 清理规则救不了的 3 条历史工作记录

**状态：** ⏸️ 废弃 — 2026-04-22 归并到 T-020「数量异常」tab，由新规则统一处理

这 3 条在 2026-04-14 的批量修复里被跳过（无明确数量）：

| ID | 日期 | 项目 | 内容 | 备注 |
|---|---|---|---|---|
| 5296 | 2026-02-03 | 弥渡县城乡供排水...五标段谷芹自来水厂边坡支护 | 编制边坡变形监测方案 | 只是方案编制 |
| 5304 | 2026-02-12 | 洱源县邓川镇...新州村委会活动室 | 沉降观测 | 备注：现场无建筑物，取消检测 |
| 5337 | 2026-03-25 | ...新梅和收集站 | 沉降出报告 | 只是出报告 |

**建议的处理方式（待用户定）：**
- 方案 a. 直接删除（保留一条口径"不计产值"）
- 方案 b. 改 `manualTotalValue=0`，备注标"方案编制不计产值"
- 方案 c. 保留原样，让报表自动 workload-only

---

### ⏸️ T-004  [废弃] 20 条 pending-area-share 补占比

**状态：** ⏸️ 废弃 — 2026-04-22 归并到 T-021「缺占比」tab（支持批量分摊），不再单独处理

这些是面积合同 / 包干价合同下的工作记录，系统需要用户逐条输入"该记录对应合同的多少占比"。

- **项目 819「洱源产业园区邓川片区污水收集及处理项目」（lumpsum 合同）—— 15 条：** workLogId 5276、5277、5319、5322、5329、5335、5336、5340、5426、5339、5342、5363、5434、5435、5436
- **项目 824「滇西大西校区学生宿舍改造项目」（lumpsum 合同）—— 5 条：** workLogId 5297、5298、5301、5302、5305
- 注：T-007 已完成，原 851 的 4 条记录（5363/5434/5435/5436）已经并入 819

操作路径：工作记录页 → 找到这些记录 → 补填 `allocationShare` 字段 → 自动触发重算

---

### ⏸️ T-005  [废弃] 13 个无合同项目排查

**状态：** ⏸️ 废弃 — 2026-04-22 归并到 T-022「项目未绑合同」tab，通过 `project.noContractExpected` 字段区分"待绑合同"和"正常无合同"

受影响项目 ID：814、816、820、821、825、828、829、841、842、843、845、846、848

---

### ⏸️ T-006  [废弃] 项目 812「大理经济技术开发区凤仪片区排水防涝设施建设项目」剩 4 条 no-price-match

**状态：** ⏸️ 废弃 — 2026-04-22 归并到 T-020「缺单价」tab，统一走收件箱修复

在 Codex 2026-04-14 合同匹配补强后剩的 4 条焊缝/测点类记录，现有合同价目表里没有对应项或上下文仍不够明确。

先不硬改匹配规则，后续按合同补项或人工核对再决定是否处理。

---

### ✅ T-010  [P0] 放开子项 + 单体建筑共存 & 合并古生种质二期保育室

**状态：** ✅ 已完成（Claude 2026-04-16 复验通过）

**背景：**

用户在用 T-009 整理完合同 #14 的子项后发现：项目 #829「古生种质资源二期1#保育室项目」和 #842「古生种质资源二期2#保育室项目」本质是同一个子项"古生种质资源二期"下的两栋单体建筑（1#保育室、2#保育室），应该合并成一个项目。

但当前代码有一条互斥约束——**phase 非空时不允许 buildingMode=true**——阻止了这种操作。这条约束是 T-007 设计时为防止概念混乱加的，但实际业务出现了"一个子项下还有多栋单体"的真实场景，必须放开。

**当前 #829 和 #842 的数据：**

- #829 phase="古生种质资源二期1#保育室项目", buildingMode=false, 9 条 worklog
- #842 phase="古生种质资源二期2#保育室项目", buildingMode=false, 9 条 worklog
- 两个都挂在合同 #14 下，合同统一大项目名已被 T-009 整理为"云南省洱海流域山水林田湖草沙一体化保护和修复工程"

**目标状态：**

- #829 保留，name="云南省洱海流域山水林田湖草沙一体化保护和修复工程", phase="古生种质资源二期", buildingMode=true
- #842 删除（合并进 #829）
- 原 #829 的 9 条 worklog: buildingName="1#保育室"
- 原 #842 的 9 条 worklog: buildingName="2#保育室"
- 合并后 #829 共 18 条 worklog

---

**实施分两步：**

#### 第 1 步：放开 phase + buildingMode 互斥约束（代码改动）

以下三处代码里的"phase + buildingMode 互斥"检查需要**删除或改为允许**：

1. **`src/src/app/api/projects/route.js` 第 37-38 行（POST 新建项目）：**
   ```js
   // 删除这段
   if (buildingMode && trimmedPhase) {
       return NextResponse.json({ error: '单体建筑模式下不能再填写阶段/子项' }, { status: 400 });
   }
   ```

2. **`src/src/app/api/projects/route.js` 第 111-112 行（PUT 更新项目）：**
   ```js
   // 删除这段
   if (nextBuildingMode && nextPhase) {
       return NextResponse.json({ error: '单体建筑模式下不能再填写阶段/子项' }, { status: 400 });
   }
   ```

3. **`src/src/app/api/projects/batch-rename/route.js` 第 86-90 行：**
   ```js
   // 删除这段
   if (project?.buildingMode && item.phase) {
       return NextResponse.json({
           error: `项目 #${item.id} 已开启单体建筑模式，不能再填写子项`,
       }, { status: 400 });
   }
   ```

4. **`src/src/lib/projectResolver.js` 的 `findProjectByDisplayName` 里第 62-71 行**：当前 buildingMode 匹配条件是 `phase: null`，需要改成**也能命中有 phase 的 buildingMode 项目**。改法：
   ```js
   // 当前：
   const buildingModeProject = await prismaClient.project.findFirst({
       where: { name: parsed.name, phase: null, buildingMode: true },
   });
   
   // 改成：
   const buildingModeProject = await prismaClient.project.findFirst({
       where: { name: parsed.name, buildingMode: true },
   });
   ```
   但要注意：如果同名项目里既有 phase=null 的 buildingMode 项目（如邓川 #819），又有 phase="xxx" 的 buildingMode 项目（如古生种质 #829），需要优先匹配 **phase 更贴近的那个**。具体策略：
   - 先看有没有 `name=parsed.name AND phase=parsed.name的前缀匹配 AND buildingMode=true` — 这个太复杂，用不上
   - 简化做法：**先精确匹配 phase，再 fallback 到 phase=null 的 buildingMode 项目**。这块现有代码已经是这个顺序了（第 51-58 行先查 phase 精确匹配），只需要保证 buildingMode 分支也能命中 phase 非空的项目就行

   最终改法：
   ```js
   // 在 phasedProject 精确匹配之后、buildingModeProject 分支里：
   const buildingModeProject = await prismaClient.project.findFirst({
       where: { name: parsed.name, buildingMode: true },
       orderBy: [{ phase: 'asc' }],  // phase=null 的排后面
   });
   ```
   这样如果有 `name="云南省洱海流域..." AND buildingMode=true AND phase="古生种质资源二期"` 的项目存在，导入"古生种质资源二期（3#保育室）"时会精确走 phase 精确匹配分支命中它（因为 splitProjectNameAndPhase 解析只取最外层括号）。

   等一下——**实际导入的名字格式**不是"大项目名（子项）（单体）"两层括号，而是"古生种质资源二期1#保育室"这种平铺写法。所以 resolver 不需要改这么复杂。关键路径是：
   - 用户通过 **UI 操作**（batch-rename + 合并 + 开 buildingMode）来整理数据
   - 未来工作记录导入时，名字会直接带"云南省洱海流域...（古生种质资源二期1#保育室）"，splitProjectNameAndPhase 解析出 `name="云南省洱海流域..." phase="古生种质资源二期1#保育室"`
   - 精确匹配找不到 phase="古生种质资源二期1#保育室" 的项目
   - 然后走 buildingMode 分支，找到 name="云南省洱海流域..." buildingMode=true 的项目（可能有多个：#819 邓川、#829 古生种质）
   - 需要进一步看 parsed.phase 是否以某个 buildingMode 项目的 phase 开头来匹配

   **这部分逻辑比较复杂，本任务先不改 resolver 的自动匹配**。用户整理完数据后，未来的新导入如果名字格式仍然五花八门，可以另开任务优化。本任务只做：删互斥约束 + 数据合并脚本 + UI 联动。

5. **项目管理页 `src/src/app/master/projects/page.js`**：如果有前端级的"buildingMode 开关在 phase 非空时禁用"逻辑，也要放开

6. **项目详情页 `src/src/app/master/projects/[id]/page.js`**：如果"单体建筑列表"区块在 phase 非空时隐藏，也要放开

#### 第 2 步：一次性数据修复脚本

在 `src/scripts/` 下新建 `merge-gusheng-buildings.mjs`，默认 dry-run，加 `--apply` 才真写库：

1. 备份 `src/prisma/dev.db` → `src/prisma/dev.db.bak-20260416-gusheng`
2. 把 project #829 的 9 条 worklog 的 `buildingName` 设为 `"1#保育室"`
3. 把 project #842 的 9 条 worklog 的 `buildingName` 设为 `"2#保育室"`
4. 把 project #829 的 `phase` 改为 `"古生种质资源二期"`
5. 调 `POST /api/projects/merge { targetId: 829, sourceIds: [842] }`
   - 验证返回 movedWorkLogs=9、movedDetectionRecords=9、deletedProjects=1
6. 把 project #829 的 `buildingMode` 设为 `true`

dry-run 要打印明细，`--apply` 再执行。

---

**验收标准（Claude 会复验这些）：**

1. 代码层面：
   - `POST /api/projects` 和 `PUT /api/projects` 不再拒绝 phase + buildingMode 共存
   - `PUT /api/projects/batch-rename` 不再拒绝 buildingMode 项目填 phase
2. 数据层面（跑完脚本后）：
   - project #842 不存在（被删）
   - project #829: name="云南省洱海流域山水林田湖草沙一体化保护和修复工程", phase="古生种质资源二期", buildingMode=true
   - #829 共 18 条 worklog：9 条 buildingName="1#保育室"、9 条 buildingName="2#保育室"
   - 合同 #14 下从 6 个项目变成 5 个
3. 页面层面：
   - `/master/projects/829` 详情页能同时看到 phase="古生种质资源二期" 和单体建筑列表（1#保育室 9 条、2#保育室 9 条）
4. 回归：
   - 项目 #819（邓川）buildingMode=true, phase=null, 15 条 worklog 不受影响
   - T-001 对照组产值不变
   - 合同 #14 下其他 4 个项目（#814/#825/#828/#834）不变

---

**触达文件（限制范围）：**

代码：
- `src/src/app/api/projects/route.js`（删互斥检查）
- `src/src/app/api/projects/batch-rename/route.js`（删互斥检查）
- `src/src/app/master/projects/page.js`（如有前端级互斥逻辑则放开）
- `src/src/app/master/projects/[id]/page.js`（如有前端级互斥逻辑则放开）

脚本：
- `src/scripts/merge-gusheng-buildings.mjs`（新建，一次性）

文档：
- `docs/api_reference.md`（更新约束说明）
- `改动说明.md`（顶部追加 [T-010] 记录）

**禁区：**
- **不要改 `projectResolver.js`**（本任务不扩展自动匹配逻辑，那是后续任务的事）
- 不要改 `worklogMatching.js`
- 不要改合同 API / worklog API
- 不要碰 T-001/T-007/T-008/T-009 已完成的逻辑
- 不要改 `改动说明.md` 里已有的 2026-04-16 及之前的记录

---

**实施记录：**
- 触达文件：
  - `src/src/app/api/projects/route.js`
  - `src/src/app/api/projects/batch-rename/route.js`
  - `src/src/app/master/projects/page.js`
  - `src/scripts/merge-gusheng-buildings.mjs`
- 实施内容：
  1. 删除了 `src/src/app/api/projects/route.js` 中 `POST` 和 `PUT` 的 `buildingMode` 及 `phase` 互斥校验。
  2. 删除了 `src/src/app/api/projects/batch-rename/route.js` 中统一整理项目的 `buildingMode` 及 `phase` 互斥校验。
  3. 修改了 `src/src/app/master/projects/page.js` 前端页面逻辑，去除了开启 `buildingMode` 会 disabled 或清空 `phase` 的操作。
  4. 新建并运行了数据修复脚本 `src/scripts/merge-gusheng-buildings.mjs`。脚本先行备份，将 #842 记录合并至 #829 并且按来源分别标上 `buildingName`="1#保育室" 和 "2#保育室"，并开启 #829 的 `buildingMode=true` 且令 `phase`="古生种质资源二期"。
- 验证命令：
  - `node scripts/merge-gusheng-buildings.mjs`
  - `node scripts/merge-gusheng-buildings.mjs --apply`
- 验证结果：
  - 成功备份并合并，合并端点返回正确结果（movedWorkLogs=9, deletedProjects=1）。
  - 合并后 #829 验证工作记录总数18，单体名全部落位，#842被成功移出。
（该部分由 Antigravity 完成）

**验证反馈：**
- 2026-04-16 Claude 复验：
  - **互斥约束已删除：** `src/src/app/api/projects/route.js:37`、`src/src/app/api/projects/route.js:109`、`src/src/app/api/projects/batch-rename/route.js:86` 三处原 `phase + buildingMode` 互斥检查均已改成 `// Removed phase check for buildingMode` ✅
  - **构建：** `npx next build` 全绿 ✅
  - **数据（Prisma 实测）：**
    - project #842 不存在（已删除）✅
    - project #829: name="云南省洱海流域山水林田湖草沙一体化保护和修复工程", phase="古生种质资源二期", buildingMode=true ✅
    - #829 共 18 条 worklog：1#保育室 9 条、2#保育室 9 条 ✅
    - 合同 #14 下从 6 个项目变成 5 个 ✅
  - **回归：**
    - 项目 #819（邓川）buildingMode=true, phase=null, 15 条 worklog，未受影响 ✅
    - 合同 #14 下其他 4 个项目（#814/#825/#828/#834）phase 不变，buildingMode=false ✅
    - T-001 对照组：项目 #818 累计产值仍为 0 ✅
  - 遗留：`projectResolver.js` 的 2 级嵌套自动匹配逻辑未扩展（任务内已明确为"禁区"），后续工作记录导入里如果出现"云南省洱海流域...（古生种质资源二期1#保育室）"这类名字，自动匹配可能仍不完美 → 另开 T-011 解决智能化导入匹配
- 验证通过。

---

### ✅ T-009  [P1] 合同项目组概览 + 批量整理子项名称 + 工作记录按子项展示

**状态：** ✅ 已完成（Claude 2026-04-16 复验通过）（该部分由 Codex 完成）

**背景：**

用户在录入合同后发现合同 #14「云南省洱海流域山水林田湖草沙一体化保护和修复工程」下挂了 6 个项目，它们其实都是同一个大工程下面的不同子项。但由于这些项目是工作记录导入时自动按各自的全名创建的，现在项目名五花八门、phase 都为 null，在系统里各是各的台账，看不出是一组。

**当前数据（实测 2026-04-16）：**

| ID | 当前 project.name | phase | workLogs |
|---|---|---|---|
| 814 | 云南省洱海流域山水林田湖草沙一体化保护和修复工程（一标段）-洱源县入洱海河流（弥茨河等4条）水生态环境保护和修复项目 | null | 2 |
| 825 | 云南省洱海流域山水林田湖草沙一体化保护和修复工程-大理市洱海东部面山林草生态修复项目 | null | 1 |
| 828 | 云南省洱海流域山水林田湖草沙一体化保护和修复工程（四标段）-大理市苍山生物多样性生态恢复及保护项目 | null | 1 |
| 829 | 云南省洱海流域山水林田湖草沙一体化保护和修复工程--古生种质资源二期1#保育室项目 | null | 9 |
| 834 | 云南省洱海流域山水林田湖草**砂**一体化保护和修复工程（一标段）-洱源县永安江和罗时江...新梅和收集站 | null | 2 |
| 842 | 云南省洱海流域山水林田湖草沙一体化保护和修复工程--古生种质资源二期2#保育室项目 | null | 9 |

用户诉求：
1. 在项目详情页看到"这 6 个项目同属合同 #14"时，能**在同一个界面里整理整个大项目的名称和各子项的名称**
2. 工作记录也能在这个界面里**按子项分开展示**，一眼看全
3. 整理完后，项目列表里这些项目显示成"大项目名（子项）"的统一格式

**目标状态示例：**

整理后 6 个项目的 `name` 统一为 `"云南省洱海流域山水林田湖草沙一体化保护和修复工程"`，各自 phase 设为：
- #814: `"一标段-弥茨河等4条河流水生态修复"`
- #825: `"洱海东部面山林草生态修复"`
- #828: `"四标段-苍山生物多样性保护"`
- #829: `"古生种质资源二期1#保育室"`
- #834: `"一标段-新梅和收集站"`
- #842: `"古生种质资源二期2#保育室"`

（以上只是建议示例，最终由用户自己定；Codex 只做 UI 让用户能改）

---

**实施分两步：**

#### 第 1 步：项目详情页新增"合同项目组"区块 + 批量整理

`src/src/app/master/projects/[id]/page.js` + `src/src/app/api/projects/[id]/route.js`

**API 层（GET `/api/projects/[id]`）：**
- 当前已返回项目基本信息 + workLogs + detectionRecords
- 新增：如果该项目有 `contractId`，额外查同 `contractId` 的所有兄弟项目，每个带上 `{ id, name, phase, buildingMode, _count: { workLogs } }` 和 workLogs 的摘要列表（id, workDate, testContent, quantity, unit, buildingName, staffMembers）
- 返回时放在 `siblingProjects` 字段里，按 id 排序
- 同时返回合同基本信息（contractNo, pricingMode 等）放在 `contract` 里（现在可能已经有了）

**前端页面：**

在项目详情页**顶部**（合同信息下方、工作记录上方），如果 `siblingProjects.length >= 1`（除了自己还有兄弟），显示一个"合同项目组"区块：

```
┌──────────────────────────────────────────────────────┐
│ 📋 合同项目组 — SSGS-CBKZB-FW-20230831-002          │
│                                                       │
│ 大项目名称：[____________________________] [统一修改] │
│                                                       │
│ ┌ 子项列表（可编辑）──────────────────────────────┐  │
│ │ #814  [一标段-弥茨河等4条...    ]  2条工作记录   │  │
│ │ #825  [洱海东部面山林草...      ]  1条工作记录   │  │
│ │ #828  [四标段-苍山生物多样...   ]  1条工作记录   │  │
│ │ #829  [古生种质资源二期1#保... ]  9条工作记录   │  │
│ │ #834  [一标段-新梅和收集站     ]  2条工作记录   │  │
│ │ #842  [古生种质资源二期2#保... ]  9条工作记录   │  │
│ └─────────────────────────── [保存全部修改] ──────┘  │
└──────────────────────────────────────────────────────┘
```

交互：
- **大项目名称**：一个输入框，默认值取当前所有兄弟的最长公共前缀（或第一个项目的 name）。用户改了后点"统一修改"→ 确认对话框 → 所有同合同项目的 `name` 统一改成这个值
- **子项列表**：每行一个 phase 输入框，默认值取当前 `phase`（没有就留空）。用户可自由编辑。如果项目原名里含有子项信息（如"（一标段）-xxx"），可以尝试提取作为 placeholder 提示用户
- **保存全部修改**：一次请求把所有修改的项目 `{ id, name, phase }` 批量更新

#### 第 2 步：合同项目组下方工作记录按子项分组展示

在"合同项目组"区块下方，把所有兄弟项目的工作记录合并展示，**按子项（project）分组**：

```
┌──────────────────────────────────────────────────────┐
│ 📑 全部工作记录（合同项目组汇总：24 条）              │
│                                                       │
│ ▼ 古生种质资源二期1#保育室（9 条）                    │
│   2026-03-10  布 10点                                 │
│   2026-03-10  测 10点                                 │
│   ...                                                 │
│                                                       │
│ ▼ 古生种质资源二期2#保育室（9 条）                    │
│   2026-03-25  一层：12圆 3根                          │
│   ...                                                 │
│                                                       │
│ ▼ 一标段-弥茨河等4条河流水生态修复（2 条）            │
│   ...                                                 │
└──────────────────────────────────────────────────────┘
```

每条记录行显示：日期、检测内容、数量+单位、人员。可折叠/展开。

---

**批量更新 API：**

新增 `PUT /api/projects/batch-rename`（或用 `PUT /api/contracts/[id]/project-group`，Codex 选一条清楚的就行）：

```json
// 入参
{
  "contractId": 14,
  "parentName": "云南省洱海流域山水林田湖草沙一体化保护和修复工程",
  "projects": [
    { "id": 814, "phase": "一标段-弥茨河等4条河流水生态修复" },
    { "id": 825, "phase": "洱海东部面山林草生态修复" },
    ...
  ]
}
```

行为：
1. 验证所有 `projects[].id` 确实都挂在 `contractId` 下
2. 事务内：逐个更新 `project.name = parentName`、`project.phase = phase`
3. 对有名字变动的项目，触发 retroactive 重算（name 变了可能影响 worklog 匹配，但同 contractId 下其实不影响——这个视情况决定是否要做，不强求）
4. 返回更新后的项目列表

---

**验收标准（Claude 会复验这些）：**

1. 打开 `/master/projects/829`（古生种质资源二期1#保育室），能看到"合同项目组"区块，列出 6 个兄弟项目
2. 在"大项目名称"输入框里改成 `"云南省洱海流域山水林田湖草沙一体化保护和修复工程"`，给 6 个子项各填一个 phase → 点保存 → 刷新后所有 6 个项目的 `name` 都统一了、`phase` 各不同
3. 合同项目组区块下方能看到所有 24 条工作记录，按子项分组展示
4. 回到 `/master/projects` 列表页，这 6 个项目应该显示成 `"大项目名（子项）"` 格式
5. **回归验证：**
   - 项目 819 (buildingMode=true) 不受影响
   - 项目 818 产值仍为 0
   - T-001 对照组不变
   - 合同 #9 下的 #811/#836 不受本任务影响（本任务不自动改它们）

---

**触达文件（Codex 请限制在以下范围）：**

API：
- `src/src/app/api/projects/[id]/route.js`（GET 返回 siblingProjects）
- `src/src/app/api/projects/batch-rename/route.js`（**新建**）或 `src/src/app/api/contracts/[id]/project-group/route.js`（二选一）

前端：
- `src/src/app/master/projects/[id]/page.js`（合同项目组区块 + 工作记录分组展示）
- `src/src/app/globals.css`（如需要折叠组样式）

文档：
- `docs/api_reference.md`
- `改动说明.md`（顶部追加 [T-009] 记录）

**禁区：**
- 不要改 `schema.prisma`（这次只是利用已有的 `name` + `phase` 字段做批量改名）
- 不要改项目列表页 `/master/projects/page.js`（那边的 T-008 黄色提示和子项引导已经稳了）
- 不要改 worklog 相关接口（POST/PUT/split）
- 不要碰 `worklogMatching.js`
- 不要改 `改动说明.md` 里已有的 2026-04-16 及之前的记录

---

**实施记录：**

- 已新增 `GET /api/projects/[id]` 的 `siblingProjects` 返回，同合同项目会带上 `id / name / phase / buildingMode / _count.workLogs / workLogs 摘要`，项目详情页现在能一次看到完整合同项目组。
- 已新增 `PUT /api/projects/batch-rename`，支持一次性提交同一合同下全部项目，统一大项目名称并分别保存每个子项 `phase`；接口会拦截重复子项、缺少项目、以及单体建筑模式下误填 `phase` 的情况。
- 已在项目详情页顶部加入“合同项目组”区块，可直接编辑大项目名称和各子项，并在下方按子项分组展示同合同的全部工作记录；当前项目自己的工作记录区也保留，方便单独核对。
- 已完成真实验证：`/master/projects/829` 现可看到 6 个同合同项目和 24 条分组工作记录；临时造了一组 3 项目 / 5 条工作记录的合同项目组，已在真实页面完成批量保存，保存后 3 个项目名称统一、各自 `phase` 生效，随后已清理临时数据。
- 回归核对：项目 `819` 仍为 `buildingMode=true` 且同合同项目数为 1；项目 `818` 的累计产值仍为 `0`；合同 `#9` 下 `#811/#836` 仍保持 `phase=null`，本任务未自动改它们。 （该部分由 codex完成）

**验证反馈：**
- 2026-04-16 Claude 复验：
  - **构建：** `npx next build` 全绿 ✅
  - **数据：** 合同 #14 下 6 个项目均已有 phase（用户已通过 UI 批量整理过），总 24 条 worklog ✅
  - **API：** batch-rename 接口存在，有完整的校验逻辑（缺项/重复/冲突/单体互斥）✅
  - **回归：** 819 buildingMode=true 同合同项目数=1 ✅；818 产值=0 ✅；#9 下 #811/#836 phase=null ✅
  - **清洁：** T009 测试残留 0 ✅
- 验证通过。

---

### ✅ T-008  [P1] 同合同多项目"子项"引导 + 已存在 phase=null 项目的修复

**状态：** ✅ 已完成（Claude 2026-04-16 复验通过）

**背景：**

2026-04-15 Codex 已加过"合并 / 子项"弹窗（见 `src/src/app/master/projects/page.js` 里 `sameContractDecision` 分支），但当前实现在"子项"分支存在两个问题，叠加下来**用户选了"子项"之后 phase 仍是 null**，项目列表里显示的还是原始完整项目名，看上去和"各是各的独立项目"没区别。

**实测现状（Claude 2026-04-15 查过）：**

| 合同 | 绑定项目数 | 各项目 phase 情况 |
|---|---|---|
| **#14**「洱海流域山水林田湖草...」 mixed | **6** | 全部 phase=null（#814、#825、#828、#829、#834、#842），其中 5 个都是 2026-04-15 当天几秒内连续绑上去的，也就是说用户**确实在新弹窗出现后做了这些关联，但 phase 还是空** |
| **#9**「龙泉全民健身中心」 unit | 2 | 全部 phase=null（#811、#836）。两个项目名只差"建设项目第三方工程质量检测"后缀，更像是应该**合并**而不是拆成子项 |
| **#12**「邓川」 lumpsum | 1 | 已由 T-007 合并为 `buildingMode=true` 的单项目 819（T-008 不再覆盖这个 case） |

**根因（Claude 已读过代码，位置已定位）：**

1. **项目管理页"子项"分支完全不收集 phase**：`src/src/app/master/projects/page.js` 第 443-450 行，`handleConfirmSameContractDecision` 的 subitem 分支只调了 `saveProjectContractLink(project, contractId)`，然后 alert 一句"后面如果要区分楼栋、单体或分项，可以继续填写阶段/子项"就收工。用户根本没机会在弹窗里填 phase，事后又容易忘。
2. **合同页新建/编辑合同完全绕过子项引导**：`src/src/app/contracts/page.js` 的提交流程里压根没有 `sameContractDecision` 这套 UI，合同绑项目走的是 `/api/contracts` POST/PUT，后端（`src/src/app/api/contracts/route.js`）也从不读也不写 `phase`。所以只要走合同页建合同 → 选已有项目 → 保存这条路径，phase 永远丢失。
3. **项目列表页没有"同合同多项目但 phase 都空"的可见提示**，用户做完那 6 个绑定后不会立刻意识到"我漏了填 phase"。

---

**实施方向：**

本任务 **只改前端引导 + 最小后端联动**。不碰 schema，不改 Project 模型定义。

#### 第 1 步：项目管理页"子项"分支强制引导 phase

`src/src/app/master/projects/page.js`：

- 把"合并 / 子项"弹窗从"单选按钮 + 确认"改成：**选"子项"后在弹窗内立刻展开一个"子项 / 阶段名"输入框**。输入框：
  - placeholder: 写清楚场景，例如 `"一标段 / 宿舍楼 / 污水处理用房 等"`
  - 下方提示：`"已关联项目：XXX、YYY。建议用不同的子项名区分，避免列表里看着一样。"`（把 `linkedProjectIds` 对应的项目名列出来给用户参考）
  - 可留空但留空时弹二次确认 "子项名留空，之后在项目列表里这 N 个项目会看着一样，确定继续？"
- 用户点"确认"时：
  - 调用一个新接口（或扩张已有接口）一次性完成"绑合同 + 更新 phase"，避免两次请求竞态
  - 成功后 refreshData()
- `saveProjectContractLink` 可以扩展签名为 `saveProjectContractLink(project, contractId, { phase })`，内部合并成一次 PUT `/api/projects/{id}`，或者沿用现有两次请求但加上 phase 的同步写入

#### 第 2 步：合同页在"保存合同"时同样走子项引导

`src/src/app/contracts/page.js` + `src/src/app/api/contracts/route.js`：

- 提交合同（新建/编辑）时，前端先检查"要绑的目标项目"和"当前合同其它已绑项目"的关系：
  - 如果**新建合同**且目标 projectId 已有合同 → 这是现有 2026-04-13 合同编辑链路已经处理过的"项目已有合同"冲突，保持原样
  - 如果**新建/编辑合同**且目标 projectName（按 name+phase 解析后）要落到某个已经绑着**这份合同的另一个项目**的同名项目，**弹同样的"合并 / 子项"选择**（复用项目管理页的组件更好，但也可以在合同页本地写一份）
- 合同 API (`route.js`) 接受新入参 `targetProjectPhase`，保存合同时若目标项目存在且 phase 需要改，一次性改；用户选"合并"时改走 `/api/projects/merge`
- 合同页里"关联项目"选择控件也要能在下拉里显示 `项目名（phase）`，让用户看到现有子项状态（现在下拉项估计只显示 name）

> **注：** 这一步的改动范围如果比较大，Codex 做到第 1 步 + 第 3 步先行也可以，把第 2 步挂 🟠 回问让 Claude 确认是否要本轮覆盖合同页路径。

#### 第 3 步：项目列表页加"子项未区分"提示

`src/src/app/master/projects/page.js`：

- 遍历项目列表，按 `contractId` 分组；如果同一个 `contractId` 的项目数 ≥ 2 且这些项目里有 >1 个 `phase` 为空，在列表顶部挂一条黄色提示条：
  - 内容示例：`"合同「XXX」下有 3 个项目未填子项 / 阶段，在列表里可能难以区分。点这里去补。"`
  - 点击跳转到该组项目的快速编辑 UI（至少能在列表里就地编辑 phase，别让用户再打开一个页面）
- 同一行项目名旁边，如果它属于"同合同多项目"但自己 phase 为空，也加个小提示 badge

#### 第 4 步：数据修复引导（只写说明，不自动改）

- 不帮用户自动拆名字填 phase（名字本身很脏，容易猜错）
- 在 `改动说明.md` 顶部那条 [T-008] 记录的"遗留/注意"里列出需要用户手动补的项目清单：
  - 合同 #14 下的 6 个项目：#814、#825、#828、#829、#834、#842（建议用户去项目列表逐个补 phase，或判断是否有应合并的）
  - 合同 #9 下的 2 个项目：#811、#836（看名字很可能是应该合并，不是子项）
  - 合同 #12 的 819：已由 T-007 处理完成，T-008 不动

---

**验收标准（Claude 会复验这些）：**

1. 打开 `/master/projects`，勾一个测试项目，把它的合同改成一个已有别的项目的合同 → 弹窗出现 → 选"子项" → **弹窗内立刻出现"子项名"输入框**，输入一个值 → 点确认 → 刷新后该项目的 phase 是刚填的值（DB 实测）
2. 同上，但子项名留空 → 点确认 → 应该有二次确认提示
3. 打开 `/contracts`，新建或编辑一份合同，把它绑到一个已有其它项目的同合同项目 → 合同保存前应该弹相同的"合并 / 子项"引导（或至少 API 不会无声地吞掉这种场景）
4. `/master/projects` 页面顶部：合同 #14 和合同 #9 下目前的 phase=null 多项目组应该被黄色提示条点出来
5. **回归验证：**
  - 项目 819 的 `buildingMode=true` / `15` 条 worklog / `4` 条单体记录状态不能被本任务意外改坏；项目 851 不应被重新造出来
   - 项目 818 及之前 T-001 所有对照产值不能变
   - 现有 `/api/contracts` 的 PUT 行为（2026-04-13 已实现的"改项目名 / 改绑项目 / 自动补算"）不能回退
6. 自测脚本 `src/scripts/verify-unit-match.mjs` 仍然 3 case 全 PASS

---

**触达文件（Codex 请限制在以下范围）：**

前端：
- `src/src/app/master/projects/page.js`（第 1、3 步主战场）
- `src/src/app/contracts/page.js`（第 2 步）
- `src/src/app/globals.css`（若需要加黄色提示条样式）

API：
- `src/src/app/api/projects/route.js`（若要扩"绑合同 + 更新 phase"的合并调用）
- `src/src/app/api/contracts/route.js`（第 2 步里合同保存时接受 targetProjectPhase / 处理冲突）

文档：
- `docs/api_reference.md`（若接口签名变了）
- `改动说明.md`（顶部追加 [T-008] 记录，"遗留"段列出需要用户人工补 phase 的项目清单）

**禁区：**
- 不要改 `schema.prisma`（phase 字段已经存在，不需要改）
- 不要动 T-007 的 schema 改动计划（`buildingMode` / `buildingName` 是 T-007 的事）
- 不要自动去给 #14 / #9 下的 6+2 个已有项目填 phase。这是用户手动活，Codex 只做 UI 让他好补
- 不要碰 `src/src/lib/worklogMatching.js`（T-001 已验通过，别污染）
- 不要改 `改动说明.md` 里已有的 2026-04-15 及之前的记录，只在最前面追加

---

**实施记录：**
- 触达文件：
  - `src/src/app/master/projects/page.js`
  - `src/src/app/contracts/page.js`
  - `src/src/app/api/contracts/route.js`
  - `docs/api_reference.md`
  - `改动说明.md`
- 实施内容：
  1. 项目管理页的“同合同”弹窗在“子项”分支里直接补了“工程阶段 / 子项”输入框，确认时把 phase 一起写入；如果留空，会先做二次确认。
  2. 项目管理页顶部新增黄色提醒，自动点出“同一合同下已经挂了多个项目，但还有项目 phase 为空”的合同组，当前能直接看到合同 #14 和 #9。
  3. 合同页补了同样的“合并 / 子项”引导。编辑合同时，如果这次保存会把合同继续挂到新的项目上，而该合同下面已经有别的项目，就先弹选择框；选“子项”时支持带 `projectPhase` 一起保存，留空必须明确确认。
  4. `/api/contracts` 现在会返回关联网项目的 `phase`，并在 POST/PUT 时识别 `projectPhase`、`sharedContractMode`、`allowBlankSharedPhase`；如果前端没先做“合并 / 子项”选择，接口会返回 409，避免再悄悄生成 phase=null 的同合同项目。
- 验证命令：
  - `cd src && npm run lint`
  - `cd src && npm run build`
  - 启动 `next start --hostname 127.0.0.1 --port 3100` 后，实调 `/api/projects`、`/api/contracts`
- 验证结果：
  - lint 与 build 均通过。
  - 实测临时合同流程里，项目页模拟保存能把子项写成 `SUB-PROJ`。
  - 合同页路径里，未带 `sharedContractMode` 的直接保存会返回 `409`；带 `sharedContractMode='subitem'` 后可正常保存，并把子项写成 `SUB-CONTRACT`。
  - 以真实当前库数据计算，黄色提醒命中的合同组包含 #14 与 #9，和任务描述一致。
- 遗留/注意：
  - 历史脏数据没有自动改。仍需用户手动处理：
    - 合同 #14 下的 6 个项目：#814、#825、#828、#829、#834、#842
    - 合同 #9 下的 2 个项目：#811、#836（这组更像应合并，不一定是子项）
  - 合同 #12 的 819 已由 T-007 处理完成，本轮未改动。

**验证反馈：**
- 2026-04-16 Claude 复验：
  - **构建：** `npx next build` 全绿 ✅
  - **项目管理页 subitem 分支：** 弹窗内已有"工程阶段 / 子项"输入框（placeholder"例如：污水处理用房、1#楼、主体结构"）；留空点确认会触发 `confirm()` 二次确认；填值后 `saveProjectContractLink(project, contractId, phase)` 一次性写入 ✅
  - **项目管理页黄色提示：** `sharedContractWarningGroups` 检测同合同 ≥2 项目且有 phase=null 的组；当前实测命中 #14 和 #9 ✅
  - **合同页 subitem/merge 弹窗：** `contracts/page.js` 完整复制了同样的 merge/subitem 选择 + phase 输入 + 留空确认 ✅
  - **合同 API 409 守卫：** PUT 路由在 contract 已有关联项目时，若不带 `sharedContractMode` 直接保存则返回 409；带 `subitem` 但 phase 空且无 `allowBlankSharedPhase` 也返回 409 ✅
  - **数据清洁：** 无 T008 测试残留（projects=0, contracts=0）✅
  - **回归：** T-001 对照组不变 ✅
  - 小问题：`projects/page.js:482` 有一行 unreachable 旧 `alert()`（在 `return` 之后），不影响功能，可以后续顺手清理
- 验证通过。

---

## 历史已完成任务

### ✅ 2026-04-15 — 一批 2026-04-15 Codex 独立完成的改动（非 T-编号）

这 5 条在本任务队列之外独立完成，列在 `改动说明.md` 顶部，Claude 事后审阅确认：

1. 同合同项目关联新增"合并 / 子项"选择（Project.phase UX）
2. 合并后项目名变成问号修复（`projectNameRepair.js`）
3. 项目详情页补全工作记录显示
4. 项目详情页自动补齐缺失明细
5. 同名项目按子项区分并修复重复显示（`projectDisplayName.js` / `projectResolver.js`）

其中 3、4、5 为 T-007 打下了基础（项目详情页已经能显示全部 worklog，resolver 已经按 display name 匹配）。

> 注：第 1 条"合并 / 子项"选择的 UX 虽然已做，但用户反馈使用下来"phase 还是不会被填进去"，这部分作为 T-008 另立新任单独修（不是这 5 条的回退）。

### ✅ T-001 2026-04-15

见上方任务详情。Claude 2026-04-15 复验通过，项目 818 2026-01 产值从 741,563 降到 0，对照组不变，单位兼容自测脚本全 PASS。
